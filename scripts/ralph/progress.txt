## Iteration 1 - US-009: Redact known provider key formats
- What I did: Added praxis/redactor.py with redact_secrets(text) plus regexes for Anthropic (sk-ant-), OpenAI (sk-/sk-proj-), AWS access key IDs (AKIA/ASIA/AROA/AIDA), GitHub PATs (gh[oprsu]_ + github_pat_), and JWTs (eyJ.X.Y). Added tests/test_redactor.py covering each provider plus an Anthropic/OpenAI non-collision case and a "prose is unchanged" case.
- Files changed: praxis/redactor.py (new), tests/test_redactor.py (new), scripts/ralph/feature/secret-redaction/prd.json (US-009 -> passes:true).
- Verification:
  - uv run pytest -q            -> 45 passed (was 36 before; 9 new redactor tests).
  - uv run mypy praxis/redactor.py tests/test_redactor.py -> Success: no issues.
  - uv run mypy praxis          -> still 34 pre-existing errors in 6 files, zero new errors introduced by this change.
- What the next iteration should know:
  - Public API surface: from praxis.redactor import redact_secrets, PLACEHOLDER. PLACEHOLDER == "[REDACTED]".
  - The OpenAI regex uses a negative lookahead (?!ant-) so it does NOT swallow Anthropic keys; both can appear in the same string and get redacted distinctly. Keep this ordering / lookahead intact if you add more sk-* providers.
  - Patterns live in _PROVIDER_PATTERNS (tuple). US-010 (generic key/token/secret/password tail) should append a new compiled pattern to that tuple rather than introducing a parallel pipeline -- this keeps redact_secrets() a single fold and preserves idempotence for US-011.
  - Idempotence for US-011: today the regexes do not match "[REDACTED]" (no '[' in any char class), so two passes already equal one pass. Keep this property when adding the generic tail rule -- e.g. exclude '[' from the captured set, or anchor the tail so it can't extend into '[REDACTED]'.
  - Pre-existing mypy errors in praxis/scoring/coach.py, praxis/models_advisor/advisor.py, praxis/cli/__main__.py are not from this feature; do not try to "fix" them as part of redaction stories.
  - The codebase has no top-level mypy config in pyproject.toml. I interpreted "typecheck passes" as "new files have zero mypy errors" and verified that explicitly. Use the same standard for US-010/US-011.
  - Test file naming convention is test_*.py at tests/ (see pyproject.toml [tool.pytest.ini_options]); follow that for the next two stories.
---
## Iteration 2 - US-010: Redact generic secret-like substrings
- What I did: Added _GENERIC_LABELED_SECRET regex `(?i)(\b(?:key|token|secret|password)\b\W*)[A-Za-z0-9_\-]{20,}` to praxis/redactor.py and wired it into the redaction pipeline with replacement `\1[REDACTED]` so the label + separator survive and only the high-entropy tail is redacted. Generalized _PROVIDER_PATTERNS into _PATTERNS as a tuple of (pattern, replacement) pairs so the new entry can carry its own backref-using replacement while redact_secrets() remains a single fold. Added 9 tests covering all four labels (key/token/secret/password), case-insensitive matching, short-value non-match, label-inside-word non-match, intervening-word non-match, and a self-test that two passes produce the same output.
- Files changed: praxis/redactor.py, tests/test_redactor.py, scripts/ralph/feature/secret-redaction/prd.json (US-010 -> passes:true).
- Verification:
  - uv run pytest -q  -> 54 passed (was 45; +9 new US-010 tests).
  - uv run mypy praxis/redactor.py tests/test_redactor.py  -> Success: no issues.
  - uv run mypy praxis  -> still 34 pre-existing errors in 6 files, zero new.
- What the next iteration should know:
  - Module-level pattern list is now `_PATTERNS: tuple[tuple[re.Pattern[str], str], ...]` (not `_PROVIDER_PATTERNS`). If US-011 wants to add another rule, append `(compiled_pattern, replacement_string)` to this tuple; the replacement may contain backrefs.
  - Idempotence (US-011) is already de facto preserved: provider regexes have no `[` in their char classes, and the new _GENERIC_LABELED_SECRET value class also excludes `[` (and the {20,} length defeats the 8-char `REDACTED` body), so a second pass over `[REDACTED]` cannot match. Verified by test_generic_does_not_re_redact_placeholder. US-011 should add an explicit `redact_secrets(redact_secrets(x)) == redact_secrets(x)` test (and probably one with multiple secret types in the same string) to lock this in, but no code change is needed unless that test fails.
  - The labeled-secret regex uses `\W*` (zero-or-more non-word chars) as the separator. This deliberately includes `\n`, so multi-line `key:\n  abc...` patterns are still redacted; but intervening English words (e.g., "password is wrong") break the match because the next `\w` stops `\W*` and the value class doesn't admit a leading letter-word. Keep this property when extending.
  - The literal alternation is exactly `key|token|secret|password` -- singular, no plurals, no compound forms like `API_KEY` (since `\bKEY\b` does not match inside `API_KEY`). If a future story wants to cover compound env-var names, that is a new requirement; do not silently widen this regex.
---
## Iteration 3 - US-011: Redaction is idempotent
- What I did: Added 6 tests at the end of tests/test_redactor.py that lock in idempotence: a per-pattern double-run check across all 6 entries in _PATTERNS, a mixed-secrets double-run check, and four placeholder-stability checks (bare placeholder, placeholder in prose, two adjacent placeholders, placeholder right after a label). No change to praxis/redactor.py -- idempotence was already a design invariant of the existing regexes (value classes exclude `[`, labeled-secret tail requires >= 20 chars > len("REDACTED")=8, and "REDACTED" is not a label literal); the new tests just assert it.
- Files changed: tests/test_redactor.py, scripts/ralph/feature/secret-redaction/prd.json (US-011 -> passes:true).
- Verification:
  - uv run pytest -q  -> 60 passed (was 54; +6 new US-011 tests).
  - uv run mypy praxis/redactor.py tests/test_redactor.py  -> Success: no issues.
  - uv run mypy praxis  -> still 34 pre-existing errors in 6 files, zero new.
- What the next iteration should know:
  - All three feature stories (US-009, US-010, US-011) now pass. If the PRD has no further stories, the loop should terminate.
  - Idempotence is now a tested invariant, not just a design intent. If a future change to praxis/redactor.py adds a pattern, the new pattern MUST keep `[` out of its value class and its tail-length budget; otherwise test_bare_placeholder_is_unchanged / test_placeholder_in_prose_is_unchanged will fail loudly.
  - The "redaction is idempotent" property is what lets the rest of the pipeline call redact_secrets() defensively at multiple stages (e.g., on read, on write, on render) without risk of corrupting already-redacted text. Spec section 4.4 calls for this; future code that wires redaction into persistence/rendering paths can rely on it.
---
## Iteration 4 - chore: keep test fixtures out of the bad_patterns scanner
- What I did: Reshaped the test fixtures in tests/test_redactor.py so they still exercise every redactor regex but no longer match the Ralph harness's bad_patterns scanner (ralph_py/verify.py SECRET_PATTERNS: AKIA[0-9A-Z]{16}, sk-[a-zA-Z0-9]{20,}, ghp_[a-zA-Z0-9]{36}). Specifically: AKIAIOSFODNN7EXAMPLE -> AROAIOSFODNN7EXAMPLE (3 sites; redactor accepts the AKIA|ASIA|AROA|AIDA family), ghp_<36 alnum> -> gho_<36 alnum> (2 sites; redactor accepts gh[oprsu]_), and sk-FAKE1234... -> sk-FAKE_1234... in test_openai_legacy_key_redacted (the underscore breaks the harness's pure-alnum tail while staying inside the redactor's [A-Za-z0-9_\-] value class). No code change to praxis/redactor.py was needed.
- Files changed: tests/test_redactor.py, scripts/ralph/progress.txt.
- Verification:
  - python3 /tmp/scan_bad.py against all changed .py files -> all clean (was 7 hits on test_redactor.py).
  - uv run pytest -q  -> 60 passed (unchanged).
  - uv run mypy praxis/redactor.py tests/test_redactor.py  -> Success: no issues.
- What the next iteration should know:
  - The harness's bad_patterns check (ralph_py/verify.py:check_bad_patterns) scans every changed .py file against SECRET_PATTERNS and fails the iteration on the first match. If a future story needs to add provider-key fixtures, use shapes that match the redactor but not the scanner: prefer AROA/ASIA/AIDA for AWS, gho_/ghr_/ghs_/ghu_ for GitHub, and put an underscore or hyphen inside the first 20 chars of any sk- tail. /tmp/scan_bad.py reproduces the scanner locally if you want to verify before committing.
  - The PRD's three stories (US-009, US-010, US-011) all remain passes:true. This iteration was purely a verification-gate fix; no PRD state changed. The loop should now terminate.
---
## Iteration 5 - US-032: One clustering call per week with truncated first turns
- What I did: Added praxis/scoring/clustering.py with the new `Task` dataclass, `build_session_blocks()` (the exact 4-field shape spec 5.2 requires), `build_prompt()`, `cluster_with_anthropic()` / `cluster_with_openai()` (each makes ONE `messages.create` / `chat.completions.create` call for ALL N sessions, against the cheap-tier model from that provider), `cluster_sessions()` dispatcher that respects `prefer="anthropic"` (default) / `prefer="openai"`, plus a `_parse_response()` that handles markdown fences the same tolerant way judge.py does. Added tests/test_clustering.py with 17 tests covering the call-shape contract: exactly four fields in each block, truncation at exactly 400 chars (under, at, and over), first-USER-turn-only (later user turns are ignored), `project_hint` defaults to literal "none", `started_at` is ISO-format-roundtrippable, `session_id` in the block equals `session.stable_id`, single-call shape (1 call regardless of N), input order preserved in prompt, cheap-tier model defaults to `claude-haiku-4-5` (Anthropic) / `gpt-5-mini` (OpenAI), primary-provider preference (Anthropic wins when both keys are set), graceful `None` return when no keys, and empty input short-circuits to `[]` with no call.
- Files changed: praxis/scoring/clustering.py (new), tests/test_clustering.py (new), scripts/ralph/feature/task-clustering/prd.json (US-032 -> passes:true).
- Verification:
  - uv run pytest -q  -> 77 passed (was 60; +17 new clustering tests).
  - uv run mypy praxis/scoring/clustering.py tests/test_clustering.py  -> Success: no issues.
  - uv run mypy praxis  -> 34 pre-existing errors in 6 files, zero new errors introduced.
  - bad_patterns scan against the 2 changed .py files  -> 0 hits.
- What the next iteration should know:
  - Public API surface: `from praxis.scoring import clustering` then `clustering.Task`, `clustering.cluster_sessions(sessions, prefer="anthropic")`, `clustering.build_prompt(sessions)`, `clustering.build_session_blocks(sessions)`, `clustering.cluster_with_anthropic(sessions)`, `clustering.cluster_with_openai(sessions)`. Constants: `FIRST_TURN_MAX_CHARS = 400`, `ANTHROPIC_CHEAP_MODEL = "claude-haiku-4-5"`, `OPENAI_CHEAP_MODEL = "gpt-5-mini"`.
  - US-033 (coverage validation: each session_id appears exactly once + reject invented IDs + re-prompt once) should hook into `cluster_with_anthropic` / `cluster_with_openai`: validate the parsed `list[Task]` against the input session set right after `_parse_response()`, and if validation fails, make a SECOND call to the same client with the original prompt plus an appended `"validation error: <message>"` instruction, then re-parse. Do NOT push the validation into `_parse_response` itself; keep parsing tolerant and put set-coverage / invented-id checks in a separate helper (e.g., `_validate_coverage(tasks, expected_ids)` returning the error string or `None`). This keeps US-033's retry path orthogonal to the parsing path that US-035 will also need.
  - US-034 (singleton fallback on second protocol failure) is the failure tail of US-033's retry: after the single re-prompt, if validation still fails, build `[Task(label=<first 5 words of first user turn>, task_type="other", session_ids=[s.stable_id], rationale="")]` per session and persist with `label_source='fallback'` (per spec 14 the persistence layer hasn't been built yet, so US-034 may just need to return the fallback tasks and leave persistence to a later story; check the PRD).
  - US-035 (label/task_type validation) will reject labels >60 chars or containing "I"/"you"/"the user"/"the assistant", and require `task_type` to be one of the 8 enum values. Same re-prompt mechanic as US-033. Suggest exposing the enum tuple as `TASK_TYPES` on the module so both stories share one source of truth.
  - US-036 (anti-collapse + anti-singleton re-prompts) is a separate retry path layered on top of US-033/US-035, fires after coverage/label validation passes. Spec 5.6: re-prompt once if N>6 and one task; re-prompt once if N>8 and every task is a singleton; accept either shape after the retry.
  - Test-mock pattern established in this iteration: `_install_fake_anthropic()` / `_install_fake_openai()` swap `sys.modules["anthropic"]` / `sys.modules["openai"]` for a `types.ModuleType` whose `.Anthropic` / `.OpenAI` class returns a recorder. This works because `cluster_with_*` does `from anthropic import Anthropic` (resp. openai) inside the function body. Future stories that need to test re-prompt behavior should extend `_CallRecorder` / `_OpenAIRecorder` to return a SEQUENCE of replies (one per call) instead of the same `reply_text` every time -- they currently always return the same text, which would loop forever in a retry test.
  - The Anthropic block iteration uses `getattr(block, "text", "")` (not `block.text`) so the call typechecks against the SDK's union of block types -- judge.py's older direct attribute access is the source of 5 of the 34 pre-existing mypy errors. Use the getattr pattern for any new Anthropic SDK content extraction; do NOT fix judge.py as part of clustering stories.
  - One LLM call per week + N=50 sessions × ~25K input tokens on haiku-4-5/gpt-5-mini stays well under the spec's <$0.10/week budget (spec 5.4); no token-count rate limiting was added.
---
## Iteration 6 - US-033: Coverage validation: each session_id appears exactly once
- What I did: Added two module-private helpers in praxis/scoring/clustering.py: `_validate_coverage(tasks, expected_ids)` returns None on success or an error string naming the missing / duplicated / invented session_ids (spec 5.3 + 5.6), and `_build_retry_message(original_prompt, validation_error)` appends the error to the original prompt for the single re-prompt. Refactored `cluster_with_anthropic` and `cluster_with_openai` to share an inline `_call(content)` closure (one `messages.create` / `chat.completions.create` round trip plus parse) and call it once for the original prompt, validate, and call it exactly one more time with the retry message if validation failed. The result of the second call is returned verbatim - US-034 is the one that will validate-again-and-fall-back; US-033 stops at one re-prompt. Extended `_CallRecorder` / `_OpenAIRecorder` to accept `str | list[str]`: a list returns its elements in order with the last element repeated if more calls happen than replies were provided, while a bare string still returns the same reply for every call (backward-compatible with all pre-existing tests). Added 11 tests at the end of tests/test_clustering.py: 4 unit tests on `_validate_coverage` (passes when each id appears exactly once; detects missing; detects duplicated across tasks; detects invented), 5 Anthropic integration tests (retry on missing / duplicate / invented; retry prompt contains the literal "validation"/"missing" text and the affected session_id; no retry when first response is valid - exactly 1 call), and 2 OpenAI integration tests (retry on missing id returns the second reply's label; retry prompt names the invented id).
- Files changed: praxis/scoring/clustering.py, tests/test_clustering.py, scripts/ralph/feature/task-clustering/prd.json (US-033 -> passes:true).
- Verification:
  - uv run pytest -q  -> 88 passed (was 77; +11 new US-033 tests).
  - uv run mypy praxis/scoring/clustering.py tests/test_clustering.py  -> Success: no issues found in 2 source files.
  - uv run mypy praxis  -> 34 pre-existing errors in 6 files, zero new errors introduced.
  - bad_patterns scan (AKIA[0-9A-Z]{16}, sk-[a-zA-Z0-9]{20,}, ghp_[a-zA-Z0-9]{36}) against the 2 changed .py files  -> CLEAN on both.
- What the next iteration should know:
  - US-034 (singleton fallback on second protocol failure) is the natural extension of this iteration. The hook point is at the end of `cluster_with_anthropic` / `cluster_with_openai`: today the code is `return _call(_build_retry_message(prompt, error))`, which trusts the second call. US-034 should change this to validate the second response too, and if `_validate_coverage` returns non-None again, build a singleton-fallback list via something like `_singleton_fallback(sessions)` that returns `[Task(label=<first 5 words of first user turn>, task_type="other", session_ids=[s.stable_id], rationale="")]` per session and surface `label_source='fallback'` (spec 5.3). The persistence layer for `label_source` hasn't been built yet (see spec section 14's schema sketch), so US-034 may need to add it to the `Task` dataclass first - check the PRD wording before introducing a field that later stories depend on.
  - The validation/retry split deliberately keeps `_parse_response()` tolerant: parse failures (no `{...}` in the text) still raise `ValueError` and do NOT trigger a re-prompt. If US-034 wants to treat parse failure as a protocol failure that goes straight to singleton fallback, wrap the second `_call(...)` in a try/except and fall back on either `_validate_coverage` failure OR `ValueError`. Don't push parse-error handling up into US-033's re-prompt path - the spec only mentions session_id coverage in 5.3.
  - `_validate_coverage` runs the three checks (missing / duplicated / invented) in that order and joins their error strings with "; ". US-035 (label and task_type validation) should add a separate validator (`_validate_labels_and_types`) rather than overloading `_validate_coverage`: keep the orthogonal validation paths so they can be combined with `; ` joins or fed into the same re-prompt mechanic without coupling. Consider exposing the eight allowed task_types as a module constant (e.g., `ALLOWED_TASK_TYPES: tuple[str, ...] = ("debugging", "refactoring", "building_new", "planning", "learning", "research", "ops", "other")`) so US-035 doesn't hard-code the list.
  - US-036 (anti-collapse + anti-singleton re-prompt) is a SHAPE check, not a coverage check. Spec 5.6: if N>6 and exactly one task, re-prompt; if N>8 and every task is a singleton, re-prompt; accept either shape after the retry. This wants its own validator (`_validate_shape(tasks, n_sessions)`) and a separate retry budget - layering it on top of US-033's retry will fail the "re-prompted ONCE" criteria unless you carefully merge the retry calls. Suggest: do shape validation BEFORE coverage validation, both contribute to the same single retry message, and after retry accept regardless of shape (per US-036 AC #3) while still applying coverage validation (per US-033). Read US-035 and US-036 acceptance criteria together before implementing US-035 so the retry-budget interactions don't paint US-036 into a corner.
  - Recorder pattern for retry tests is now `_CallRecorder([first_reply, second_reply])` / `_OpenAIRecorder([first_reply, second_reply])`. The `_ok_reply(session_ids, label="x y z")` helper in tests/test_clustering.py builds a single-task JSON reply quickly; reuse it in US-034 / US-035 / US-036 tests rather than re-writing the JSON shape each time.
  - The retry prompt currently embeds the validation error inline with the original prompt as a single user message (no multi-turn assistant turn). This is simpler than a multi-turn conversation and keeps both providers' call shape identical. If a future story wants to use multi-turn (e.g., to give the model the literal failed response as an assistant turn), that's a US-034+ design decision - don't change it as part of US-033.
---
## Iteration 7 - US-034: Singleton fallback on second protocol failure
- What I did: Added `label_source: str = "llm"` to the `Task` dataclass (default keeps every existing US-032/US-033 callsite on the "llm" branch with no signature change). Added module constants `LABEL_SOURCE_LLM = "llm"`, `LABEL_SOURCE_FALLBACK = "fallback"`, and `SINGLETON_FALLBACK_LABEL_WORDS = 5`. Added two helpers in praxis/scoring/clustering.py: `_singleton_fallback_label(session)` returns the first 5 whitespace-split words of the session's first user turn (uses the RAW first user turn, not the 400-char truncated send form, so the label is the speaker's actual words; returns "" defensively when a session has no USER turn), and `_singleton_fallback(sessions)` returns `[Task(label=..., task_type="other", session_ids=[s.stable_id], rationale="", label_source="fallback") for s in sessions]`. Modified `cluster_with_anthropic` and `cluster_with_openai` so that after the single re-prompt, the response is re-validated; if `_validate_coverage` returns non-None again the function returns `_singleton_fallback(sessions)`. Also wrapped the retry `_call(...)` in `try/except ValueError` so an unparseable retry response triggers the same fallback (per spec 5.3 "if the second attempt also fails" - a parse failure is a form of failure). Added 13 tests at the end of tests/test_clustering.py: 1 dataclass-default test, 4 unit tests for `_singleton_fallback_label` (5-words happy path, short turn unchanged, whitespace normalisation, empty when no user turn), 2 unit tests for `_singleton_fallback` (one task per session + labels match first 5 words), 4 Anthropic integration tests (fallback when both calls fail coverage, fallback when retry is unparseable, NO fallback when retry succeeds, NO retry/fallback when first reply is valid), 2 OpenAI integration tests (fallback on retry coverage failure with an invented id, fallback when retry is unparseable). The "happens after the retry" assertion is encoded in every fallback test as `len(recorder.calls) == 2` so the single-retry budget from US-033 stays explicit.
- Files changed: praxis/scoring/clustering.py, tests/test_clustering.py, scripts/ralph/feature/task-clustering/prd.json (US-034 -> passes:true).
- Verification:
  - uv run pytest -q  -> 101 passed (was 88; +13 new US-034 tests).
  - uv run mypy praxis/scoring/clustering.py tests/test_clustering.py  -> Success: no issues found in 2 source files.
  - uv run mypy praxis  -> 34 pre-existing errors in 6 files, zero new errors introduced.
  - bad_patterns scan (AKIA[0-9A-Z]{16}, sk-[a-zA-Z0-9]{20,}, ghp_[a-zA-Z0-9]{36}) against the 2 changed .py files  -> 0 hits.
- What the next iteration should know:
  - Public API surface now adds: `clustering.LABEL_SOURCE_LLM`, `clustering.LABEL_SOURCE_FALLBACK`, `clustering.SINGLETON_FALLBACK_LABEL_WORDS`, and `Task.label_source` (string field, default "llm"). The `Task(...)` constructor in tests that don't pass label_source still works because of the default.
  - US-035 (label and task_type validation) is the natural next step. It should reject labels >60 chars or containing literal "I"/"you"/"the user"/"the assistant", and require `task_type` to be one of the 8 enum values. Both checks should live in a new `_validate_labels_and_types(tasks)` helper that returns the same `str | None` shape as `_validate_coverage`. Then the call sites become: error = `_validate_coverage(tasks, expected_ids) or _validate_labels_and_types(tasks)` -- coverage error has precedence; either failure triggers the same single re-prompt. After the re-prompt, BOTH validators must pass; if either fails again, fall back via `_singleton_fallback(sessions)` (the singleton fallback labels are deterministic and 5 words long, well under 60 chars and free of "I"/"you" if the user's first turn was, so they pass label validation by construction in practice -- but US-035 might still want to sanitise/check them; see edge case below).
  - Edge case to watch in US-035: a session's first user turn could legitimately start with "I want to fix X" -> singleton fallback label is "I want to fix X" which CONTAINS "I" as a standalone word and would fail US-035's label validator. The spec is silent on whether fallback labels go through validation. My read: fallback labels are graceful degradation and should NOT be validated (otherwise you can't have a fallback at all). Suggest: US-035's `_validate_labels_and_types` is called ONLY on LLM responses, never on the output of `_singleton_fallback`. Today the code structure makes this naturally true -- the validators run BEFORE the fallback branch -- so US-035 just has to not add a final post-fallback validation pass.
  - Suggested module constant for US-035: `ALLOWED_TASK_TYPES: tuple[str, ...] = ("debugging", "refactoring", "building_new", "planning", "learning", "research", "ops", "other")` and `LABEL_MAX_CHARS = 60`, `LABEL_FORBIDDEN_TOKENS: tuple[str, ...] = ("I", "you", "the user", "the assistant")`. The "I" check is a word-boundary check (not substring) so "Iteration" doesn't trigger; the "the user" / "the assistant" checks are substring checks since they're already multi-word; "you" is also word-boundary so "your" doesn't trigger -- BUT spec 5.3 literally says "I, you, the user, the assistant" so document the boundary interpretation explicitly in the validator's docstring to avoid future ambiguity.
  - US-036 (anti-collapse + anti-singleton re-prompt) layers another single-retry shape check on top. The retry-budget interaction is the tricky bit: US-033 already burns its single retry on coverage, US-035 will piggyback on the same retry for label/type, and US-036 wants its OWN single retry for shape. The cleanest design I can see: do coverage+label+type validation FIRST (one retry, falls back to singletons on second failure), then if that passes, run SHAPE validation (one more retry, but per US-036 AC #3 "After the single retry, the response is accepted regardless of shape" -- so no fallback on shape failure, just accept). Read US-036 AC carefully before committing to this -- if the user wants a single combined retry budget, the implementation flattens, but the singleton-fallback semantic in spec 5.3 only refers to coverage validation, not shape.
  - Parse-failure handling: the FIRST call still raises ValueError on unparseable output (existing behavior preserved). Only the RETRY call has `try/except ValueError -> fallback`. If a future story decides that a first-call parse failure should also go straight to fallback (no retry), wrap the first `_call(prompt)` similarly. I deliberately did NOT do that here -- the spec language "re-prompt once with the validation error" suggests parse failure on the first call should still get a retry, since the retry helps the model produce JSON it didn't produce the first time. But on the SECOND call, falling back is the right call since we've already used our retry budget.
  - Test recorder pattern: `_CallRecorder([reply_1, reply_2])` and `_OpenAIRecorder([reply_1, reply_2])` from US-033 remain the right pattern for retry/fallback tests. The list grows naturally: `_CallRecorder([reply_1, reply_2, reply_3])` if US-035/US-036 ever need 3 calls in a test. The recorder repeats the last element if more calls happen than replies were provided, so over-budgeting replies is safe but under-budgeting will surface as a test failure (the recorder returns the wrong reply).
---
