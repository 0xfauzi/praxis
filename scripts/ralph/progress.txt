# progress.txt: union of factory component logs

## Iteration 1 - US-079: install-weekly creates and loads the plist
- What I did: Added `praxis install-weekly` subcommand backed by `praxis/cli/install_weekly.py`. Generates ~/Library/LaunchAgents/co.praxis.weekly.plist (Label=co.praxis.weekly) using `schedule.day/hour/minute` from ~/.praxis/config.toml and loads it with `launchctl load -w`. Idempotent re-runs unload first. CLI exits 4 with stderr message on launchctl failure (InstallWeeklyError surfacing returncode + stderr).
- Files changed: praxis/cli/install_weekly.py (new), praxis/cli/__main__.py (cmd_install_weekly + subparser), tests/test_install_weekly.py (new, 12 tests), scripts/ralph/feature/macos-launch-agent/prd.json (passes=true), scripts/ralph/progress.txt (this entry).
- Verification: `uv run pytest -q` -> 1024 passed (12 new + 1012 existing). `uv run pyright praxis/cli/install_weekly.py` -> 0 errors. The 3 pre-existing pyright errors on cli/__main__.py (WeeklyRunSummary vs RunSummary) are unchanged before/after this commit; confirmed by git stash + pyright comparison.
- What the next iteration should know:
  - Patterns established:
    * Subprocess is monkeypatched via `praxis.cli.install_weekly.subprocess.run`; tests do not call real launchctl. Reuse this stub fixture (`fake_launchctl`) when implementing US-080 uninstall-weekly.
    * The plist path resolver `plist_path(home=...)` accepts a home override but defaults to `Path.home()`, which the tmp_home conftest fixture patches; that's how tests land the plist inside tmp_path.
    * `PLIST_LABEL = "co.praxis.weekly"` is exported from install_weekly.py; uninstall-weekly should import and reuse it (don't duplicate the string).
    * `_WEEKDAY_BY_NAME` maps 'sunday'..'saturday' to 0..6 per Apple's launchd Weekday spec.
  - Hooks for upcoming stories:
    * US-080 (uninstall-weekly): needs to `launchctl unload <plist>` then delete the file; if file missing or unload fails, must still exit 0 per AC. Reuse `plist_path()` and `PLIST_LABEL`.
    * US-081 / US-082 (--notify path inside `praxis week`): the existing `_post_notify` in cli/__main__.py already shells out to osascript and includes the trajectory label in the title. ACs may want osascript moved to a dedicated module so test stubbing is easier, and the BODY (not just title) should mention `~/.praxis/latest.html`. Today the body only references `~/.praxis/weeks/{iso}.html` -- check whether US-081's AC ("opening ~/.praxis/latest.html") wants the body changed.
    * US-083 (non-macOS path): cmd_install_weekly currently prints a placeholder stderr message on non-Darwin and returns 0. That placeholder is the seam to flesh out: print the systemd timer / Task Scheduler XML to stdout AND save to ~/.praxis/install-weekly-snippet.txt.
  - Gotchas:
    * The pre-existing pyright errors in cli/__main__.py (WeeklyRunSummary type mismatch at lines 145/150/332) are noise from an orchestrator/reports type-coupling issue unrelated to this story. Don't try to "fix" them as part of macos-launch-agent stories unless explicitly assigned.
    * `_resolve_praxis_command()` falls back to `[sys.executable, '-m', 'praxis.cli']` when `shutil.which('praxis')` returns None; this matters when running install-weekly outside an activated venv. Tests don't exercise the fallback path, but they should not need to.
---

## Iteration 2 - US-080: uninstall-weekly removes the plist
- What I did: Added `praxis uninstall-weekly` subcommand backed by `uninstall_weekly_macos()` in praxis/cli/install_weekly.py. It runs `launchctl unload` on the existing plist (best-effort) then deletes the file, returning True. When the plist does not exist, it returns False without shelling out, and the CLI exits 0 in both cases. Non-Darwin platforms exit 0 with an explanatory stderr message.
- Files changed: praxis/cli/install_weekly.py (added uninstall_weekly_macos + docstring update), praxis/cli/__main__.py (cmd_uninstall_weekly + 'uninstall-weekly' subparser), tests/test_install_weekly.py (added 6 tests, import uninstall_weekly_macos, header updated), scripts/ralph/feature/macos-launch-agent/prd.json (passes=true), scripts/ralph/progress.txt (this entry).
- Verification: `uv run pytest -q` -> 1030 passed (6 new + 1024 existing). `uv run pyright praxis/cli/install_weekly.py` -> 0 errors. `uv run pyright praxis/cli/__main__.py` -> 3 errors, identical to baseline before this commit (verified via `git stash` round-trip); they are the pre-existing WeeklyRunSummary issue documented in iteration 1.
- What the next iteration should know:
  - Patterns established:
    * `uninstall_weekly_macos(home=None)` returns `bool` indicating whether anything was actually removed. The CLI uses this to choose between "Removed..." vs "No weekly LaunchAgent found at..." messaging.
    * The "tolerate launchctl unload failure" rule -- launchctl returns non-zero when the job is not currently loaded, and we cannot distinguish that from a real error. uninstall always proceeds to delete the file. If a future story needs to surface real unload errors, it must add a separate code path that distinguishes them (e.g., parse stderr for the "Could not find specified service" text), not change this default.
  - Hooks for upcoming stories:
    * US-081 (--notify body / osascript): `_post_notify` in cli/__main__.py currently puts the trajectory label in the title only and uses a `~/.praxis/weeks/{iso}.html` body. AC US-081 wants the body to mention `~/.praxis/latest.html` -- check whether to change the body text or introduce a `latest.html` symlink/copy. Existing notify happy-path tests (if any) live in tests/ for the week command; grep for `_post_notify` before touching.
    * US-082 (trajectory in body + non-crashing osascript): `_post_notify` already wraps the osascript call in a broad try/except, but the AC says "if osascript fails for any reason, the run logs the failure and still exits 0" -- need a test that covers a FileNotFoundError (osascript binary missing) explicitly, since today's try/except is best-effort but unverified by tests.
    * US-083 (non-macOS install-weekly path): the placeholder "not yet implemented" stderr message in cmd_install_weekly is the seam to flesh out -- print the systemd / Task Scheduler XML to stdout AND save to ~/.praxis/install-weekly-snippet.txt. The matching non-macOS branch for cmd_uninstall_weekly is already a sensible no-op ("non-macOS has no scheduled job to remove."); US-083 may not need to touch it.
  - Gotchas:
    * `fake_launchctl` fixture is reusable but accumulates calls across `install_weekly_macos()` + `uninstall_weekly_macos()` in the same test, so slice the list from the install call count before asserting on uninstall's behavior (see test_uninstall_weekly_unloads_and_deletes_plist).
    * `cmd_uninstall_weekly` imports from `praxis.cli.install_weekly` inside the function (not at module top) to match the existing install pattern -- keeps non-macOS imports off the hot path.
---

## Iteration 3 - US-081: macOS notification via osascript
- What I did: Reworked `_post_notify` in praxis/cli/__main__.py so the AppleScript title is the fixed string "Praxis weekly read is ready" and the body is "Open ~/.praxis/latest.html to read." (per spec 13.2 / AC US-081). Dropped the `week_iso` parameter (no longer referenced) and the title-override-by-trajectory branch; the trajectory-label-in-body work is US-082's. Updated the caller in `cmd_week` to call `_post_notify()` with no args, removing the now-dead `traj_label` calculation.
- Files changed: praxis/cli/__main__.py (refactored `_post_notify`, simplified caller), tests/test_cli.py (added 4 US-081 tests + `fake_osascript` fixture), scripts/ralph/feature/macos-launch-agent/prd.json (passes=true), scripts/ralph/progress.txt (this entry).
- Verification: `uv run pytest -q` -> 1034 passed (4 new + 1030 existing). `uv run pyright praxis/cli/__main__.py tests/test_cli.py` -> 4 errors, all pre-existing (3 WeeklyRunSummary on __main__.py + 1 "pytest could not be resolved" on test_cli.py). Confirmed unchanged via `git stash` round-trip; my edits introduce 0 new pyright errors.
- What the next iteration should know:
  - Patterns established:
    * `fake_osascript` fixture in tests/test_cli.py monkeypatches `praxis.cli.__main__.subprocess.run` and records every call. Reuse it for US-082's "osascript fails -> run still exits 0" test (just have the stub raise FileNotFoundError or return non-zero).
    * `_post_notify()` now takes no arguments. US-082 must re-add `trajectory_label: str | None` (it was removed in this iteration since US-081 has no use for it) and the caller in `cmd_week` needs to recompute `summary.trajectory.label.value.title()` before passing it through. The pre-edit code is in the git log at 381704b for reference.
    * The body text MUST contain the literal substring `~/.praxis/latest.html` -- US-082's expanded body (with trajectory label) must keep that substring intact, otherwise `test_notify_body_mentions_latest_html` will fail.
    * The title MUST stay literally "Praxis weekly read is ready" -- `test_notify_title_is_fixed_string` asserts `'with title "Praxis weekly read is ready"'` verbatim. US-082 adds the trajectory label to BODY, not title.
  - Hooks for upcoming stories:
    * US-082 (trajectory label in body + osascript failure tolerance): Re-add `trajectory_label` parameter to `_post_notify`, then weave the label into the body (spec section 13.2 example: "Praxis: Drifting this week. Open ~/.praxis/latest.html for the detail."). Add an explicit test that monkeypatches subprocess.run to raise `FileNotFoundError("osascript missing")` and asserts the CLI still exits 0 -- today's broad try/except handles it but no test verifies the contract.
    * US-083 (non-macOS install-weekly): the `fake_osascript` fixture is irrelevant; that story is about `install-weekly` printing a systemd/Task Scheduler snippet to stdout and saving it to `~/.praxis/install-weekly-snippet.txt`. See progress entries for iter 1/2 for the placeholder seam in `cmd_install_weekly`.
  - Gotchas:
    * `latest.html` is a SYMLINK created by the HTML digest writer (praxis/reports/digest_html.py, US-064), not by `_post_notify`. Tests don't verify the symlink exists at notification time -- the contract is only that the body MENTIONS the path. If a future change to the notify path tries to validate the symlink before notifying, it would couple notification to the HTML writer's symlink semantics, which is a regression in separation of concerns.
    * The notification posts AFTER `_weekly_html_path(target_week).write_text(...)` in `cmd_week`. The "after writing the HTML" half of the AC is structural (it's the code order), so the test asserts an HTML file exists at notify time as a smoke check rather than ordering subprocess.run vs write_text directly.
    * Tests monkeypatch `sys.platform` via `monkeypatch.setattr(sys, "platform", "darwin")`. monkeypatch reverts at teardown, so the patch is fully scoped to one test even though `sys` is a process-wide module.
---

## Iteration 4 - US-082: Notification includes trajectory label and never crashes the run
- What I did: Re-added the `trajectory_label: str | None = None` parameter to `_post_notify` in praxis/cli/__main__.py and wove the label into the body using the format "<Label> this week. Open ~/.praxis/latest.html for the detail." (spec 13.2 example). The caller in `cmd_week` now maps `summary.trajectory.label.value` through a small `_TRAJECTORY_LABEL_DISPLAY` dict so raw enum strings like `stable_engaged` render as user-facing `Engaged`. Hardened the subprocess block to capture stdout/stderr and log non-zero exits explicitly, plus the broad `except Exception` already in place catches `FileNotFoundError` / `PermissionError` etc. so the run still exits 0.
- Files changed: praxis/cli/__main__.py (`_TRAJECTORY_LABEL_DISPLAY` dict, `_post_notify` takes `trajectory_label`, capture+log non-zero exit, caller wiring in `cmd_week`), tests/test_cli.py (5 new US-082 tests), scripts/ralph/feature/macos-launch-agent/prd.json (passes=true), scripts/ralph/progress.txt (this entry).
- Verification: `uv run pytest -q` -> 1039 passed (5 new + 1034 existing). `uv run pyright praxis/cli/__main__.py tests/test_cli.py` -> 4 errors, identical to baseline before this iteration (verified via `git stash` round-trip). My edits introduce 0 new pyright errors.
- What the next iteration should know:
  - Patterns established:
    * Enum -> display mapping for trajectory labels lives in `_TRAJECTORY_LABEL_DISPLAY` at module top of cli/__main__.py. Keys mirror `TrajectoryLabel` enum values ("learning", "stable_engaged", "stable_passive", "atrophying", "insufficient_data"). The fallback for unknown labels is `raw.title()`. If a new TrajectoryLabel variant lands, add it here so the notification body stays user-facing.
    * The display dict is identical in spirit to the terminal renderer's `_TRAJ_TITLE` (praxis/reports/terminal.py L98), but kept separate to avoid a cross-module import from the CLI hot path. If a third surface needs it, lift it into a shared helper module.
    * To stub `run_weekly` in CLI tests, monkeypatch `praxis.cli.__main__.run_weekly` (not the source module) -- the CLI captured a reference at import time. See `test_notify_body_uses_user_facing_trajectory_label`.
    * Constructing a minimal `WeeklyRunSummary` in tests requires only: week_iso, sessions=[], tasks=[], judge_results={}, moments=[], selection=None, snapshot (with session_count>=1), rendered_html, rendered_terminal, elapsed_seconds, trajectory. All other fields default. `ProfileSnapshot` can be built directly (not via `from_scores`) when you want a known session_count without seeding rows.
  - Hooks for upcoming stories:
    * US-083 (non-macOS install-weekly): the trajectory/notification path is done -- US-083 is fully on `cmd_install_weekly`'s non-Darwin branch. The placeholder stderr message is the seam: replace it with code that prints the systemd timer or Task Scheduler XML to stdout AND saves it to `~/.praxis/install-weekly-snippet.txt`. Exit 0. The matching non-macOS branch in `cmd_uninstall_weekly` is already a sensible no-op -- US-083 likely doesn't need to touch it.
    * The schedule config (day/hour/minute) for the systemd / Task Scheduler snippets lives in `~/.praxis/config.toml` under `[schedule]`. See praxis/cli/install_weekly.py for how the macOS path loads it; reuse the same config reader.
  - Gotchas:
    * The notification body's prefix is `"<Label> this week."`. For `INSUFFICIENT_DATA` this becomes `"Reading this week."` -- mildly awkward but matches the spec 13.2 example phrasing. If product wants different phrasing per-label, change the mapping; don't add special-case branches around the label string concatenation.
    * `subprocess.run(..., capture_output=True, text=True)` is now used so we can log stderr on non-zero exits. The previous US-081 implementation passed neither, which left the subprocess writing directly to the parent's stderr. The change is invisible to existing tests because `fake_osascript` returns a CompletedProcess with empty stdout/stderr.
    * Tests that stub `run_weekly` MUST set `tmp_home` AND `fake_api_key` fixtures, otherwise the exit-2 (no API key) gate fires before `run_weekly` is reached. See `test_notify_body_uses_user_facing_trajectory_label`.
---

## Iteration 5 - US-083: Non-macOS prints snippet and exits cleanly
- What I did: Added pure renderers `build_systemd_snippet` (Linux user-timer) and `build_task_scheduler_snippet` (Windows XML) plus a `write_non_macos_snippet(praxis_home=None, *, platform_override=None)` dispatcher in `praxis/cli/install_weekly.py`. The CLI's non-macOS branch in `cmd_install_weekly` now calls the dispatcher, prints the content to stdout, prints "Saved snippet to: <path>" to stdout, and exits 0. The snippet is persisted to `~/.praxis/install-weekly-snippet.txt` via the new `snippet_path()` helper. Windows is detected via `sys.platform.startswith("win") or == "cygwin"`; everything else (Linux, FreeBSD, etc.) gets systemd.
- Files changed: praxis/cli/install_weekly.py (renderers + dispatcher + day-name maps + `_format_praxis_exec`), praxis/cli/__main__.py (cmd_install_weekly non-Darwin branch rewritten), tests/test_install_weekly.py (14 new US-083 tests, ScheduleConfig import, expanded header docstring), scripts/ralph/feature/macos-launch-agent/prd.json (passes=true), scripts/ralph/progress.txt (this entry).
- Verification: `uv run pytest -q` -> 1053 passed (14 new + 1039 existing). `uv run pyright praxis/cli/install_weekly.py praxis/cli/__main__.py tests/test_install_weekly.py` -> 4 errors, identical to baseline before this iteration (3 WeeklyRunSummary on __main__.py + 1 pytest import on test_install_weekly.py). Confirmed unchanged via `git stash` round-trip; my edits introduce 0 new pyright errors.
- What the next iteration should know:
  - All 5 PRD stories now pass. Ralph should detect <promise>COMPLETE</promise> on the next iteration.
  - Patterns established (in case follow-on stories touch this surface):
    * `build_systemd_snippet(schedule, praxis_argv)` and `build_task_scheduler_snippet(schedule, praxis_argv)` are pure -- no I/O, no config load. Tests call them directly with `ScheduleConfig(...)` to pin output without touching the tmp_home config file.
    * `write_non_macos_snippet(praxis_home=None, *, platform_override=None)` is the I/O layer: it reads `~/.praxis/config.toml`, resolves the praxis argv, dispatches by platform, writes the snippet to disk, and returns `(path, content)`. Tests pass `platform_override="linux" | "win32" | "freebsd13"` to exercise dispatch without touching `sys.platform`.
    * Day-of-week dispatch uses two new dicts: `_SYSTEMD_DAY_BY_NAME` ("sunday" -> "Sun") and `_TASKSCHEDULER_DAY_BY_NAME` ("sunday" -> "Sunday"). Both normalize via lowercase strip; invalid day strings raise `InstallWeeklyError` (so the user gets a usable error instead of an invalid unit/XML file).
    * `snippet_path(home=None)` returns `<home or PRAXIS_HOME>/install-weekly-snippet.txt`, mirroring `plist_path` in shape so tests can pass an explicit home or rely on PRAXIS_HOME via tmp_home.
  - Gotchas:
    * `_resolve_praxis_command()` calls `shutil.which("praxis")`, which on Python 3.13 dives into `_winapi.NeedCurrentDirectoryForExePath` when `sys.platform == "win32"`. That attribute is None on macOS, so the test that monkeypatches `sys.platform = "win32"` must ALSO stub `praxis.cli.install_weekly.shutil.which` to avoid an AttributeError crash. See `test_cli_install_weekly_windows_saves_task_scheduler_xml`. Tests that use the `platform_override=` parameter (not sys.platform monkeypatching) don't need this stub because `shutil.which` still sees the host's real platform.
    * The CLI prints the snippet body AND the "Saved snippet to:" line to stdout (not stderr). This matches the existing macOS branch ("Installed weekly LaunchAgent: ...") which also goes to stdout. If a user pipes stdout to a file, both lines will land there; that's deliberate so the saved unit text is self-contained.
    * Task Scheduler XML uses a fixed `StartBoundary` of `2020-01-01T<hh>:<mm>:00` so the trigger is immediately valid. If a future story needs date-aware output (e.g., to start "next Sunday"), inject a clock; don't randomize the fixed string.
---

## Iteration 6 - US-005: Specification, error-naming, and iterative-refinement signals
- What I did: Added three new int fields to `BehavioralSignals` with default=0 (specification_artifact_count, error_naming_count, iterative_refinement_count). Wrote three regex patterns in praxis/behavior/signals.py with cite-comment block pointing at Anthropic Claude Code best practices and Shen & Tamkin (2026). Wired the three counts through both `extract()` paths (empty-session and populated-session). Built tests/behavior/fixtures/{specification_artifact,error_naming,iterative_refinement}/{positive,negative}_0[1-5].json (30 JSON fixtures total) plus tests/behavior/test_expansion_signals.py which parametrises positive/negative assertions plus empty- and whitespace-only-session guards.
- Files changed: praxis/behavior/signals.py (new patterns + new fields + extract wiring), tests/behavior/__init__.py (new, empty marker), tests/behavior/test_expansion_signals.py (new, 8 tests), tests/behavior/fixtures/<3 subdirs>/<10 JSON files each> (30 new), scripts/ralph/feature/behavioral-signals-expansion/prd.json (US-005 passes=true), scripts/ralph/progress.txt (this entry).
- Verification: `uv run pytest -q` -> 1061 passed (8 new + 1053 existing baseline). `uv run mypy praxis/behavior/signals.py tests/behavior/test_expansion_signals.py` -> 0 errors on my files; 8 pre-existing errors all in praxis/behavior/trajectory.py (Anthropic SDK Union[TextBlock,...] and OpenAI/Anthropic-client mixup) confirmed unchanged via `git stash` round-trip. `uv run mypy praxis` -> 1 pre-existing syntax error in praxis/reports/digest_html.py:595 (f-string backslash) that blocks deeper analysis and is outside this feature's allowedPaths.
- What the next iteration should know:
  - Patterns established (US-006/007/008 should reuse these):
    * **Fixtures live under `tests/behavior/fixtures/<signal_name>/{positive,negative}_NN.json`** where each JSON file is a list of `{"role": "user|assistant", "content": "..."}` dicts. The loader is `_load_session` in tests/behavior/test_expansion_signals.py - copy that helper or import from it. Each signal needs exactly 5 positive + 5 negative fixtures (the loader asserts the count).
    * **New BehavioralSignals fields are dataclass-defaulted to 0** so existing positional constructors in tests/test_behavior.py (which lists 13 args) keep working. Add new fields AFTER the existing block of 13 fields; do not insert in the middle.
    * **Citation block style**: the three new patterns share one comment block at lines 81-92 of signals.py. Future signal sets should add their own labelled comment block (e.g., `# --- Expansion signals (US-006) ---`) above their patterns rather than fragmenting cites across each regex. Keep cites concrete (paper + arXiv id, or doc URL family) - the codebase consistently cites Shen & Tamkin (2026) arXiv 2601.20245.
    * **Test parametrisation**: I use `@pytest.mark.parametrize` over `(signal_dir_name, signal_attribute_name)` pairs. US-006/007/008 can extend the same pattern; new signal counts get a new parametrize tuple. For US-006's dict-valued field, dispatch differently - assert `sig.knowledge_gaps[key] >= 1` rather than `getattr(sig, attribute)`.
  - Hooks for upcoming stories:
    * **US-006 (knowledge_gaps dict)**: AC says "writes 0 for all keys when no gap detected" - this means the empty-session branch in extract() must construct the dict with all four keys at 0, NOT an empty dict. Don't forget to update the empty-user-turns branch as well (it's the second BehavioralSignals constructor in signals.py). The "precedence rule" test fixture should live alongside the other fixtures, probably under `tests/behavior/fixtures/knowledge_gaps/` with one extra `precedence_ambiguous_01.json` file that the test loads directly.
    * **US-007 (5 more flat counters)**: Same field-add pattern as US-005. Acceptance specifically requires "5+5 fixtures stable across re-runs" - the JSON file approach guarantees stability since the content is checked in. Don't use random session ids in the fixture loader; mine uses `f"fixture-{fixture_path.stem}"` deterministically.
    * **US-008 (verification_depth dict + tool_ladder int 0..4)**: tool_ladder semantics is "max ladder rung observed" - mirror this with `max(...)` over per-turn classifications, NOT `sum(...)`. The "mix of tool call + hook -> 3" test should construct a Session with mixed turns (one referencing tool calls, another referencing hooks). The Turn dataclass already has `tool_calls: list[dict[str, Any]]` so US-008 can inspect those structurally, not just by content regex.
  - Gotchas:
    * **`Role` import is "unused" per pyright** - signals.py imports `from praxis.models import Role, Session` but never references `Role`. It was a pre-existing minor pyright complaint; do not delete the import as later stories (US-008 uses Role for tool ladder detection) likely need it.
    * **The PRD mentions "primary source from Section 14"** but there is no Section 14 of the relevant signals document in this repo (PRAXIS_V0_2_SPEC.md's §14 is unrelated, about storage schema). I treated this as a placeholder and cited the actual primary sources the rest of the codebase already references (Shen & Tamkin 2026; Anthropic Claude Code best practices). US-007 mentions concrete sources (Anthropic, OpenAI Codex docs, arXiv 2506.01604) - cite those concretely.
    * **`uv run` triggers a venv install on first invocation in a fresh worktree** and creates uv.lock as a side effect; it is untracked. Don't `git stash --include-untracked` mid-iteration if you've already run uv - the stash pop will fail with an untracked-file conflict on uv.lock. Instead, compare baselines by running mypy on individual files (mypy traverses imports so it still surfaces pre-existing errors in the dependency graph; you can read which file owns each error from the report).
    * **Whitespace-only user turns are not zero-cost** - the user_turns filter in Session keeps them, so the extract() denominator becomes >0. My test `test_whitespace_only_session_has_zero_new_counts` covers this. None of the three new regexes match whitespace, so counts are still 0, but future regex-based signals must not rely on Session filtering whitespace turns.
---

## Iteration 7 - US-006: Knowledge-gap four-subtype classifier
- What I did: Added `knowledge_gaps: dict[str, int]` to `BehavioralSignals` (default_factory=`_zero_knowledge_gaps`, four keys always populated). Wrote `_classify_knowledge_gap(text)` in praxis/behavior/signals.py with documented precedence order (missing_context > missing_specs > multiple_context > unclear_instructions). Each subtype has its own helper or regex: `_is_missing_context` (bare this/it/that referent in <=100-char turns with no code-block/file-ext/Error-class/traceback/path anchor), `_is_missing_specs` (creation imperative + no spec markers), `_is_multiple_context` (>=2 transition markers), `_UNCLEAR_INSTRUCTION_MARKERS` regex (somehow/the right way/as needed/or whatever/etc). Wired the dict through both extract() return paths. Built 40 JSON fixtures (4 subtypes x 5 positive x 5 negative) under tests/behavior/fixtures/knowledge_gaps/<subtype>/, plus tests/behavior/fixtures/knowledge_gaps/precedence_ambiguous_01.json. Test suite parametrises over subtypes for positive/negative; adds explicit empty/no-gap zero-default tests; adds explicit precedence test asserting only missing_specs increments on the ambiguous turn (sum == 1, not 2).
- Files changed: praxis/behavior/signals.py (field import; KNOWLEDGE_GAP_KEYS + 5 regex/helpers + _classify_knowledge_gap + new dataclass field; wired into both extract() branches; also broadened `_SPEC_MARKERS` to accept plural inputs/outputs/goals/requirements so US-006 negative fixtures match), tests/behavior/test_expansion_signals.py (KNOWLEDGE_GAP_KEYS import; 6 new tests + parametrized helper), tests/behavior/fixtures/knowledge_gaps/<5 subdirs>/<41 JSON fixtures>, scripts/ralph/feature/behavioral-signals-expansion/prd.json (US-006 passes=true), scripts/ralph/progress.txt (this entry).
- Verification: `uv run pytest -q` -> 1072 passed (11 new + 1061 prior). `uv run pytest tests/behavior/ -v` -> 19 passed. `uv run mypy praxis/behavior/signals.py tests/behavior/test_expansion_signals.py` -> 8 errors, all pre-existing in praxis/behavior/trajectory.py (Anthropic SDK Union[TextBlock,...] and OpenAI/Anthropic client mixup) and identical to iter-6 baseline; my files produce 0 errors. `uv run mypy praxis` still blocked by the pre-existing f-string-backslash syntax error at praxis/reports/digest_html.py:595 (untouched in this iteration; outside allowedPaths and outside this feature's scope).
- What the next iteration should know:
  - Patterns established (US-007/008 should follow):
    * **Dict-valued BehavioralSignals fields use `field(default_factory=_zero_<name>)`** with the factory function defined at module top. This keeps the asdict + json.dumps round-trip clean for session_scores.signals_json (orchestrator.py L525) and makes mypy happy without a `# type: ignore`. US-008's `verification_depth: dict[str, int]` should mirror this exactly.
    * **`KNOWLEDGE_GAP_KEYS` tuple is exported** (no leading underscore) so tests can iterate the canonical key set without re-declaring it. Future dict-valued signal fields should export their key tuples similarly (e.g., `VERIFICATION_DEPTH_KEYS` for US-008).
    * **Precedence is enforced in a single `_classify_<name>` function** that short-circuits on the first match. The precedence comment block above the helper is the source of truth; the function body is a plain if/elif/elif ladder so the order is visually obvious and a future reordering only touches one place.
    * **Fixture layout: tests/behavior/fixtures/<category>/<subtype>/{positive,negative}_NN.json** for dict-valued signals (knowledge_gaps follows this pattern); flat `tests/behavior/fixtures/<signal_name>/...` for scalar US-005-style signals. The `_knowledge_gap_fixture_paths` helper asserts 5 fixtures per polarity to catch dropped files.
    * **Test parametrize uses `subtype: str`** alone (not the (signal, attribute) tuples used by US-005) because dict-valued signals share a single attribute (`knowledge_gaps`). US-008's tool_ladder_level is INT-valued (not dict) so it needs its own test shape -- don't shoehorn it into this parametrize.
  - Hooks for upcoming stories:
    * **US-007 (5 flat counters)**: same field-add pattern as US-005 -- defaulted int fields at the end of BehavioralSignals + per-turn regex + extract() wiring + fixture grid under tests/behavior/fixtures/<signal_name>/. Patterns sourced from Anthropic Claude Code best practices (plan_mode, scaffolding, recipe), OpenAI Codex docs (context_instructions), and arXiv 2506.01604 (tdd_marker). The empty- and whitespace-only-session zero-default tests in test_expansion_signals.py already parametrise over the US-005 fields; extend them as new attributes land.
    * **US-008 (verification_depth dict + tool_ladder_level int)**: verification_depth follows the US-006 dict pattern; tool_ladder_level is a new int field. AC explicitly says max-rung-observed (not sum) and the mixed tool-call+hook test must assert `level == 3` not `level == 5`. The Turn dataclass already carries `tool_calls: list[dict[str, Any]]` so tool ladder detection can inspect `turn.tool_calls` structurally for rung 2; hook references and subagent calls likely need transcript-content scanning. The ladder rules MUST live in cite-commented constants in signals.py per AC.
  - Gotchas:
    * **`_SPEC_MARKERS` was tightened in this iteration**: previously matched bare `\binput\b`/`\boutput\b`/`\bgoal\b`/`\bconstraint\b`/`\brequirement\b`/`\bnon-goal\b`; now matches `\binputs?\b`/`\boutputs?\b`/`\bgoals?\b`/`\bconstraints?\b`/`\brequirements?\b`/`\bnon-goals?\b` plus added explicit `criteria|criterion`. The change is backward compatible (all old matches still match) but US-007 may want to reference the same marker set if a "Goal:" / "Inputs:" prefix should suppress its signal too -- copy the pattern; don't redefine ad hoc.
    * **The "writes 0 for all keys when no gap detected" AC has TWO test cases**: (1) empty session (no user turns at all) and (2) populated session where no turn matches any subtype. Both must assert `set(sig.knowledge_gaps.keys()) == set(KNOWLEDGE_GAP_KEYS)` AND `sum == 0`. The default_factory alone is not enough -- the populated branch in extract() explicitly constructs the dict via `_zero_knowledge_gaps()` then increments; without the explicit reset, multiple sequential extract() calls could share a dict (Python mutable-default-arg foot-gun in older code). My implementation builds a fresh dict per call, so this is safe.
    * **The precedence test reads ONE fixture file directly** (not via the parametrized helper) because the AC asks about the relationship between two subtypes on a SINGLE user turn, not over a 5+5 grid. Don't add it to `_KNOWLEDGE_GAP_SUBTYPES` or the parametrize will think it's a real subtype.
    * **My ambiguous fixture text is short ("build me a new feature for the dashboard somehow", 49 chars)** and DELIBERATELY chosen to NOT contain a bare `this/it/that` referent; otherwise it would fire missing_context first and the precedence test would assert against the wrong winner. If a future iteration tweaks the missing_context length cap (currently 100 chars), recheck this fixture.
    * **Untracked `uv.lock` is still in the worktree** from iter-1; git status carries the same noise. Don't `git add -A` or you'll commit the lock file alongside your changes; use explicit paths to commit.
---

## Iteration 8 - US-007: Plan mode, scaffolding, TDD, recipe, context-and-instructions signals
- What I did: Added five new flat int fields to `BehavioralSignals` (plan_mode_count, scaffolding_artifact_count, tdd_marker_count, recipe_pattern_count, context_instructions_count) with default=0. Wrote five regex patterns in praxis/behavior/signals.py with a cite-comment header pointing at Anthropic Claude Code best practices, OpenAI Codex docs, and arXiv 2506.01604. Wired each count through both `extract()` paths (empty-session and populated-session). Built tests/behavior/fixtures/{plan_mode,scaffolding_artifact,tdd_marker,recipe_pattern,context_instructions}/{positive,negative}_0[1-5].json (50 new JSON fixtures). Refactored test_expansion_signals.py to use a shared `_SCALAR_SIGNAL_PAIRS` tuple so US-005 + US-007 share the parametrize, and folded the new field names into the empty/whitespace zero-default tests via the same tuple.
- Files changed: praxis/behavior/signals.py (5 new patterns + cite block + 5 new fields + extract() wiring on both branches), tests/behavior/test_expansion_signals.py (consolidated _SCALAR_SIGNAL_PAIRS, empty/whitespace tests now iterate that tuple), tests/behavior/fixtures/<5 new subdirs>/<10 JSON files each> (50 new), scripts/ralph/feature/behavioral-signals-expansion/prd.json (US-007 passes=true), scripts/ralph/progress.txt (this entry).
- Verification: `uv run pytest` -> 1082 passed (10 new + 1072 prior). `uv run mypy praxis/behavior/signals.py tests/behavior/test_expansion_signals.py` -> 8 errors, all pre-existing in praxis/behavior/trajectory.py (Anthropic SDK Union[TextBlock,...] + OpenAI/Anthropic client mixup) and identical to the iter-6 / iter-7 baseline; my files produce 0 errors. `uv run mypy praxis` still blocked by the pre-existing f-string-backslash syntax error at praxis/reports/digest_html.py:595 (untouched; outside allowedPaths and feature scope).
- What the next iteration should know:
  - One PRD story remains: US-008 (verification_depth dict + code_comprehension_count + tool_ladder_level int). After it completes, ralph should detect <promise>COMPLETE</promise>.
  - Patterns established (US-008 should reuse):
    * **`_SCALAR_SIGNAL_PAIRS` tuple at the top of test_expansion_signals.py** is now the canonical place to register a new flat-int signal -- adding a `(fixture_dir_name, attribute_name)` row hooks it into positive/negative/empty/whitespace tests in one shot. `code_comprehension_count` (US-008) is a flat int, so it slots into this tuple. `verification_depth` is dict-valued -- use the US-006 KNOWLEDGE_GAP_KEYS export pattern instead (export `VERIFICATION_DEPTH_KEYS`, write a parallel `_verification_depth_fixture_paths` helper).
    * **Citation block style for a multi-signal group**: I put the source list above the FIRST pattern in the group (`# --- Expansion signals (US-007) ---` block at signals.py L307), then a one-line "Cite:" sub-comment above each individual regex when the source differs from the group default. This keeps the long source list readable instead of duplicating it five times.
    * **Field order in BehavioralSignals matters for positional constructors**. New fields go AFTER all existing defaulted fields at the end of the dataclass (see L348-355 of signals.py). The existing positional-arg call sites in tests/test_behavior.py only pass the first 13 fields, so adding to the tail is safe.
  - Hooks for US-008 (the last story):
    * **`tool_ladder_level: int`** must be `max(...)` over per-turn rung classifications, NOT `sum(...)`. The AC explicitly says max-rung-observed, and there is a test requirement that a session mixing tool calls AND hook references yields ladder_level=3 (not 5). Sketch: classify each turn as rung 0/1/2/3/4 based on (prompt-only / tool calls / skill use / hook references / subagent calls), then `max(0, *per_turn_rungs)` over the session.
    * **`verification_depth: dict[str, int]`** with keys source_check, test_run, spot_check, blanket_accept. Follow the US-006 pattern: `VERIFICATION_DEPTH_KEYS` tuple, `_zero_verification_depth()` factory, `field(default_factory=...)`, fresh dict per extract() call. Each user turn can increment multiple keys (no precedence rule like US-006), OR only one (precedence rule) -- read the AC carefully before implementing.
    * **`code_comprehension_count: int`** is a flat int; slot into `_SCALAR_SIGNAL_PAIRS` and build 5+5 fixtures under tests/behavior/fixtures/code_comprehension/.
    * **Tool/hook/subagent ladder detection**: Turn dataclass has `tool_calls: list[dict[str, Any]]` already (praxis/models.py L38). For rung 2 (tools-on), inspect `turn.tool_calls` truthiness. For rungs 3 and 4 (hooks, subagents), there's no structured signal so transcript-content scanning is needed -- be explicit in the cite comment that these are heuristic.
  - Gotchas:
    * **Be careful with overlap between context_instructions and specification_artifact**: spec_artifact (US-005) catches `^\s*(##+\s*)?context\s*[:\-]` at line start, and context_instructions (US-007) also catches `^\s*context\s*[:\-]` via the broader pattern. This means a "Context:" prefix increments BOTH counters -- intentional, they're different lenses on the same artifact. If product later wants disjoint counters, the cleanest fix is to drop the bare "Context:" prefix from context_instructions (CLAUDE.md/AGENTS.md references and "for context" prose are enough to keep US-007 distinct).
    * **My US-007 negative fixtures avoid overlap with the corresponding positive cases for OTHER signals**. E.g., `tdd_marker/negative_03.json` is "Fix the broken test in test_auth.py" -- the word "test" appears, but my regex requires "test first" / "tdd" / "test-driven" / "red phase" / "failing test first" etc., so this stays at 0. Adjacent signal regexes are NOT cross-checked in the per-signal negative test, so US-008 should keep this discipline for cleanliness.
    * **The shared parametrize means a regression on US-005 will surface alongside any US-007 break in the same test report**. That is good test coupling for these fixture-driven signals; do not split them back apart for US-008 unless you genuinely need different test machinery (which `verification_depth`'s dict shape WILL need).
    * **Untracked `uv.lock` is still in the worktree** from iter-1; git status carries the same noise. Use explicit paths when committing; don't `git add -A`.
---
