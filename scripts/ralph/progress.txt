# progress.txt: union of factory component logs

## Iteration 1 - US-066: Fits in <80 columns
- What I did: Created the v0.2 terminal digest renderer skeleton with a strict 79-visible-char-per-line budget and the test that enforces it. Renderer lives at praxis/reports/digest_terminal.py and exposes WeeklyDigest dataclass + render() function.
- Files changed: praxis/reports/digest_terminal.py (new), tests/test_digest_terminal.py (new), scripts/ralph/feature/terminal-digest-renderer/prd.json (US-066 passes=true).
- Verification: `uv run pytest tests/` -> 702 passed. `uvx mypy praxis/reports/digest_terminal.py tests/test_digest_terminal.py --ignore-missing-imports --follow-imports=silent` -> Success, no issues.
- What the next iteration should know:
  * The v0.2 digest renderer is a SEPARATE module from praxis/reports/terminal.py (the v0.1 `praxis scan` renderer). Don't grafit US-067/068/069 onto the v0.1 terminal.py - extend digest_terminal.py instead. test_renderer_module_does_not_clobber_v01_terminal locks this.
  * WeeklyDigest currently has only `week_label` and `trajectory_headline`. US-067 must add fields for headline moment and follow_up (US-068 cost ledger + tasks, US-069 dim panel). When adding, default each to None so a missing section renders a clear placeholder rather than crashing.
  * The 80-col constraint is enforced by tests/test_digest_terminal.py::test_populated_digest_lines_under_80 and test_long_trajectory_headline_wraps_within_budget. Every new section helper MUST pass these. Use the `_wrap()` helper in digest_terminal for text wrapping; it preserves identifiers and hyphenated tokens.
  * Helper constants: MAX_LINE_WIDTH=79, INDENT="  " (2 spaces), CONTENT_WIDTH=77. Section eyebrows use `_section_rule(title)` to keep the rule width consistent across sections.
  * No em-dashes anywhere (CLAUDE.md house rule). Use hyphens or colons. Masthead uses a literal " - " between PRAXIS and the week label.
  * The visible_width() helper strips ANSI before measuring; the test uses its own _strip_ansi helper to avoid tautology with the renderer.
---

## Iteration 2 - US-067: Mandatory sections render
- What I did: Wired the three MUST-render sections (trajectory, headline moment, follow-up) into the renderer with placeholder degradation when an input is None. Added HeadlineMomentView and FollowUpView lightweight dataclasses for the renderer's view; the caller maps real Moment/FollowUp objects to these.
- Files changed: praxis/reports/digest_terminal.py (added section helpers, dataclasses, placeholders, dim_title resolver), tests/test_digest_terminal.py (added 13 new US-067 test cases), scripts/ralph/feature/terminal-digest-renderer/prd.json (US-067 passes=true).
- Verification: `uv run pytest tests/` -> 715 passed (was 702, +13 new tests). `uvx mypy praxis/reports/digest_terminal.py tests/test_digest_terminal.py --ignore-missing-imports --follow-imports=silent` -> Success, no issues. Eyeballed real-data and empty-state renders; structure is correct, all lines under 79 cols.
- What the next iteration should know:
  * Section pattern is now locked: each MUST-render section's helper always emits `_section_rule(title)` first, then either real-data lines or `_placeholder_lines(...)`. US-068 / US-069 should follow this pattern. New constants `_BODY_INDENT` (4 chars) and `_BODY_WIDTH` (75 chars) standardize content-line geometry; use `_body_line(text, ansi=...)` to compose them.
  * Placeholder copy is module-level (`_TRAJECTORY_PLACEHOLDER`, `_HEADLINE_MOMENT_PLACEHOLDER`, `_FOLLOW_UP_PLACEHOLDER`). Tests assert on them by reference, so a copy edit is a single-line change but trips the test if the rendered output drifts.
  * `_dim_title(dim_key)` resolves rubric keys to titles via `praxis.scoring.rubric.by_key` and falls through to the raw key on unknown dims (no crash). US-068's "worst dim" label per task and US-069's dim panel should reuse this helper.
  * Renderer-facing view dataclasses (HeadlineMomentView, FollowUpView) are intentionally narrow: only the fields the renderer shows. When wiring up real data, construct them at the boundary; don't import praxis.scoring.moment_selector.Moment or praxis.follow_up.FollowUp into digest_terminal.py.
  * Follow-up tracking line has two branches: pending (measured_value is None) shows `(baseline 0.32, pending)`; closed shows `0.32 -> 0.58 [improved]`. Hyphens-not-arrows is required (CLAUDE.md no em dashes; also keeps ASCII for predictable terminal widths).
  * Section order is locked by `test_mandatory_sections_render_in_spec_order`: trajectory -> headline moment -> follow-up. US-068 and US-069 add sections AFTER these (US-068's cost ledger / tasks after follow-up, US-069's six-dim panel as the footer).
---

## Iteration 3 - US-068: Compact cost ledger and tasks
- What I did: Added the COST LEDGER and WHERE THE WEEK WENT sections to digest_terminal.py with the section-helper + placeholder pattern locked in US-067. Introduced two renderer-facing view dataclasses (CostLedgerView, TaskRowView) on WeeklyDigest, both defaulting to None so the renderer degrades to a placeholder when the upstream pipeline hasn't produced data yet.
- Files changed: praxis/reports/digest_terminal.py (added CostLedgerView + TaskRowView, _cost_ledger() + _where_the_week_went() helpers, _format_spend_line / _format_tier_fit_line / _format_task_meta formatters, MAX_TASKS_RENDERED=3 cap, _COST_LEDGER_PLACEHOLDER / _TASKS_PLACEHOLDER), tests/test_digest_terminal.py (added 23 new US-068 test cases), scripts/ralph/feature/terminal-digest-renderer/prd.json (US-068 passes=true).
- Verification: `uv run pytest tests/` -> 738 passed (was 715, +23 new tests). `uvx mypy praxis/reports/digest_terminal.py tests/test_digest_terminal.py --ignore-missing-imports --follow-imports=silent` -> Success, no issues. Eyeballed real-data + empty-state renders; cost ledger reads as `This week: $14.50 vs baseline $8.20 (+$6.30)` over `Biggest: Opus on <task>`, tasks read as a 2-line block per row (`1. <label>` + DIM `   N sessions, $X.XX, worst: <dim title>`). Empty-state placeholders are dim and single-line.
- What the next iteration should know:
  * CostLedgerView has 8 fields, all renderer-shaped (pre-aggregated USD floats, pre-formatted strings). `baseline_usd: float | None = None` is the "<14 days of data" branch; the formatter switches to a literal `--` and skips the delta. The boundary code (`praxis week` glue, when wired) constructs the view from upstream pipeline data; do NOT import the cost-ledger feature module into digest_terminal.py.
  * TaskRowView carries only the 4 columns US-068's acceptance criteria fix: label, sessions, total_usd, worst_dim_key. Upstream is responsible for ranking (session count, cost as tiebreaker) and supplying at most 3; the renderer enforces `MAX_TASKS_RENDERED = 3` defensively. Singular/plural session label handled by _format_task_meta.
  * Section order now locks five sections: trajectory -> headline moment -> follow-up -> cost ledger -> WHERE THE WEEK WENT. US-069's six-dim panel lands AFTER WHERE THE WEEK WENT (as the footer, per spec section 6.2's "full six-dim panel SHOULD be a footer"). Update `test_sections_appear_in_spec_order_with_ledger_and_tasks` or its successor when adding US-069.
  * Tier-fit callout has two branches: `over_tier_sessions > 0` renders the savings amount, `== 0` renders "Tier-fit: clean" (positive reinforcement signal, not suppression). Sign-aware delta in spend line preserves "+$0.00" / "-$X.XX" distinction.
  * Task rows wrap labels using `_BODY_WIDTH - len("1. ")` and metadata using `_BODY_WIDTH - 3` (continuation indent). Long labels (60-char upstream cap) wrap cleanly. The continuation lines use a 3-space indent to align under the label after the "N. " prefix.
  * Placeholders MUST fit on a single body line (<=75 visible chars after the 4-char body indent). Initial _TASKS_PLACEHOLDER was 81 chars and split; shortened to "No task breakdown yet. Clustering surfaces tasks once it runs." (62 chars). Keep all future placeholders under 75 to avoid tripping `test_*_placeholder_when_input_missing` assertions on the literal string.
---

## Iteration 4 - US-069: Six-dim panel as footer
- What I did: Added the THE SIX DIMENSIONS section as the digest's final footer per spec section 6.2. Introduced DimRowView (dim_key, score, baseline) on WeeklyDigest as the renderer-facing view. The row formatter delegates to praxis.reports.gating.format_delta and praxis.reports.baseline_panel.format_baseline_value, so the significance gate (spec 8.3) and baseline-forming display (spec 8.4) match the HTML renderer byte-for-byte.
- Files changed: praxis/reports/digest_terminal.py (added DimRowView dataclass + dimensions field on WeeklyDigest, _format_dim_row formatter, _six_dim_panel section helper, _DIMENSIONS_PLACEHOLDER, _DIM_TITLE_COL_WIDTH=27 / _BASELINE_COL_WIDTH=12 layout constants, imports format_delta + format_baseline_value), tests/test_digest_terminal.py (added 20 new US-069 test cases), scripts/ralph/feature/terminal-digest-renderer/prd.json (US-069 passes=true).
- Verification: `uv run pytest tests/` -> 758 passed (was 738, +20 new tests). `uvx mypy praxis/reports/digest_terminal.py tests/test_digest_terminal.py --ignore-missing-imports --follow-imports=silent` -> Success, no issues. Eyeballed the full-digest render: footer reads as 6 aligned rows like `Planning before prompting    5.7/10  baseline 5.1  ↑ +0.6` with the baseline-forming row degrading to `Model–task fit               6.0/10  baseline --   (forming)`. All lines under 79 cols.
- What the next iteration should know:
  * All 4 PRD stories are now passing. The next /ralph invocation should see passes=true on US-066..US-069 and stop with `<promise>COMPLETE</promise>` rather than picking up new work.
  * Section order is now fully locked: trajectory -> headline moment -> follow-up -> cost ledger -> WHERE THE WEEK WENT -> THE SIX DIMENSIONS. `test_sections_appear_in_spec_order_with_dimensions_as_footer` is the canonical contract test.
  * DimRowView is renderer-shaped: dim_key (str), score (float, 0-10), baseline (float | None). When None, the renderer shows `baseline --  (forming)` instead of a delta. The caller computes the score and baseline (from praxis.scoring snapshots) and constructs the list in rubric order; the renderer treats the list as a pre-ranked sequence and does not iterate RUBRIC itself, parallel to how `tasks` is handled.
  * Significance gating is delegated to praxis.reports.gating.format_delta so HTML and terminal show the same `~` / `↑ +X.X` / `↓ -X.X` strings. format_baseline_value is reused for the "--" placeholder so a future tweak to the forming display is one audit point. Do NOT inline the gating logic in digest_terminal.py.
  * Layout uses _DIM_TITLE_COL_WIDTH=27 (longest rubric title is "Planning before prompting" at 25 chars; +2 breathing room) and _BASELINE_COL_WIDTH=12 ("baseline X.X"). All six rubric titles fit; future renamings up to ~27 chars would not need re-tuning. If a future rubric adds a longer title, bump _DIM_TITLE_COL_WIDTH and re-verify line widths.
  * The rubric's "Model–task fit" uses an en-dash (U+2013), NOT an em-dash (U+2014). The CLAUDE.md no-em-dash rule applies to em-dashes specifically; the en-dash already lives in praxis/scoring/rubric.py and the renderer does not rewrite rubric titles. If a future copy edit removes the en-dash from the rubric, the `_RUBRIC_TITLES` tuple in tests/test_digest_terminal.py needs to be updated too.
---
