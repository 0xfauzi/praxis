# progress.txt: union of factory component logs

## Iteration 1 - US-079: install-weekly creates and loads the plist
- What I did: Added `praxis install-weekly` subcommand backed by `praxis/cli/install_weekly.py`. Generates ~/Library/LaunchAgents/co.praxis.weekly.plist (Label=co.praxis.weekly) using `schedule.day/hour/minute` from ~/.praxis/config.toml and loads it with `launchctl load -w`. Idempotent re-runs unload first. CLI exits 4 with stderr message on launchctl failure (InstallWeeklyError surfacing returncode + stderr).
- Files changed: praxis/cli/install_weekly.py (new), praxis/cli/__main__.py (cmd_install_weekly + subparser), tests/test_install_weekly.py (new, 12 tests), scripts/ralph/feature/macos-launch-agent/prd.json (passes=true), scripts/ralph/progress.txt (this entry).
- Verification: `uv run pytest -q` -> 1024 passed (12 new + 1012 existing). `uv run pyright praxis/cli/install_weekly.py` -> 0 errors. The 3 pre-existing pyright errors on cli/__main__.py (WeeklyRunSummary vs RunSummary) are unchanged before/after this commit; confirmed by git stash + pyright comparison.
- What the next iteration should know:
  - Patterns established:
    * Subprocess is monkeypatched via `praxis.cli.install_weekly.subprocess.run`; tests do not call real launchctl. Reuse this stub fixture (`fake_launchctl`) when implementing US-080 uninstall-weekly.
    * The plist path resolver `plist_path(home=...)` accepts a home override but defaults to `Path.home()`, which the tmp_home conftest fixture patches; that's how tests land the plist inside tmp_path.
    * `PLIST_LABEL = "co.praxis.weekly"` is exported from install_weekly.py; uninstall-weekly should import and reuse it (don't duplicate the string).
    * `_WEEKDAY_BY_NAME` maps 'sunday'..'saturday' to 0..6 per Apple's launchd Weekday spec.
  - Hooks for upcoming stories:
    * US-080 (uninstall-weekly): needs to `launchctl unload <plist>` then delete the file; if file missing or unload fails, must still exit 0 per AC. Reuse `plist_path()` and `PLIST_LABEL`.
    * US-081 / US-082 (--notify path inside `praxis week`): the existing `_post_notify` in cli/__main__.py already shells out to osascript and includes the trajectory label in the title. ACs may want osascript moved to a dedicated module so test stubbing is easier, and the BODY (not just title) should mention `~/.praxis/latest.html`. Today the body only references `~/.praxis/weeks/{iso}.html` -- check whether US-081's AC ("opening ~/.praxis/latest.html") wants the body changed.
    * US-083 (non-macOS path): cmd_install_weekly currently prints a placeholder stderr message on non-Darwin and returns 0. That placeholder is the seam to flesh out: print the systemd timer / Task Scheduler XML to stdout AND save to ~/.praxis/install-weekly-snippet.txt.
  - Gotchas:
    * The pre-existing pyright errors in cli/__main__.py (WeeklyRunSummary type mismatch at lines 145/150/332) are noise from an orchestrator/reports type-coupling issue unrelated to this story. Don't try to "fix" them as part of macos-launch-agent stories unless explicitly assigned.
    * `_resolve_praxis_command()` falls back to `[sys.executable, '-m', 'praxis.cli']` when `shutil.which('praxis')` returns None; this matters when running install-weekly outside an activated venv. Tests don't exercise the fallback path, but they should not need to.
---

## Iteration 2 - US-080: uninstall-weekly removes the plist
- What I did: Added `praxis uninstall-weekly` subcommand backed by `uninstall_weekly_macos()` in praxis/cli/install_weekly.py. It runs `launchctl unload` on the existing plist (best-effort) then deletes the file, returning True. When the plist does not exist, it returns False without shelling out, and the CLI exits 0 in both cases. Non-Darwin platforms exit 0 with an explanatory stderr message.
- Files changed: praxis/cli/install_weekly.py (added uninstall_weekly_macos + docstring update), praxis/cli/__main__.py (cmd_uninstall_weekly + 'uninstall-weekly' subparser), tests/test_install_weekly.py (added 6 tests, import uninstall_weekly_macos, header updated), scripts/ralph/feature/macos-launch-agent/prd.json (passes=true), scripts/ralph/progress.txt (this entry).
- Verification: `uv run pytest -q` -> 1030 passed (6 new + 1024 existing). `uv run pyright praxis/cli/install_weekly.py` -> 0 errors. `uv run pyright praxis/cli/__main__.py` -> 3 errors, identical to baseline before this commit (verified via `git stash` round-trip); they are the pre-existing WeeklyRunSummary issue documented in iteration 1.
- What the next iteration should know:
  - Patterns established:
    * `uninstall_weekly_macos(home=None)` returns `bool` indicating whether anything was actually removed. The CLI uses this to choose between "Removed..." vs "No weekly LaunchAgent found at..." messaging.
    * The "tolerate launchctl unload failure" rule -- launchctl returns non-zero when the job is not currently loaded, and we cannot distinguish that from a real error. uninstall always proceeds to delete the file. If a future story needs to surface real unload errors, it must add a separate code path that distinguishes them (e.g., parse stderr for the "Could not find specified service" text), not change this default.
  - Hooks for upcoming stories:
    * US-081 (--notify body / osascript): `_post_notify` in cli/__main__.py currently puts the trajectory label in the title only and uses a `~/.praxis/weeks/{iso}.html` body. AC US-081 wants the body to mention `~/.praxis/latest.html` -- check whether to change the body text or introduce a `latest.html` symlink/copy. Existing notify happy-path tests (if any) live in tests/ for the week command; grep for `_post_notify` before touching.
    * US-082 (trajectory in body + non-crashing osascript): `_post_notify` already wraps the osascript call in a broad try/except, but the AC says "if osascript fails for any reason, the run logs the failure and still exits 0" -- need a test that covers a FileNotFoundError (osascript binary missing) explicitly, since today's try/except is best-effort but unverified by tests.
    * US-083 (non-macOS install-weekly path): the placeholder "not yet implemented" stderr message in cmd_install_weekly is the seam to flesh out -- print the systemd / Task Scheduler XML to stdout AND save to ~/.praxis/install-weekly-snippet.txt. The matching non-macOS branch for cmd_uninstall_weekly is already a sensible no-op ("non-macOS has no scheduled job to remove."); US-083 may not need to touch it.
  - Gotchas:
    * `fake_launchctl` fixture is reusable but accumulates calls across `install_weekly_macos()` + `uninstall_weekly_macos()` in the same test, so slice the list from the install call count before asserting on uninstall's behavior (see test_uninstall_weekly_unloads_and_deletes_plist).
    * `cmd_uninstall_weekly` imports from `praxis.cli.install_weekly` inside the function (not at module top) to match the existing install pattern -- keeps non-macOS imports off the hot path.
---

## Iteration 3 - US-081: macOS notification via osascript
- What I did: Reworked `_post_notify` in praxis/cli/__main__.py so the AppleScript title is the fixed string "Praxis weekly read is ready" and the body is "Open ~/.praxis/latest.html to read." (per spec 13.2 / AC US-081). Dropped the `week_iso` parameter (no longer referenced) and the title-override-by-trajectory branch; the trajectory-label-in-body work is US-082's. Updated the caller in `cmd_week` to call `_post_notify()` with no args, removing the now-dead `traj_label` calculation.
- Files changed: praxis/cli/__main__.py (refactored `_post_notify`, simplified caller), tests/test_cli.py (added 4 US-081 tests + `fake_osascript` fixture), scripts/ralph/feature/macos-launch-agent/prd.json (passes=true), scripts/ralph/progress.txt (this entry).
- Verification: `uv run pytest -q` -> 1034 passed (4 new + 1030 existing). `uv run pyright praxis/cli/__main__.py tests/test_cli.py` -> 4 errors, all pre-existing (3 WeeklyRunSummary on __main__.py + 1 "pytest could not be resolved" on test_cli.py). Confirmed unchanged via `git stash` round-trip; my edits introduce 0 new pyright errors.
- What the next iteration should know:
  - Patterns established:
    * `fake_osascript` fixture in tests/test_cli.py monkeypatches `praxis.cli.__main__.subprocess.run` and records every call. Reuse it for US-082's "osascript fails -> run still exits 0" test (just have the stub raise FileNotFoundError or return non-zero).
    * `_post_notify()` now takes no arguments. US-082 must re-add `trajectory_label: str | None` (it was removed in this iteration since US-081 has no use for it) and the caller in `cmd_week` needs to recompute `summary.trajectory.label.value.title()` before passing it through. The pre-edit code is in the git log at 381704b for reference.
    * The body text MUST contain the literal substring `~/.praxis/latest.html` -- US-082's expanded body (with trajectory label) must keep that substring intact, otherwise `test_notify_body_mentions_latest_html` will fail.
    * The title MUST stay literally "Praxis weekly read is ready" -- `test_notify_title_is_fixed_string` asserts `'with title "Praxis weekly read is ready"'` verbatim. US-082 adds the trajectory label to BODY, not title.
  - Hooks for upcoming stories:
    * US-082 (trajectory label in body + osascript failure tolerance): Re-add `trajectory_label` parameter to `_post_notify`, then weave the label into the body (spec section 13.2 example: "Praxis: Drifting this week. Open ~/.praxis/latest.html for the detail."). Add an explicit test that monkeypatches subprocess.run to raise `FileNotFoundError("osascript missing")` and asserts the CLI still exits 0 -- today's broad try/except handles it but no test verifies the contract.
    * US-083 (non-macOS install-weekly): the `fake_osascript` fixture is irrelevant; that story is about `install-weekly` printing a systemd/Task Scheduler snippet to stdout and saving it to `~/.praxis/install-weekly-snippet.txt`. See progress entries for iter 1/2 for the placeholder seam in `cmd_install_weekly`.
  - Gotchas:
    * `latest.html` is a SYMLINK created by the HTML digest writer (praxis/reports/digest_html.py, US-064), not by `_post_notify`. Tests don't verify the symlink exists at notification time -- the contract is only that the body MENTIONS the path. If a future change to the notify path tries to validate the symlink before notifying, it would couple notification to the HTML writer's symlink semantics, which is a regression in separation of concerns.
    * The notification posts AFTER `_weekly_html_path(target_week).write_text(...)` in `cmd_week`. The "after writing the HTML" half of the AC is structural (it's the code order), so the test asserts an HTML file exists at notify time as a smoke check rather than ordering subprocess.run vs write_text directly.
    * Tests monkeypatch `sys.platform` via `monkeypatch.setattr(sys, "platform", "darwin")`. monkeypatch reverts at teardown, so the patch is fully scoped to one test even though `sys` is a process-wide module.
---

## Iteration 4 - US-082: Notification includes trajectory label and never crashes the run
- What I did: Re-added the `trajectory_label: str | None = None` parameter to `_post_notify` in praxis/cli/__main__.py and wove the label into the body using the format "<Label> this week. Open ~/.praxis/latest.html for the detail." (spec 13.2 example). The caller in `cmd_week` now maps `summary.trajectory.label.value` through a small `_TRAJECTORY_LABEL_DISPLAY` dict so raw enum strings like `stable_engaged` render as user-facing `Engaged`. Hardened the subprocess block to capture stdout/stderr and log non-zero exits explicitly, plus the broad `except Exception` already in place catches `FileNotFoundError` / `PermissionError` etc. so the run still exits 0.
- Files changed: praxis/cli/__main__.py (`_TRAJECTORY_LABEL_DISPLAY` dict, `_post_notify` takes `trajectory_label`, capture+log non-zero exit, caller wiring in `cmd_week`), tests/test_cli.py (5 new US-082 tests), scripts/ralph/feature/macos-launch-agent/prd.json (passes=true), scripts/ralph/progress.txt (this entry).
- Verification: `uv run pytest -q` -> 1039 passed (5 new + 1034 existing). `uv run pyright praxis/cli/__main__.py tests/test_cli.py` -> 4 errors, identical to baseline before this iteration (verified via `git stash` round-trip). My edits introduce 0 new pyright errors.
- What the next iteration should know:
  - Patterns established:
    * Enum -> display mapping for trajectory labels lives in `_TRAJECTORY_LABEL_DISPLAY` at module top of cli/__main__.py. Keys mirror `TrajectoryLabel` enum values ("learning", "stable_engaged", "stable_passive", "atrophying", "insufficient_data"). The fallback for unknown labels is `raw.title()`. If a new TrajectoryLabel variant lands, add it here so the notification body stays user-facing.
    * The display dict is identical in spirit to the terminal renderer's `_TRAJ_TITLE` (praxis/reports/terminal.py L98), but kept separate to avoid a cross-module import from the CLI hot path. If a third surface needs it, lift it into a shared helper module.
    * To stub `run_weekly` in CLI tests, monkeypatch `praxis.cli.__main__.run_weekly` (not the source module) -- the CLI captured a reference at import time. See `test_notify_body_uses_user_facing_trajectory_label`.
    * Constructing a minimal `WeeklyRunSummary` in tests requires only: week_iso, sessions=[], tasks=[], judge_results={}, moments=[], selection=None, snapshot (with session_count>=1), rendered_html, rendered_terminal, elapsed_seconds, trajectory. All other fields default. `ProfileSnapshot` can be built directly (not via `from_scores`) when you want a known session_count without seeding rows.
  - Hooks for upcoming stories:
    * US-083 (non-macOS install-weekly): the trajectory/notification path is done -- US-083 is fully on `cmd_install_weekly`'s non-Darwin branch. The placeholder stderr message is the seam: replace it with code that prints the systemd timer or Task Scheduler XML to stdout AND saves it to `~/.praxis/install-weekly-snippet.txt`. Exit 0. The matching non-macOS branch in `cmd_uninstall_weekly` is already a sensible no-op -- US-083 likely doesn't need to touch it.
    * The schedule config (day/hour/minute) for the systemd / Task Scheduler snippets lives in `~/.praxis/config.toml` under `[schedule]`. See praxis/cli/install_weekly.py for how the macOS path loads it; reuse the same config reader.
  - Gotchas:
    * The notification body's prefix is `"<Label> this week."`. For `INSUFFICIENT_DATA` this becomes `"Reading this week."` -- mildly awkward but matches the spec 13.2 example phrasing. If product wants different phrasing per-label, change the mapping; don't add special-case branches around the label string concatenation.
    * `subprocess.run(..., capture_output=True, text=True)` is now used so we can log stderr on non-zero exits. The previous US-081 implementation passed neither, which left the subprocess writing directly to the parent's stderr. The change is invisible to existing tests because `fake_osascript` returns a CompletedProcess with empty stdout/stderr.
    * Tests that stub `run_weekly` MUST set `tmp_home` AND `fake_api_key` fixtures, otherwise the exit-2 (no API key) gate fires before `run_weekly` is reached. See `test_notify_body_uses_user_facing_trajectory_label`.
---

## Iteration 5 - US-083: Non-macOS prints snippet and exits cleanly
- What I did: Added pure renderers `build_systemd_snippet` (Linux user-timer) and `build_task_scheduler_snippet` (Windows XML) plus a `write_non_macos_snippet(praxis_home=None, *, platform_override=None)` dispatcher in `praxis/cli/install_weekly.py`. The CLI's non-macOS branch in `cmd_install_weekly` now calls the dispatcher, prints the content to stdout, prints "Saved snippet to: <path>" to stdout, and exits 0. The snippet is persisted to `~/.praxis/install-weekly-snippet.txt` via the new `snippet_path()` helper. Windows is detected via `sys.platform.startswith("win") or == "cygwin"`; everything else (Linux, FreeBSD, etc.) gets systemd.
- Files changed: praxis/cli/install_weekly.py (renderers + dispatcher + day-name maps + `_format_praxis_exec`), praxis/cli/__main__.py (cmd_install_weekly non-Darwin branch rewritten), tests/test_install_weekly.py (14 new US-083 tests, ScheduleConfig import, expanded header docstring), scripts/ralph/feature/macos-launch-agent/prd.json (passes=true), scripts/ralph/progress.txt (this entry).
- Verification: `uv run pytest -q` -> 1053 passed (14 new + 1039 existing). `uv run pyright praxis/cli/install_weekly.py praxis/cli/__main__.py tests/test_install_weekly.py` -> 4 errors, identical to baseline before this iteration (3 WeeklyRunSummary on __main__.py + 1 pytest import on test_install_weekly.py). Confirmed unchanged via `git stash` round-trip; my edits introduce 0 new pyright errors.
- What the next iteration should know:
  - All 5 PRD stories now pass. Ralph should detect <promise>COMPLETE</promise> on the next iteration.
  - Patterns established (in case follow-on stories touch this surface):
    * `build_systemd_snippet(schedule, praxis_argv)` and `build_task_scheduler_snippet(schedule, praxis_argv)` are pure -- no I/O, no config load. Tests call them directly with `ScheduleConfig(...)` to pin output without touching the tmp_home config file.
    * `write_non_macos_snippet(praxis_home=None, *, platform_override=None)` is the I/O layer: it reads `~/.praxis/config.toml`, resolves the praxis argv, dispatches by platform, writes the snippet to disk, and returns `(path, content)`. Tests pass `platform_override="linux" | "win32" | "freebsd13"` to exercise dispatch without touching `sys.platform`.
    * Day-of-week dispatch uses two new dicts: `_SYSTEMD_DAY_BY_NAME` ("sunday" -> "Sun") and `_TASKSCHEDULER_DAY_BY_NAME` ("sunday" -> "Sunday"). Both normalize via lowercase strip; invalid day strings raise `InstallWeeklyError` (so the user gets a usable error instead of an invalid unit/XML file).
    * `snippet_path(home=None)` returns `<home or PRAXIS_HOME>/install-weekly-snippet.txt`, mirroring `plist_path` in shape so tests can pass an explicit home or rely on PRAXIS_HOME via tmp_home.
  - Gotchas:
    * `_resolve_praxis_command()` calls `shutil.which("praxis")`, which on Python 3.13 dives into `_winapi.NeedCurrentDirectoryForExePath` when `sys.platform == "win32"`. That attribute is None on macOS, so the test that monkeypatches `sys.platform = "win32"` must ALSO stub `praxis.cli.install_weekly.shutil.which` to avoid an AttributeError crash. See `test_cli_install_weekly_windows_saves_task_scheduler_xml`. Tests that use the `platform_override=` parameter (not sys.platform monkeypatching) don't need this stub because `shutil.which` still sees the host's real platform.
    * The CLI prints the snippet body AND the "Saved snippet to:" line to stdout (not stderr). This matches the existing macOS branch ("Installed weekly LaunchAgent: ...") which also goes to stdout. If a user pipes stdout to a file, both lines will land there; that's deliberate so the saved unit text is self-contained.
    * Task Scheduler XML uses a fixed `StartBoundary` of `2020-01-01T<hh>:<mm>:00` so the trigger is immediately valid. If a future story needs date-aware output (e.g., to start "next Sunday"), inject a clock; don't randomize the fixed string.
---

## Iteration 6 - US-038: Behavioral patterns panel + adapter wiring
- What I did: Introduced `praxis/reports/panel_inputs.py` carrying `PanelInputs`, `BehavioralPatternsPanel`, and `BehavioralPatternRow` dataclasses for the v0.3 expansion panels (US-038..042). Added `detect_signal_kinds(turn)` + `SIGNAL_KINDS_IN_PANEL_ORDER` to `praxis/behavior/signals.py`. Extended adapter with `_behavioral_patterns_panel(summary)` + `build_panel_inputs(summary)`, wired the result into both digest builders. Both digest_html and digest_terminal got a `panel_inputs: PanelInputs | None` field on their `WeeklyDigest` dataclass and a new section renderer that surfaces label/count/up-to-2-excerpts/citation per row, or the verbatim "No behavioral patterns captured this week." copy when has_signals is False (or panel_inputs is None).
- Files changed: praxis/behavior/signals.py (added detect_signal_kinds + SIGNAL_KINDS_IN_PANEL_ORDER + Turn import), praxis/reports/panel_inputs.py (new module), praxis/reports/adapter.py (panel-inputs builder + integration into build_html_digest/build_terminal_digest), praxis/reports/digest_html.py (panel_inputs field + _behavioral_patterns_section + CSS), praxis/reports/digest_terminal.py (panel_inputs field + _behavioral_patterns + render wiring + _BEHAVIORAL_PATTERNS_EMPTY constant), tests/test_adapter_panel_inputs.py (new, 18 tests), tests/test_behavior.py (+6 detect_signal_kinds tests), tests/test_digest_html.py (+9 panel tests), tests/test_digest_terminal.py (+9 panel tests; also widened the six-dim panel slice in test_six_dim_panel_renders_six_body_lines to ignore the new section below it), scripts/ralph/feature/report-panels-expansion/prd.json (US-038 passes=true).
- Verification: `uv run mypy praxis` -> Success: no issues found in 50 source files. `uv run pytest` -> 1112 passed in 0.92s (59 new + 1053 existing).
- What the next iteration should know:
  - Patterns established (US-039..042 should mirror these):
    * v0.3 panel inputs live in `praxis/reports/panel_inputs.py`. Add new fields to `PanelInputs` (don't proliferate sibling modules). All panel dataclasses should be frozen.
    * Renderer fields on `WeeklyDigest` (both digest_html + digest_terminal) follow `panel_inputs: PanelInputs | None = None` shape. The adapter populates the single field; the renderer extracts what it needs at the top of render().
    * Empty-state copy lives as a module-level constant in each renderer (e.g. `_BEHAVIORAL_PATTERNS_EMPTY`) so tests can import-assert on it and a future copy change is one audit point per renderer.
    * Citation strings live in dicts keyed by `signal_kind` in `panel_inputs.py` (see `SIGNAL_CITATIONS`). When US-039..042's new signals land, add their kinds + citations alongside the existing keys rather than building per-panel citation maps.
    * The 80-column hard cap (terminal) still binds; the new section's tests include the visible_width assertion. Any new terminal section must do the same.
    * HTML CSS for the new panel uses the same OKLCH terracotta/cream palette and serif stack; the `.bp-*` class block follows the editorial-list (not card-grid) approach. Keep US-061's self-containment contract: no <link>, no <script>, no http(s)://, no @import, no url().
  - Hooks for upcoming stories:
    * US-039 (augmentation/automation balance + cadence): two new fields on `PanelInputs` (e.g. `aug_auto_balance`, `cadence`). When aug_auto is unavailable (no API key), wire `None` rather than zero counts so the renderer can show "Classifier unavailable for this week." instead of misleading 0/0/0. Cadence-detector lives at `praxis/behavior/cadence.py` (not yet implemented; check the manifest at `scripts/ralph/manifest.coaching.json` for the API contract).
    * US-040 (repeat-task radar + verification calibration): `praxis/behavior/repeat_task.py` already exposes `detect_repeats(clusters, window_days)`. Adapter should call it from summary.tasks (or from a clustering step upstream) and surface `RepeatTask` rows.
    * US-041 / US-042 follow the same pattern.
  - Gotchas:
    * The existing test `test_six_dim_panel_renders_six_body_lines` in tests/test_digest_terminal.py used to slice from "THE SIX DIMENSIONS" to EOF. With US-038's new section appended below, the slice must end at the next eyebrow's line start. Done here; future panels added BETWEEN the six-dim panel and the BEHAVIORAL PATTERNS section would not need an update, but future panels added AFTER would (or the test would need to switch to slicing between specific eyebrows generically).
    * `_BEHAVIORAL_PATTERNS_EMPTY` copy is asserted verbatim in tests; if a copy change ships, update both the constant and the test assertion.
    * The TERMINAL renderer's empty-state message must be ASCII-safe; the HTML renderer's must go through `_safe()` (redact + escape). Both paths are covered by tests in this iteration.
    * `Turn` import was added to praxis/behavior/signals.py for the `detect_signal_kinds(turn: Turn) -> set[str]` signature. Existing code already imports `Session` from the same module; the addition is non-breaking but worth noting if a future refactor moves Turn.
---

## Iteration 7 - US-039: Augmentation/automation balance + cadence panels
- What I did: Added AugAutoBalancePanel + CadencePanel dataclasses to praxis/reports/panel_inputs.py (plus the AUG_AUTO_ANCHOR_CITATION, AUG_AUTO_INDUSTRY_AUG_SHARE/AUTO_SHARE, CADENCE_ANCHOR_CITATION, CADENCE_WINDOW_DAYS, CADENCE_POSITION_LABELS constants). Wired adapter helpers (_aug_auto_balance_panel, _cadence_panel, _classify_high_adopter) plus build_panel_inputs population. Added empty-state copy + section renderers to both digest_terminal.py (_aug_auto_balance, _cadence) and digest_html.py (_aug_auto_balance_section, _cadence_section + CSS for .bal-* / .cad-* classes). The unavailable + no-activity strings ("Classifier unavailable for this week." / "No substantive sessions in the last 21 days.") are asserted verbatim by tests.
- Files changed: praxis/reports/panel_inputs.py (panel dataclasses + constants), praxis/reports/adapter.py (aug_auto/cadence builders + build_panel_inputs wiring + _classify_high_adopter helper), praxis/reports/digest_terminal.py (empty-state constants + 2 renderer functions + render wiring), praxis/reports/digest_html.py (empty-state constants + 2 renderer functions + render wiring + 2 CSS blocks .bal-*/.cad-*), tests/test_adapter_panel_inputs.py (+17 tests for US-039 adapter), tests/test_digest_terminal.py (+13 tests for US-039 terminal renderer), tests/test_digest_html.py (+13 tests for US-039 HTML renderer), scripts/ralph/feature/report-panels-expansion/prd.json (US-039 passes=true), scripts/ralph/progress.txt (this entry).
- Verification: `uv run mypy praxis` -> Success: no issues found in 50 source files. `uv run pytest` -> 1155 passed in 1.10s (43 new + 1112 existing).
- What the next iteration should know:
  - Patterns established (US-040..042 should mirror these):
    * Panel-input dataclasses live in panel_inputs.py; ALL panels have an empty-state property (e.g. AugAutoBalancePanel.classifier_unavailable, CadencePanel.has_activity) that the renderer reads to decide between empty-state + populated paths.
    * Citation strings stay module-level constants (AUG_AUTO_ANCHOR_CITATION, CADENCE_ANCHOR_CITATION) so the adapter, renderers, and tests share one source of truth.
    * The adapter detects aug_auto data via `getattr(session, 'aug_auto_classification', None)`. This shape lets the augmentation-automation-classifier dependency story land independently: when the orchestrator integration attaches the attribute, the panel populates automatically. Until then, every session is unclassified -> classifier_unavailable=True -> empty-state copy fires.
    * `_classify_high_adopter(streak, window_days)` is a placeholder helper in adapter.py with documented 1/3 + 2/3 thresholds. When cadence-detector ships its full implementation, replace this placeholder with the canonical call; the renderer reads through CadencePanel.high_adopter_position (a 'low'|'moderate'|'high'|None Literal-ish string) so the seam is one line in adapter.py.
    * The fake summary pattern for new tests: use `SimpleNamespace(sessions=[SimpleNamespace(aug_auto_classification='augmentation', started_at=..., user_turns=[...])])`. The existing `_FakeSummary` dataclass at the top of test_adapter_panel_inputs.py has `sessions: list[Session]` which fights pyright's list invariance when SimpleNamespace stubs are passed; use SimpleNamespace directly for new tests.
    * Each renderer surfaces its empty-state via module-level constants (_AUG_AUTO_BALANCE_CLASSIFIER_UNAVAILABLE, _AUG_AUTO_BALANCE_NO_SESSIONS, _CADENCE_NO_ACTIVITY in both digest_terminal.py and digest_html.py). Tests import these constants and assert verbatim, so a copy change is one-line per renderer.
    * Section order in both renderers: ... -> THE SIX DIMENSIONS -> BEHAVIORAL PATTERNS -> AUGMENTATION/AUTOMATION -> CADENCE -> (US-040..042 panels). Adding the next panel slots into the same data_block / parts list in render().
  - Hooks for upcoming stories:
    * US-040 (repeat-task radar + verification calibration): praxis/behavior/repeat_task.py already exposes detect_repeats. Adapter should add `_repeat_task_radar_panel(summary)` that reads `summary.tasks` (or clusters them upstream) and surfaces RepeatTask rows. Use the same empty-state pattern: when detect_repeats returns [], the panel renders "No repeat tasks detected this week." Per AC the radar shows occurrences + estimated_minutes_per_occurrence + a "Could become a skill" tag.
    * US-041 (specification / context / knowledge-gap): three new panels mirror US-039's shape. The specification-adoption signal lives in praxis/behavior/signals.py (per the manifest); for knowledge-gap distribution, every category gets an explicit zero count (no silent drops) and the empty-state fires only when EVERY category is zero.
    * US-042 (tool/agent ladder + refined cost): the ladder rung lookup (prompt-only / tools-on / skills / hooks / subagents) needs a new signal in signals.py; the refined cost-effectiveness panel reads from cost_ledger + per-session model_hint and applies a deterministic counterfactual rule. Document the rule in praxis/models_advisor/advisor.py per AC.
  - Gotchas:
    * aug_auto data isn't yet plumbed through WeeklyRunSummary - the augmentation-automation-classifier story will add it. Until then, every session lacks the attribute and the panel renders "Classifier unavailable for this week." across the board. That matches the AC ("when aug_auto is unavailable for the week (all rows NULL because no API key)") so this is correct, not a regression.
    * cadence reads `started_at.weekday()` from each session. The `_classify_high_adopter` thresholds assume a 21-day window but the in-memory summary's sessions typically cover only 7 days, so the streak is bounded by the calendar week unless additional history is loaded. The cadence-detector story will replace this with a proper 21-day window from storage; the panel will populate uniformly when that lands.
    * The HTML test that asserts "0% not in out" had to be scoped to within the aug-auto-balance section because the global CSS uses % for OKLCH lightness ("oklch(80% 0.006 55)"). Use the same scoping trick for any future HTML test that wants to assert against numeric tokens.
    * The OKLCH-percentage gotcha also means the test_us039_panels_self_containment_holds test cannot just check for "%" globally; assert on specific markers (e.g. "Anthropic Economic Index") instead.
---

## Iteration 8 - US-040: Repeat-task radar + verification calibration panels
- What I did: Brought praxis/behavior/repeat_task.py + tests/test_repeat_task.py in from the sibling ralph/factory/repeat-task-detector branch (this feature depends on it per the coaching manifest) and extended praxis/behavior/__init__.py to re-export its symbols. Added RepeatTaskRow / RepeatTaskRadarPanel / VerificationCalibrationPanel dataclasses + constants (REPEAT_TASK_CITATION, REPEAT_TASK_SKILL_TAG, REPEAT_TASK_WINDOW_DAYS, VERIFICATION_CALIBRATION_CITATION, VERIFICATION_CALIBRATION_KINDS_IN_PANEL_ORDER, VERIFICATION_CALIBRATION_LABELS) in panel_inputs.py. Added detect_verification_calibration_kinds + categorize_session_verification to signals.py with three regex patterns (source_check / test_run / spot_check) plus blanket_accept as the implicit default. Wired _repeat_task_radar_panel + _verification_calibration_panel adapter helpers (the radar builds Cluster objects from summary.tasks and calls detect_repeats; the calibration panel buckets each session by its highest-rigor verification activity). Added section renderers in both digest_terminal.py (_repeat_task_radar + _verification_calibration with new _REPEAT_TASK_EMPTY + _VERIFICATION_CALIBRATION_NO_SESSIONS constants) and digest_html.py (_repeat_task_radar_section + _verification_calibration_section + CSS for .rt-* / .vc-* classes).
- Files changed: praxis/behavior/repeat_task.py (new, copied verbatim from sibling branch), praxis/behavior/__init__.py (re-export repeat_task symbols), praxis/behavior/signals.py (verification calibration detectors), praxis/reports/panel_inputs.py (US-040 dataclasses + constants), praxis/reports/adapter.py (US-040 panel builders + duration helpers + build_panel_inputs wiring), praxis/reports/digest_terminal.py (US-040 sections + constants + render wiring + _format_minutes), praxis/reports/digest_html.py (US-040 sections + CSS + render wiring + _format_minutes_html), tests/test_repeat_task.py (new, copied from sibling branch), tests/test_behavior.py (+8 verification calibration tests), tests/test_adapter_panel_inputs.py (+18 US-040 adapter tests), tests/test_digest_terminal.py (+12 US-040 terminal renderer tests), tests/test_digest_html.py (+13 US-040 HTML renderer tests), scripts/ralph/feature/report-panels-expansion/prd.json (US-040 passes=true).
- Verification: `uv run mypy praxis` -> Success: no issues found in 51 source files. `uv run pytest` -> 1226 passed in 1.01s (71 new + 1155 existing).
- What the next iteration should know:
  - Patterns established (US-041/US-042 should mirror these):
    * For panels whose adapter depends on a sibling-component module (e.g. detect_repeats from repeat_task.py), the cleanest path is to bring the source + tests over from the sibling branch directly. The coaching manifest's component dependency graph means the file WILL be present at merge time; copying ahead in this branch lets the panel adapter be exercised by unit tests without relying on cross-branch imports.
    * RepeatTaskRow's `skill_tag` field defaults to REPEAT_TASK_SKILL_TAG ("Could become a skill") so a future per-row tag override is one point of change. Tests assert on the tag presence; if you change the literal, update both the constant and the test assertions.
    * VerificationCalibrationPanel has a `count_for(kind)` lookup that returns 0 for unknown keys. Renderers iterate VERIFICATION_CALIBRATION_KINDS_IN_PANEL_ORDER and call count_for(); a new bucket should append to that tuple AND to VERIFICATION_CALIBRATION_LABELS to land in both renderers without ad-hoc branches.
    * The session-level "highest-rigor verification" categorization is in signals.py:categorize_session_verification(). The rigor order is source_check > test_run > spot_check > blanket_accept; if a new verification kind lands (e.g. fuzz_run), insert it in detect_verification_calibration_kinds AND update the categorize_session_verification fallthrough so the rigor ladder stays explicit.
    * _format_minutes (terminal) and _format_minutes_html (html) round to int when the fractional part is below 0.05 minutes (3 seconds). The duplication is deliberate: both renderers must match exactly but neither should import the other.
  - Hooks for upcoming stories:
    * US-041 (specification / context / knowledge-gap panels): three new PanelInputs fields. The spec-adoption signal looks for spec blocks at session opening; check the manifest for whether behavioral-signals-expansion has already shipped a spec_block detector. The knowledge-gap classifier wants four arXiv-2501.11709 categories with explicit zeros (no silent drops).
    * US-042 (tool/agent ladder + refined cost): the ladder rung lookup wants a new signal in signals.py (prompt-only / tools-on / skills / hooks / subagents) and the refined cost-effectiveness panel needs a deterministic counterfactual rule documented in praxis/models_advisor/advisor.py.
  - Gotchas:
    * praxis/behavior/repeat_task.py is now in this branch even though the manifest says the repeat-task-detector component owns it. If the manifest's component-level merge replays the same file, the merge should be a no-op (identical content). If a future iteration of repeat-task-detector lands additional commits that diverge from the version cherry-picked here, the merge will need to be a manual reconciliation.
    * Session duration is estimated in adapter.py:_session_duration_minutes via either (a) the timestamp delta between first and last user turns when both carry timestamps, or (b) a fallback of len(user_turns) * 2.0 minutes. The 2.0 factor is a renderer-side heuristic; it lives in _MINUTES_PER_USER_TURN_FALLBACK and changing it shifts every repeat-task radar's "min each" value by the same proportional amount. The detector treats whatever the caller hands it as wall-clock truth, so calibration belongs here, not in repeat_task.py.
    * Verification calibration patterns (in signals.py) are conservative-by-design - precision over recall - to avoid misclassifying a delegating session as rigorous. If a future story finds the panel is under-counting source-check or test-run, broaden the regex pattern in _SOURCE_CHECK_MARKERS / _TEST_RUN_MARKERS rather than relaxing categorize_session_verification's hierarchy.
    * The terminal renderer's _REPEAT_TASK_EMPTY constant is asserted verbatim by tests; the HTML renderer has its own _REPEAT_TASK_EMPTY constant. Both must change in lockstep if the empty-state copy changes (the literal "No repeat tasks detected this week." is the AC's verbatim wording).
---

## Iteration 9 - US-041: Specification, context engineering, knowledge-gap panels
- What I did: Added three US-041 panels and their pre-requisite signal detectors. New detectors in praxis/behavior/signals.py: detect_spec_block (Markdown `## Goal` / inline `Goal:` form, evaluated on the session's first user turn only - mid-session spec writing does not count as adoption), detect_scaffolding_kinds (CLAUDE.md / AGENTS.md / copilot-instructions / Projects/Custom GPT / Skills patterns, returns a set of kinds present anywhere in the session), detect_knowledge_gap_kinds + count_session_knowledge_gaps (four arXiv 2501.11709 buckets: missing_context, missing_specs, multiple_context, unclear_instructions). New panel dataclasses in panel_inputs.py: SpecificationAdoptionPanel, ContextEngineeringDepthPanel (+ ContextEngineeringRow), KnowledgeGapDistributionPanel (+ KnowledgeGapRow); citations live as module-level constants (SPECIFICATION_ADOPTION_CITATION, CONTEXT_ENGINEERING_CITATION, KNOWLEDGE_GAP_CITATION) so the adapter + both renderers + tests share one source of truth. Adapter builders (_specification_adoption_panel, _context_engineering_panel, _knowledge_gap_distribution_panel) wired into build_panel_inputs. Both digest_terminal.py and digest_html.py gained the three section renderers + empty-state constants (_SPECIFICATION_ADOPTION_NO_SESSIONS, _CONTEXT_ENGINEERING_NO_ARTIFACTS, _KNOWLEDGE_GAP_EMPTY) + CSS for .sa-*/.ce-*/.kg-* classes; the three new sections render in order after verification calibration so the document reads habit -> verify -> craft.
- Files changed: praxis/behavior/signals.py (US-041 detectors + constants), praxis/reports/panel_inputs.py (3 panel dataclasses + 2 row dataclasses + 3 citations + 1 label dict + __all__ entries), praxis/reports/adapter.py (3 panel builders + build_panel_inputs wiring + imports), praxis/reports/digest_terminal.py (3 section renderers + empty-state constants + render wiring + imports), praxis/reports/digest_html.py (3 section renderers + empty-state constants + CSS for .sa-*/.ce-*/.kg-* + render wiring + imports), tests/test_behavior.py (+18 detector tests), tests/test_adapter_panel_inputs.py (+13 adapter tests), tests/test_digest_terminal.py (+13 terminal renderer tests), tests/test_digest_html.py (+16 HTML renderer tests), scripts/ralph/feature/report-panels-expansion/prd.json (US-041 passes=true), scripts/ralph/progress.txt (this entry).
- Verification: `uv run mypy praxis` -> Success: no issues found in 51 source files. `uv run pytest` -> 1290 passed in 0.98s (64 new + 1226 existing).
- What the next iteration should know:
  - All 5 PRD stories are now passing. Ralph should detect the COMPLETE state on the next iteration.
  - Patterns established (US-042 if/when it lands should mirror these):
    * The "every category gets an explicit zero" pattern (US-041 AC for knowledge gaps) is implemented by always emitting all four KnowledgeGapRow rows from the adapter; the renderer drops the panel to the empty-state copy only when has_gaps is False (every count is zero). For US-042's tool/agent ladder, the same shape will work: every rung emits a count, the empty-state fires only when every rung is zero.
    * Adapter helpers use `getattr(summary, "sessions", None) or []` so a SimpleNamespace fake with no sessions field cannot crash the panel build - matches the US-038..040 pattern.
    * detect_spec_block looks ONLY at the first user turn (session opening). A spec block written later does not count - the panel measures adoption at session openings specifically per the Section 11 framing.
    * Knowledge-gap detector is precision-first: a turn that references "this function" AND contains a code fence (```) or 4-space indented block does NOT fire missing_context. This is intentional - the user has supplied the code.
    * The _BUILD_IMPERATIVE regex only matches at start-of-string ("^"), so "write me X" fires but "also write X" does not. Tests for missing_specs accumulation must use start-of-string imperatives (see test_count_session_knowledge_gaps_accumulates_per_turn for the corrected fixture).
    * CSS in digest_html.py is added in section order: .sa-section (specification adoption) -> .ce-section (context engineering) -> .kg-section (knowledge gaps), sitting between .vc-section and .f-section blocks. Future panels should be placed in the same block region to keep CSS grouped editorially.
  - Hooks for upcoming stories:
    * US-042 (tool/agent ladder + refined cost-effectiveness): the ladder rungs are prompt-only / tools-on / skills / hooks / subagents per spec. Adapter should add a `_tool_agent_ladder_panel(summary)` that uses the same scaffolding-detector signals (skills, hooks, subagents) plus a new signal for "tools-on" (assistant turns with tool_calls). The cost-effectiveness refined panel needs a deterministic counterfactual rule in praxis/models_advisor/advisor.py (model docs in v0.1 already exists at praxis/scoring/cost_ledger.py for ref).
  - Gotchas:
    * `_SCAFFOLDING_PATTERNS["projects"]` is case-sensitive on purpose (capital-P "Projects" + "Custom GPT") so a generic mention of "this project" does not falsely fire. If the ChatGPT spelling drifts, broaden the regex but keep the capital-letter anchor.
    * The "also write X" / "and also fix Y" pattern fires multiple_context, not missing_specs - that's the intentional decomposition: a single turn with two unrelated tasks is categorized by its STRUCTURE (multiple_context), not by what's missing. A future detector iteration could add a per-clause missing_specs check, but that risks false positives.
    * The HTML CSS uses OKLCH percentages, so a test that asserts a literal percentage like "60%" must scope to the section body (find('id="specification-adoption"')..find("</section>")) to avoid matching `oklch(80% ...)` tokens in the global theme.
    * `tmp_home` is a pre-existing pytest fixture (unused-arg pyright warnings are pre-existing); do not "fix" by removing the parameter - it's bound to a fixture by pytest's collection.
---


## Iteration 10 - US-042: Tool/agent ladder + refined cost-effectiveness panel
- What I did: Added LADDER_KINDS_IN_PANEL_ORDER + LADDER_LABELS + detect_session_ladder_rungs + categorize_session_ladder_rung in praxis/behavior/signals.py (rung order lowest-to-highest: prompt_only < tools_on < skills < hooks < subagents; tools_on fires from assistant turns with non-empty tool_calls; the other three fire from user-turn content regexes that are deliberately distinct from _SCAFFOLDING_PATTERNS["skills"] so each rung is separate). Added compute_counterfactual_overspend + CounterfactualOverspend in praxis/models_advisor/advisor.py with the deterministic rule documented inline (frontier-tier card with fast-tier sibling AND <=3 user turns AND <=200 avg prompt chars qualifies; overspend = frontier_cost - fast_cost summed per (higher, lower) pair; tiebreak by ascending card id). Added ToolAgentLadderPanel + LadderRungRow + RefinedCostEffectivenessPanel dataclasses + TOOL_AGENT_LADDER_CITATION + COST_EFFECTIVENESS_CITATION in panel_inputs.py. Wired _tool_agent_ladder_panel + _refined_cost_effectiveness_panel adapter helpers into build_panel_inputs. Added section renderers + empty-state constants to both digest_terminal.py (_tool_agent_ladder + _refined_cost_effectiveness with _TOOL_AGENT_LADDER_NO_ACTIVITY + _COST_EFFECTIVENESS_NO_COST_DATA) and digest_html.py (_tool_agent_ladder_section + _refined_cost_effectiveness_section + CSS for .tal-* / .rce-*). The cost-effectiveness panel has three states: no cost data -> verbatim "No cost data this week." copy (per US-042 AC, NOT "$0 overspend" because that would falsely imply optimality); cost data but no overspend -> positive-signal sentence ("No tier-mismatch overspend detected this week."); cost data and overspend>0 -> the canonical AC sentence "You spent $X on <higher-tier> for tasks <lower-tier> could have done = $Y overspend".
- Files changed: praxis/behavior/signals.py (ladder constants + detectors), praxis/models_advisor/advisor.py (CounterfactualOverspend + compute_counterfactual_overspend + thresholds), praxis/reports/panel_inputs.py (US-042 dataclasses + constants + __all__ entries + PanelInputs fields), praxis/reports/adapter.py (US-042 panel builders + build_panel_inputs wiring + imports), praxis/reports/digest_terminal.py (US-042 sections + empty-state constants + render wiring + imports), praxis/reports/digest_html.py (US-042 sections + CSS + empty-state constants + render wiring + imports), tests/test_behavior.py (+10 US-042 detector tests), tests/test_model_cards.py (+10 counterfactual-rule tests), tests/test_adapter_panel_inputs.py (+13 US-042 adapter tests), tests/test_digest_terminal.py (+12 US-042 terminal renderer tests), tests/test_digest_html.py (+10 US-042 HTML renderer tests), scripts/ralph/feature/report-panels-expansion/prd.json (US-042 passes=true), scripts/ralph/progress.txt (this entry).
- Verification: `uv run mypy praxis` -> Success: no issues found in 51 source files. `uv run pytest` -> 1345 passed in 1.00s (55 new + 1290 existing).
- What the next iteration should know:
  - All 5 PRD stories are now passing. Ralph should detect the COMPLETE state on the next iteration.
  - Patterns established (if a follow-on story ever extends the panels):
    * The counterfactual-overspend rule in advisor.py uses module-level threshold constants (COUNTERFACTUAL_MAX_USER_TURNS=3, COUNTERFACTUAL_MAX_AVG_PROMPT_CHARS=200.0, COUNTERFACTUAL_CHARS_PER_TOKEN=4.0, COUNTERFACTUAL_OUTPUT_TO_INPUT_RATIO=1.5) that intentionally mirror praxis/scoring/cost_ledger.py's TIER_FIT_MAX_USER_TURNS / TIER_FIT_MAX_AVG_PROMPT_CHARS / COST_CHARS_PER_TOKEN / COST_OUTPUT_TO_INPUT_RATIO so both ledger surfaces stay in lockstep. If the spec ever drifts one, drift the other in the same commit.
    * The (higher, lower) pair tiebreak is ascending card id (deterministic_under_reordering test locks it in). If a future card carries a new id that should beat an older one on the same overspend, the rule needs explicit per-family precedence rather than relying on id-sort luck.
    * The HTML renderer's $-aware copy uses {value:.2f} formatting so 4-decimal-rounded internal numbers display cleanly as cents. Tests with very-short prompts (<=12 chars) can trip the rounding to collapse spent and overspend to the same value -- prefer 200-char fixtures for "has_overspend is True with distinguishable values" assertions.
  - Gotchas:
    * The "had_any_priced_session" gate fires the moment any input session resolves to a card AND has cost > 0. A zero-input-char session on a priced card does NOT flip the gate (cost is 0), which matches the renderer's empty-state contract: "no priced activity at all" vs "priced activity but no overspend qualified".
    * _LADDER_SKILLS_MARKERS / _LADDER_HOOKS_MARKERS / _LADDER_SUBAGENTS_MARKERS are independent of _SCAFFOLDING_PATTERNS["skills"] (US-041 context-engineering panel) because the ladder panel needs each rung separate while the scaffolding panel collapses them. Keep both alive; don't try to share the regex.
    * Section order in both renderers now ends with: ...verification calibration -> specification adoption -> context engineering -> knowledge gaps -> TOOL/AGENT LADDER -> COST-EFFECTIVENESS. The HTML data block likewise appends the two new sections AFTER knowledge-gap distribution and BEFORE weekly trajectory.
    * `tmp_home` is a pre-existing pytest fixture; unused-arg pyright warnings are pre-existing and intentional - pytest binds the parameter by name. Do not strip them.
---

## Iteration 11 - Post-completion fixes for harness verification (linter + diff_scope)
- What I did: Fixed two harness verification failures left over from iteration 10's attempt. (1) Linter: removed `f` prefix from 4 placeholder-free f-strings in tests/make_synthetic_data.py (2 strings) and tests/test_digest_html.py (1 string), and removed the pre-existing unused `Role` import in tests/test_scanners.py. (2) diff_scope: added `praxis/models_advisor/` to the PRD's allowedPaths so US-042's required modification to praxis/models_advisor/advisor.py is in-scope per the AC text ("rule must be documented in praxis/models_advisor/advisor.py"). The allowedPaths list was missing this directory even though the AC explicitly named the file path.
- Files changed: tests/make_synthetic_data.py (2 print-line `f` prefixes removed), tests/test_digest_html.py (1 f-string in TaskRow fixture), tests/test_scanners.py (removed unused `from praxis.models import Role`), scripts/ralph/feature/report-panels-expansion/prd.json (added "praxis/models_advisor/" to allowedPaths).
- Verification: `uv run ruff check .` -> clean (no output). `uv run mypy praxis` -> Success: no issues found in 51 source files. `uv run pytest -q` -> 1345 passed (unchanged from iteration 10; no behavior changed).
- What the next iteration should know:
  - All 5 PRD stories are passing AND both harness checks (linter + diff_scope) now pass. Ralph should detect COMPLETE on the next iteration.
  - The pre-existing pyright "Import 'pytest' could not be resolved" warning on test files persists; it is environmental (venv resolution) and was present before this branch.
  - The pre-existing pyright "unused parameter" warnings on tmp_home / synthetic_*_session fixtures persist intentionally; pytest binds them by name (see iteration 10 gotcha).
---
