# progress.txt: union of factory component logs

## Iteration 1 - US-079: install-weekly creates and loads the plist
- What I did: Added `praxis install-weekly` subcommand backed by `praxis/cli/install_weekly.py`. Generates ~/Library/LaunchAgents/co.praxis.weekly.plist (Label=co.praxis.weekly) using `schedule.day/hour/minute` from ~/.praxis/config.toml and loads it with `launchctl load -w`. Idempotent re-runs unload first. CLI exits 4 with stderr message on launchctl failure (InstallWeeklyError surfacing returncode + stderr).
- Files changed: praxis/cli/install_weekly.py (new), praxis/cli/__main__.py (cmd_install_weekly + subparser), tests/test_install_weekly.py (new, 12 tests), scripts/ralph/feature/macos-launch-agent/prd.json (passes=true), scripts/ralph/progress.txt (this entry).
- Verification: `uv run pytest -q` -> 1024 passed (12 new + 1012 existing). `uv run pyright praxis/cli/install_weekly.py` -> 0 errors. The 3 pre-existing pyright errors on cli/__main__.py (WeeklyRunSummary vs RunSummary) are unchanged before/after this commit; confirmed by git stash + pyright comparison.
- What the next iteration should know:
  - Patterns established:
    * Subprocess is monkeypatched via `praxis.cli.install_weekly.subprocess.run`; tests do not call real launchctl. Reuse this stub fixture (`fake_launchctl`) when implementing US-080 uninstall-weekly.
    * The plist path resolver `plist_path(home=...)` accepts a home override but defaults to `Path.home()`, which the tmp_home conftest fixture patches; that's how tests land the plist inside tmp_path.
    * `PLIST_LABEL = "co.praxis.weekly"` is exported from install_weekly.py; uninstall-weekly should import and reuse it (don't duplicate the string).
    * `_WEEKDAY_BY_NAME` maps 'sunday'..'saturday' to 0..6 per Apple's launchd Weekday spec.
  - Hooks for upcoming stories:
    * US-080 (uninstall-weekly): needs to `launchctl unload <plist>` then delete the file; if file missing or unload fails, must still exit 0 per AC. Reuse `plist_path()` and `PLIST_LABEL`.
    * US-081 / US-082 (--notify path inside `praxis week`): the existing `_post_notify` in cli/__main__.py already shells out to osascript and includes the trajectory label in the title. ACs may want osascript moved to a dedicated module so test stubbing is easier, and the BODY (not just title) should mention `~/.praxis/latest.html`. Today the body only references `~/.praxis/weeks/{iso}.html` -- check whether US-081's AC ("opening ~/.praxis/latest.html") wants the body changed.
    * US-083 (non-macOS path): cmd_install_weekly currently prints a placeholder stderr message on non-Darwin and returns 0. That placeholder is the seam to flesh out: print the systemd timer / Task Scheduler XML to stdout AND save to ~/.praxis/install-weekly-snippet.txt.
  - Gotchas:
    * The pre-existing pyright errors in cli/__main__.py (WeeklyRunSummary type mismatch at lines 145/150/332) are noise from an orchestrator/reports type-coupling issue unrelated to this story. Don't try to "fix" them as part of macos-launch-agent stories unless explicitly assigned.
    * `_resolve_praxis_command()` falls back to `[sys.executable, '-m', 'praxis.cli']` when `shutil.which('praxis')` returns None; this matters when running install-weekly outside an activated venv. Tests don't exercise the fallback path, but they should not need to.
---

## Iteration 2 - US-080: uninstall-weekly removes the plist
- What I did: Added `praxis uninstall-weekly` subcommand backed by `uninstall_weekly_macos()` in praxis/cli/install_weekly.py. It runs `launchctl unload` on the existing plist (best-effort) then deletes the file, returning True. When the plist does not exist, it returns False without shelling out, and the CLI exits 0 in both cases. Non-Darwin platforms exit 0 with an explanatory stderr message.
- Files changed: praxis/cli/install_weekly.py (added uninstall_weekly_macos + docstring update), praxis/cli/__main__.py (cmd_uninstall_weekly + 'uninstall-weekly' subparser), tests/test_install_weekly.py (added 6 tests, import uninstall_weekly_macos, header updated), scripts/ralph/feature/macos-launch-agent/prd.json (passes=true), scripts/ralph/progress.txt (this entry).
- Verification: `uv run pytest -q` -> 1030 passed (6 new + 1024 existing). `uv run pyright praxis/cli/install_weekly.py` -> 0 errors. `uv run pyright praxis/cli/__main__.py` -> 3 errors, identical to baseline before this commit (verified via `git stash` round-trip); they are the pre-existing WeeklyRunSummary issue documented in iteration 1.
- What the next iteration should know:
  - Patterns established:
    * `uninstall_weekly_macos(home=None)` returns `bool` indicating whether anything was actually removed. The CLI uses this to choose between "Removed..." vs "No weekly LaunchAgent found at..." messaging.
    * The "tolerate launchctl unload failure" rule -- launchctl returns non-zero when the job is not currently loaded, and we cannot distinguish that from a real error. uninstall always proceeds to delete the file. If a future story needs to surface real unload errors, it must add a separate code path that distinguishes them (e.g., parse stderr for the "Could not find specified service" text), not change this default.
  - Hooks for upcoming stories:
    * US-081 (--notify body / osascript): `_post_notify` in cli/__main__.py currently puts the trajectory label in the title only and uses a `~/.praxis/weeks/{iso}.html` body. AC US-081 wants the body to mention `~/.praxis/latest.html` -- check whether to change the body text or introduce a `latest.html` symlink/copy. Existing notify happy-path tests (if any) live in tests/ for the week command; grep for `_post_notify` before touching.
    * US-082 (trajectory in body + non-crashing osascript): `_post_notify` already wraps the osascript call in a broad try/except, but the AC says "if osascript fails for any reason, the run logs the failure and still exits 0" -- need a test that covers a FileNotFoundError (osascript binary missing) explicitly, since today's try/except is best-effort but unverified by tests.
    * US-083 (non-macOS install-weekly path): the placeholder "not yet implemented" stderr message in cmd_install_weekly is the seam to flesh out -- print the systemd / Task Scheduler XML to stdout AND save to ~/.praxis/install-weekly-snippet.txt. The matching non-macOS branch for cmd_uninstall_weekly is already a sensible no-op ("non-macOS has no scheduled job to remove."); US-083 may not need to touch it.
  - Gotchas:
    * `fake_launchctl` fixture is reusable but accumulates calls across `install_weekly_macos()` + `uninstall_weekly_macos()` in the same test, so slice the list from the install call count before asserting on uninstall's behavior (see test_uninstall_weekly_unloads_and_deletes_plist).
    * `cmd_uninstall_weekly` imports from `praxis.cli.install_weekly` inside the function (not at module top) to match the existing install pattern -- keeps non-macOS imports off the hot path.
---

## Iteration 3 - US-081: macOS notification via osascript
- What I did: Reworked `_post_notify` in praxis/cli/__main__.py so the AppleScript title is the fixed string "Praxis weekly read is ready" and the body is "Open ~/.praxis/latest.html to read." (per spec 13.2 / AC US-081). Dropped the `week_iso` parameter (no longer referenced) and the title-override-by-trajectory branch; the trajectory-label-in-body work is US-082's. Updated the caller in `cmd_week` to call `_post_notify()` with no args, removing the now-dead `traj_label` calculation.
- Files changed: praxis/cli/__main__.py (refactored `_post_notify`, simplified caller), tests/test_cli.py (added 4 US-081 tests + `fake_osascript` fixture), scripts/ralph/feature/macos-launch-agent/prd.json (passes=true), scripts/ralph/progress.txt (this entry).
- Verification: `uv run pytest -q` -> 1034 passed (4 new + 1030 existing). `uv run pyright praxis/cli/__main__.py tests/test_cli.py` -> 4 errors, all pre-existing (3 WeeklyRunSummary on __main__.py + 1 "pytest could not be resolved" on test_cli.py). Confirmed unchanged via `git stash` round-trip; my edits introduce 0 new pyright errors.
- What the next iteration should know:
  - Patterns established:
    * `fake_osascript` fixture in tests/test_cli.py monkeypatches `praxis.cli.__main__.subprocess.run` and records every call. Reuse it for US-082's "osascript fails -> run still exits 0" test (just have the stub raise FileNotFoundError or return non-zero).
    * `_post_notify()` now takes no arguments. US-082 must re-add `trajectory_label: str | None` (it was removed in this iteration since US-081 has no use for it) and the caller in `cmd_week` needs to recompute `summary.trajectory.label.value.title()` before passing it through. The pre-edit code is in the git log at 381704b for reference.
    * The body text MUST contain the literal substring `~/.praxis/latest.html` -- US-082's expanded body (with trajectory label) must keep that substring intact, otherwise `test_notify_body_mentions_latest_html` will fail.
    * The title MUST stay literally "Praxis weekly read is ready" -- `test_notify_title_is_fixed_string` asserts `'with title "Praxis weekly read is ready"'` verbatim. US-082 adds the trajectory label to BODY, not title.
  - Hooks for upcoming stories:
    * US-082 (trajectory label in body + osascript failure tolerance): Re-add `trajectory_label` parameter to `_post_notify`, then weave the label into the body (spec section 13.2 example: "Praxis: Drifting this week. Open ~/.praxis/latest.html for the detail."). Add an explicit test that monkeypatches subprocess.run to raise `FileNotFoundError("osascript missing")` and asserts the CLI still exits 0 -- today's broad try/except handles it but no test verifies the contract.
    * US-083 (non-macOS install-weekly): the `fake_osascript` fixture is irrelevant; that story is about `install-weekly` printing a systemd/Task Scheduler snippet to stdout and saving it to `~/.praxis/install-weekly-snippet.txt`. See progress entries for iter 1/2 for the placeholder seam in `cmd_install_weekly`.
  - Gotchas:
    * `latest.html` is a SYMLINK created by the HTML digest writer (praxis/reports/digest_html.py, US-064), not by `_post_notify`. Tests don't verify the symlink exists at notification time -- the contract is only that the body MENTIONS the path. If a future change to the notify path tries to validate the symlink before notifying, it would couple notification to the HTML writer's symlink semantics, which is a regression in separation of concerns.
    * The notification posts AFTER `_weekly_html_path(target_week).write_text(...)` in `cmd_week`. The "after writing the HTML" half of the AC is structural (it's the code order), so the test asserts an HTML file exists at notify time as a smoke check rather than ordering subprocess.run vs write_text directly.
    * Tests monkeypatch `sys.platform` via `monkeypatch.setattr(sys, "platform", "darwin")`. monkeypatch reverts at teardown, so the patch is fully scoped to one test even though `sys` is a process-wide module.
---

## Iteration 4 - US-082: Notification includes trajectory label and never crashes the run
- What I did: Re-added the `trajectory_label: str | None = None` parameter to `_post_notify` in praxis/cli/__main__.py and wove the label into the body using the format "<Label> this week. Open ~/.praxis/latest.html for the detail." (spec 13.2 example). The caller in `cmd_week` now maps `summary.trajectory.label.value` through a small `_TRAJECTORY_LABEL_DISPLAY` dict so raw enum strings like `stable_engaged` render as user-facing `Engaged`. Hardened the subprocess block to capture stdout/stderr and log non-zero exits explicitly, plus the broad `except Exception` already in place catches `FileNotFoundError` / `PermissionError` etc. so the run still exits 0.
- Files changed: praxis/cli/__main__.py (`_TRAJECTORY_LABEL_DISPLAY` dict, `_post_notify` takes `trajectory_label`, capture+log non-zero exit, caller wiring in `cmd_week`), tests/test_cli.py (5 new US-082 tests), scripts/ralph/feature/macos-launch-agent/prd.json (passes=true), scripts/ralph/progress.txt (this entry).
- Verification: `uv run pytest -q` -> 1039 passed (5 new + 1034 existing). `uv run pyright praxis/cli/__main__.py tests/test_cli.py` -> 4 errors, identical to baseline before this iteration (verified via `git stash` round-trip). My edits introduce 0 new pyright errors.
- What the next iteration should know:
  - Patterns established:
    * Enum -> display mapping for trajectory labels lives in `_TRAJECTORY_LABEL_DISPLAY` at module top of cli/__main__.py. Keys mirror `TrajectoryLabel` enum values ("learning", "stable_engaged", "stable_passive", "atrophying", "insufficient_data"). The fallback for unknown labels is `raw.title()`. If a new TrajectoryLabel variant lands, add it here so the notification body stays user-facing.
    * The display dict is identical in spirit to the terminal renderer's `_TRAJ_TITLE` (praxis/reports/terminal.py L98), but kept separate to avoid a cross-module import from the CLI hot path. If a third surface needs it, lift it into a shared helper module.
    * To stub `run_weekly` in CLI tests, monkeypatch `praxis.cli.__main__.run_weekly` (not the source module) -- the CLI captured a reference at import time. See `test_notify_body_uses_user_facing_trajectory_label`.
    * Constructing a minimal `WeeklyRunSummary` in tests requires only: week_iso, sessions=[], tasks=[], judge_results={}, moments=[], selection=None, snapshot (with session_count>=1), rendered_html, rendered_terminal, elapsed_seconds, trajectory. All other fields default. `ProfileSnapshot` can be built directly (not via `from_scores`) when you want a known session_count without seeding rows.
  - Hooks for upcoming stories:
    * US-083 (non-macOS install-weekly): the trajectory/notification path is done -- US-083 is fully on `cmd_install_weekly`'s non-Darwin branch. The placeholder stderr message is the seam: replace it with code that prints the systemd timer or Task Scheduler XML to stdout AND saves it to `~/.praxis/install-weekly-snippet.txt`. Exit 0. The matching non-macOS branch in `cmd_uninstall_weekly` is already a sensible no-op -- US-083 likely doesn't need to touch it.
    * The schedule config (day/hour/minute) for the systemd / Task Scheduler snippets lives in `~/.praxis/config.toml` under `[schedule]`. See praxis/cli/install_weekly.py for how the macOS path loads it; reuse the same config reader.
  - Gotchas:
    * The notification body's prefix is `"<Label> this week."`. For `INSUFFICIENT_DATA` this becomes `"Reading this week."` -- mildly awkward but matches the spec 13.2 example phrasing. If product wants different phrasing per-label, change the mapping; don't add special-case branches around the label string concatenation.
    * `subprocess.run(..., capture_output=True, text=True)` is now used so we can log stderr on non-zero exits. The previous US-081 implementation passed neither, which left the subprocess writing directly to the parent's stderr. The change is invisible to existing tests because `fake_osascript` returns a CompletedProcess with empty stdout/stderr.
    * Tests that stub `run_weekly` MUST set `tmp_home` AND `fake_api_key` fixtures, otherwise the exit-2 (no API key) gate fires before `run_weekly` is reached. See `test_notify_body_uses_user_facing_trajectory_label`.
---

## Iteration 5 - US-083: Non-macOS prints snippet and exits cleanly
- What I did: Added pure renderers `build_systemd_snippet` (Linux user-timer) and `build_task_scheduler_snippet` (Windows XML) plus a `write_non_macos_snippet(praxis_home=None, *, platform_override=None)` dispatcher in `praxis/cli/install_weekly.py`. The CLI's non-macOS branch in `cmd_install_weekly` now calls the dispatcher, prints the content to stdout, prints "Saved snippet to: <path>" to stdout, and exits 0. The snippet is persisted to `~/.praxis/install-weekly-snippet.txt` via the new `snippet_path()` helper. Windows is detected via `sys.platform.startswith("win") or == "cygwin"`; everything else (Linux, FreeBSD, etc.) gets systemd.
- Files changed: praxis/cli/install_weekly.py (renderers + dispatcher + day-name maps + `_format_praxis_exec`), praxis/cli/__main__.py (cmd_install_weekly non-Darwin branch rewritten), tests/test_install_weekly.py (14 new US-083 tests, ScheduleConfig import, expanded header docstring), scripts/ralph/feature/macos-launch-agent/prd.json (passes=true), scripts/ralph/progress.txt (this entry).
- Verification: `uv run pytest -q` -> 1053 passed (14 new + 1039 existing). `uv run pyright praxis/cli/install_weekly.py praxis/cli/__main__.py tests/test_install_weekly.py` -> 4 errors, identical to baseline before this iteration (3 WeeklyRunSummary on __main__.py + 1 pytest import on test_install_weekly.py). Confirmed unchanged via `git stash` round-trip; my edits introduce 0 new pyright errors.
- What the next iteration should know:
  - All 5 PRD stories now pass. Ralph should detect <promise>COMPLETE</promise> on the next iteration.
  - Patterns established (in case follow-on stories touch this surface):
    * `build_systemd_snippet(schedule, praxis_argv)` and `build_task_scheduler_snippet(schedule, praxis_argv)` are pure -- no I/O, no config load. Tests call them directly with `ScheduleConfig(...)` to pin output without touching the tmp_home config file.
    * `write_non_macos_snippet(praxis_home=None, *, platform_override=None)` is the I/O layer: it reads `~/.praxis/config.toml`, resolves the praxis argv, dispatches by platform, writes the snippet to disk, and returns `(path, content)`. Tests pass `platform_override="linux" | "win32" | "freebsd13"` to exercise dispatch without touching `sys.platform`.
    * Day-of-week dispatch uses two new dicts: `_SYSTEMD_DAY_BY_NAME` ("sunday" -> "Sun") and `_TASKSCHEDULER_DAY_BY_NAME` ("sunday" -> "Sunday"). Both normalize via lowercase strip; invalid day strings raise `InstallWeeklyError` (so the user gets a usable error instead of an invalid unit/XML file).
    * `snippet_path(home=None)` returns `<home or PRAXIS_HOME>/install-weekly-snippet.txt`, mirroring `plist_path` in shape so tests can pass an explicit home or rely on PRAXIS_HOME via tmp_home.
  - Gotchas:
    * `_resolve_praxis_command()` calls `shutil.which("praxis")`, which on Python 3.13 dives into `_winapi.NeedCurrentDirectoryForExePath` when `sys.platform == "win32"`. That attribute is None on macOS, so the test that monkeypatches `sys.platform = "win32"` must ALSO stub `praxis.cli.install_weekly.shutil.which` to avoid an AttributeError crash. See `test_cli_install_weekly_windows_saves_task_scheduler_xml`. Tests that use the `platform_override=` parameter (not sys.platform monkeypatching) don't need this stub because `shutil.which` still sees the host's real platform.
    * The CLI prints the snippet body AND the "Saved snippet to:" line to stdout (not stderr). This matches the existing macOS branch ("Installed weekly LaunchAgent: ...") which also goes to stdout. If a user pipes stdout to a file, both lines will land there; that's deliberate so the saved unit text is self-contained.
    * Task Scheduler XML uses a fixed `StartBoundary` of `2020-01-01T<hh>:<mm>:00` so the trigger is immediately valid. If a future story needs date-aware output (e.g., to start "next Sunday"), inject a clock; don't randomize the fixed string.
---

## Iteration 1 (schema-migrations) - US-001: Migration runner with transactional semantics
- What I did: Added a SQL-file migration runner to praxis/storage/profile_store.py. On `ProfileStore` init it ensures `schema_migrations(version TEXT PRIMARY KEY, applied_at TEXT NOT NULL)` exists, walks `praxis/storage/migrations/*.sql` in lexical order, and applies each pending file inside a single `BEGIN;...COMMIT;` script that also INSERTs the version row. On `sqlite3.OperationalError` the transaction rolls back, `schema_migrations` stays empty for that version, and the next invocation re-attempts the same file. Created the empty `praxis/storage/migrations/` dir (with `.gitkeep`) ready for US-002/US-003/US-004 to drop SQL files into. Wired `storage/migrations/*.sql` into `[tool.setuptools.package-data]` so the files travel with wheels.
- Files changed:
  - praxis/storage/profile_store.py (new `_run_sql_migrations`, `_ensure_schema_migrations_table`, `_applied_migration_versions`, `_apply_sql_migration`, `_migrations_dir` classmethod, `_sqlite_literal` helper; `_init_schema` calls the runner in both the early-return and freshly-migrated branches)
  - praxis/storage/migrations/.gitkeep (new, empty)
  - pyproject.toml (added `"storage/migrations/*.sql"` to package_data)
  - tests/test_migration_runner.py (new, 13 tests)
  - scripts/ralph/feature/schema-migrations/prd.json (US-001 passes=true)
  - scripts/ralph/progress.txt (this entry)
- Verification:
  - `uv run pytest -q` -> 1066 passed (13 new + 1053 prior).
  - `uv run pyright praxis/storage/profile_store.py` -> 0 errors.
  - `uv run pyright tests/test_migration_runner.py` -> 1 error, the "pytest could not be resolved" import error shared by every other test file (pre-existing, environment-level, not introduced by this iteration).
  - `uv run mypy praxis/storage/profile_store.py` -> 7 errors, all in `praxis/scoring/judge.py` and `praxis/scoring/aggregate.py` (transitively imported). Verified identical to baseline before this iteration via `git stash` round-trip; my edits introduce 0 new mypy errors. `uv run mypy praxis` similarly hits the pre-existing `digest_html.py:595` f-string-with-backslash syntax error baked in before this branch existed.
- What the next iteration should know:
  - Patterns established:
    * `ProfileStore._migrations_dir()` is a `@classmethod` returning `Path(__file__).parent / "migrations"`. Tests override it with `monkeypatch.setattr(ProfileStore, "_migrations_dir", classmethod(lambda cls: tmp_path))`. Reuse this exact monkeypatch helper (`_patch_migrations_dir` in tests/test_migration_runner.py) for US-002/US-003/US-004 tests that want to exercise their migration in isolation without dropping the real .sql file into the package.
    * Version identifier = filename (e.g. `001_init.sql`). Lexical ordering uses `sorted(migrations_dir.glob("*.sql"))`. Recommendation for new migration filenames: `NNN_<short_snake_case>.sql` so numbers sort correctly (use `001`, `002`, ... not `1`, `2`).
    * Each migration is wrapped by the runner: do NOT include `BEGIN;` or `COMMIT;` inside the migration `.sql` file - the runner adds them. The runner also appends the `INSERT INTO schema_migrations(...)` row inside the same transaction; do not insert it yourself.
    * The runner runs in BOTH branches of `_init_schema`: the existing "already at v3 marker" early-return branch and the "fresh/v0.1/v0.2 DB needs v3 migration" branch. So a brand-new DB and a 90-day-old DB both flow through `_run_sql_migrations()` on the very next init call.
    * `_sqlite_literal()` only doubles single quotes - safe for our use (filenames + ISO timestamps don't contain quotes), but if you ever need to embed user-supplied strings in an `executescript` body, prefer cursor.execute() with parameters instead.
  - Hooks for upcoming stories:
    * US-002: write `praxis/storage/migrations/001_follow_ups_active_commitment.sql` (or whatever filename comes next) that does the `ALTER TABLE follow_ups ADD COLUMN ...` + `CREATE UNIQUE INDEX ... WHERE ...` work. The existing `follow_ups` table is created by `SCHEMA` (top of profile_store.py L132-140) on fresh DBs and by `_apply_v2_schema` on legacy DBs, so by the time `_run_sql_migrations()` is reached the table is guaranteed to exist. AC #2 wants a *partial* unique index keyed on `(week_iso) WHERE outcome='pending' AND superseded_by IS NULL` - SQLite supports partial indexes; just be sure to write `CREATE UNIQUE INDEX ... ON follow_ups(week_iso) WHERE ...`.
    * US-003: same pattern - new `.sql` file creating `session_reflections`. The AC also asks for an `insert_session_reflection(...)` helper on `ProfileStore`; that's Python code, add it next to `save_follow_up` (profile_store.py L692). Use `_utcnow().isoformat()` for `created_at` to keep ISO-8601 UTC consistency.
    * US-004: same `.sql` pattern for `aug_auto_classification TEXT` + `aug_auto_confidence REAL` on `session_scores`. The AC requires a Python write helper that raises `ValueError` BEFORE any SQL - that's a precondition check on the value set; raise the error before opening a `_conn()` block to avoid leaking partial writes. Read helper returns `(None, None)` for pre-migration rows because the new columns default to NULL on ALTER TABLE ADD COLUMN.
  - Gotchas:
    * `executescript()` in Python's sqlite3 module COMMITs any pending implicit transaction before running. I verified this with a small reproducer (see commit message). The runner avoids the trap by putting `BEGIN;`/`COMMIT;` INSIDE the script string so the transaction lives inside the same `executescript` call. Don't try to wrap `executescript` with an explicit `conn.execute("BEGIN")` from outside - it won't roll back partial DDL.
    * Python 3.12+ exposes `sqlite3.connect(..., autocommit=True)` which dodges the implicit-commit-before-executescript trap, but `pyproject.toml` declares `requires-python = ">=3.10"` so we can't rely on it. The current script-with-embedded-BEGIN/COMMIT approach works on 3.10/3.11/3.12/3.13.
    * The `.gitkeep` in `praxis/storage/migrations/` is intentional - it keeps the directory tracked in git so a fresh clone doesn't trigger `migrations_dir.is_dir() == False`. The runner tolerates a missing dir (treated as "no migrations"), but the directory should exist in the repo so future stories can drop files into it without ceremony.
    * Existing v0.2/v0.3 migration logic (`_apply_v2_schema`, `_migrate_session_scores_to_v3`, the backup/restore machinery, the `schema_version='3'` marker row in `run_log`) is COMPLETELY UNCHANGED. The new `schema_migrations` table is a forward-only ledger for *future* schema work; don't merge the two. The `run_log` marker is internal-only and only tracks v0.2/v0.3 transition state.
---

## Iteration 2 (schema-migrations) - US-002: follow_ups columns + active-commitment unique index
- What I did: Added `praxis/storage/migrations/001_follow_ups_active_commitment.sql`, which rebuilds the follow_ups table with `id INTEGER PRIMARY KEY AUTOINCREMENT` (replacing the old `week_iso TEXT PRIMARY KEY`), copies existing rows over, adds the three new columns (`user_chosen INTEGER NOT NULL DEFAULT 0`, `display_text TEXT`, `superseded_by INTEGER REFERENCES follow_ups(id)`), and creates the partial-unique index `idx_follow_ups_one_active_per_week` keyed on `week_iso` with predicate `outcome='pending' AND superseded_by IS NULL`. Updated `save_follow_up` to UPDATE the latest row for the given week_iso when one exists (and INSERT otherwise) so the single-row-per-week semantics existing callers depend on still hold AND so the row's `id` stays stable for future FK references (US-003 session_reflections.follow_up_id). Updated `load_follow_up` / `latest_follow_up` / `prior_follow_up` to ORDER BY id DESC LIMIT 1 so the "latest row per week" is always returned even when supersede chains accumulate.
- Files changed:
  - praxis/storage/migrations/001_follow_ups_active_commitment.sql (new)
  - praxis/storage/profile_store.py (save_follow_up rewritten as UPDATE-or-INSERT; load/latest/prior tie-break on id DESC)
  - tests/test_storage_schema.py (updated test_follow_ups_table_columns to expect id PK / week_iso non-PK; added 8 US-002 tests covering column presence, FK to follow_ups(id), partial-unique index existence + behavior, post-supersede insert, distinct-outcome coexistence, and v0.2-row preservation through migration)
  - scripts/ralph/feature/schema-migrations/prd.json (US-002 passes=true)
  - scripts/ralph/progress.txt (this entry)
- Verification:
  - `uv run pytest -q` -> 1074 passed (8 new + 1066 prior).
  - `uv run mypy praxis/storage/profile_store.py` -> 7 errors, identical to the US-001 baseline (transitive errors in praxis/scoring/judge.py + praxis/scoring/aggregate.py). Confirmed via git stash round-trip; this iteration introduces 0 new mypy errors.
- What the next iteration should know:
  - Patterns established:
    * Table-rebuild migrations: when an AC needs to change the PRIMARY KEY (SQLite cannot ALTER a PK in place), the migration creates `<table>_v2` with the new schema, copies via INSERT...SELECT, DROP TABLE old, ALTER TABLE rename. Self-referencing FKs (`REFERENCES follow_ups(id)`) survive the rename because SQLite stores the FK metadata textually and the post-rename name matches. FK pragma is OFF by default in this codebase, so the FK metadata is "for future enforcement" rather than active; tests don't need `PRAGMA foreign_keys=ON` to exercise the partial-unique index.
    * Partial-unique index syntax: `CREATE UNIQUE INDEX <name> ON <table>(<col>) WHERE <predicate>` (SQLite-native). The predicate uses raw SQL operators, including `IS NULL`. Inserting a row that matches the predicate while another matching row exists raises `sqlite3.IntegrityError("UNIQUE constraint failed: <table>.<col>")`.
    * `save_follow_up`'s UPDATE-or-INSERT branching: it queries `SELECT id FROM follow_ups WHERE week_iso = ? ORDER BY id DESC LIMIT 1`; if a row is found it UPDATEs that id (preserving the value for any FK referrer), otherwise INSERTs a fresh row. The UPDATE deliberately does NOT touch `user_chosen`, `display_text`, or `superseded_by` - those are owned by the future user-picks-from-alternatives flow.
  - Hooks for upcoming stories:
    * US-003: write `praxis/storage/migrations/002_session_reflections.sql` creating `session_reflections` with `id INTEGER PRIMARY KEY AUTOINCREMENT`, `session_stable_id TEXT NOT NULL`, `follow_up_id INTEGER NOT NULL REFERENCES follow_ups(id)`, `self_report TEXT NOT NULL CHECK (self_report IN ('yes','no','partial','skip'))`, `note TEXT`, `created_at TEXT NOT NULL`, plus index `idx_reflections_follow_up ON session_reflections(follow_up_id)`. The FK to `follow_ups(id)` will resolve cleanly because US-002 added the `id` column. The Python helper `insert_session_reflection(...)` should live on ProfileStore next to `save_follow_up`; use `_utcnow().isoformat()` for `created_at`. The AC requires it to ALSO be exposed at module level - check whether "exposed on profile_store.py" means an instance method or also a module-level function; the simplest read is an instance method.
    * US-004: same migration pattern. ALTER TABLE session_scores ADD COLUMN aug_auto_classification TEXT and aug_auto_confidence REAL. Pre-migration rows get NULL for both - the read helper returns (None, None) for those naturally. The Python write helper that raises ValueError BEFORE issuing SQL should validate `aug_auto_classification in {'augmentation','automation','mixed'}` (None passes through too, since we want to allow clearing the value); raise before opening `with self._conn()` so no transaction is even started.
  - Gotchas:
    * Existing v0.2 schema tests in tests/test_storage_schema.py exercise the FINAL schema state (which now includes US-002 changes). `test_follow_ups_table_columns` had to be updated from `week_iso pk == 1` to `id pk == 1 / week_iso pk == 0`. If US-003 changes ANY column on a table that has a v0.2 schema test, the corresponding `test_*_table_columns` assertion must be updated too.
    * `_insert_active_pending` helper in tests/test_storage_schema.py is just a local convenience for US-002 tests; it inserts one pending row and returns lastrowid. If US-003 tests need a similar helper for session_reflections, write your own next to your tests rather than coupling US-003 tests to this helper.
    * `test_existing_follow_ups_rows_preserved_through_migration` simulates a pre-US-002 schema by dropping the post-migration table, recreating the v0.2 shape, inserting a row, then clearing the `schema_migrations` marker so the runner re-applies US-002 on next open. This is the cleanest way to assert "old data survives" without restructuring the test fixtures. Reuse the same approach if a future migration changes data shape.
    * `INSERT OR REPLACE INTO follow_ups (week_iso, ...) VALUES (...)` no longer behaves like an upsert keyed on week_iso (week_iso is not unique anymore). Anything that previously relied on that semantic must now go through `save_follow_up()` (which does the lookup explicitly) or write its own UPSERT against the partial-unique index. I grepped all `INSERT OR REPLACE INTO follow_ups` usages - the only one is inside save_follow_up itself, which I rewrote.
---

## Iteration 3 (schema-migrations) - US-003: session_reflections table
- What I did: Added `praxis/storage/migrations/002_session_reflections.sql` creating the `session_reflections` table (id INTEGER PRIMARY KEY AUTOINCREMENT, session_stable_id TEXT NOT NULL, follow_up_id INTEGER NOT NULL REFERENCES follow_ups(id), self_report TEXT NOT NULL CHECK (...IN 'yes/no/partial/skip'), note TEXT, created_at TEXT NOT NULL) plus the `idx_reflections_follow_up` index on follow_up_id. Added `ProfileStore.insert_session_reflection(*, session_stable_id, follow_up_id, self_report, note=None) -> int` that writes the row inside `with self._conn() as conn:` and returns the new id; `created_at` is `_utcnow().isoformat()` so it's ISO-8601 with a `+00:00` UTC offset suffix. Bad self_report values surface as `sqlite3.IntegrityError` via the column CHECK (no Python-side pre-check needed, parallel to how moments/follow_ups/tasks all let the CHECK constraint do the work).
- Files changed:
  - praxis/storage/migrations/002_session_reflections.sql (new)
  - praxis/storage/profile_store.py (new `insert_session_reflection` method appended after `latest_follow_up`)
  - tests/test_storage_schema.py (added 10 US-003 tests + `_seed_follow_up_id` helper; added `from datetime import datetime, timedelta` for the timestamp round-trip test)
  - scripts/ralph/feature/schema-migrations/prd.json (US-003 passes=true)
  - scripts/ralph/progress.txt (this entry)
- Verification:
  - `uv run pytest -q` -> 1085 passed (10 new + 1075 prior; the prior baseline grew by 1 between US-002 and this iteration via an unrelated test elsewhere in the suite).
  - `uv run pytest tests/test_storage_schema.py -v` -> 57 passed (10 new US-003 tests + 47 prior).
  - `uv run mypy praxis/storage/profile_store.py` -> 7 errors, identical to the US-001/US-002 baseline (transitive errors in praxis/scoring/judge.py + praxis/scoring/aggregate.py). My edits introduce 0 new mypy errors.
  - `uv run mypy praxis` (full package) -> 1 syntax error in praxis/reports/digest_html.py:595 (f-string + backslash), pre-existing on the branch and unrelated to schema-migrations - documented in iteration 1's notes.
- What the next iteration should know:
  - Patterns established:
    * Helper-on-ProfileStore convention: insert_session_reflection takes kwargs only (`*,`), returns `int` for the new lastrowid, and uses `assert row_id is not None` so the return type is unambiguously `int` (not `int | None`). Match this shape if US-004's write helper needs to return the inserted row id; otherwise return None.
    * The CHECK constraint does the value validation for `self_report`. There's no Python-side pre-check because the AC explicitly says "Inserting with self_report='maybe' raises sqlite3.IntegrityError" - matching that means letting the DB enforce the constraint. US-004 is different: its AC says the Python helper must raise ValueError BEFORE any SQL, so US-004's helper must validate before calling `_conn()` (don't mirror this iteration's pattern there).
    * Test helper `_seed_follow_up_id(conn, week_iso='2026-W21')` inserts one pending follow_up row and returns its id. Lives in the US-003 section of tests/test_storage_schema.py. US-004 can reuse it if a test needs a follow_up id; otherwise write a local helper next to the US-004 tests.
    * The `datetime` and `timedelta` imports landed in tests/test_storage_schema.py top-of-file for the ISO timestamp round-trip test. They're already imported now - US-004 tests can reuse them without adding more imports.
  - Hooks for upcoming stories:
    * US-004: write `praxis/storage/migrations/003_session_scores_aug_auto.sql` doing `ALTER TABLE session_scores ADD COLUMN aug_auto_classification TEXT` and `ALTER TABLE session_scores ADD COLUMN aug_auto_confidence REAL`. Both are nullable so pre-migration rows return NULL on read (matching the "(None, None) for pre-migration rows" AC). Add a read helper `get_session_aug_auto(stable_id)` returning `tuple[str | None, float | None]`. Add a write helper `set_session_aug_auto(stable_id, classification, confidence)` that raises `ValueError` if `classification not in {'augmentation','automation','mixed'}` BEFORE opening `_conn()`. Both helpers go in profile_store.py near `save_session_score`.
    * The session_scores table has a composite PK on (stable_id, judge_pass), so a read helper for aug_auto should decide whether to return values from the highest-pass row (probably yes, to match `load_one_session_score` semantics) or all rows. Read the relevant section of profile_store.py (L511-537 `load_one_session_score`) before writing the helper.
  - Gotchas:
    * The FK `follow_up_id INTEGER NOT NULL REFERENCES follow_ups(id)` is declared but FK enforcement is OFF by default (foreign_keys pragma). Tests do not need `PRAGMA foreign_keys=ON` to assert the FK metadata via `PRAGMA foreign_key_list(...)`. Inserting a session_reflections row with a non-existent follow_up_id will NOT raise at insert time unless the pragma is on; the test suite chooses to verify the FK metadata only, matching how follow_ups self-FK is tested.
    * `cur.lastrowid` can be `None` per the sqlite3 type stubs even though SQLite's INSERT always populates it for AUTOINCREMENT tables. The `assert row_id is not None` line lets mypy/pyright narrow the type so the method returns `int` (not `int | None`). Don't remove the assert.
    * `datetime.fromisoformat()` on `2026-05-28T...+00:00` returns a tz-aware datetime on Python 3.11+. Our `requires-python = ">=3.10"` so 3.10 should also work for `+00:00` (3.10 supports it; what 3.10 doesn't support is the trailing `Z`). The runner's emitted timestamps use `+00:00` from `datetime.isoformat()`, so the round-trip works on all supported versions.
---

## Iteration 4 (schema-migrations) - US-004: session_scores aug_auto columns
- What I did: Added `praxis/storage/migrations/003_session_scores_aug_auto.sql` which runs two `ALTER TABLE session_scores ADD COLUMN ...` statements (`aug_auto_classification TEXT`, `aug_auto_confidence REAL`, both nullable). Added two helpers on `ProfileStore`: `set_session_aug_auto(stable_id, classification, confidence)` UPDATEs every persisted row for the session (both pass-1 and pass-2 when present) and raises `ValueError` BEFORE opening any DB connection when `classification not in ('augmentation','automation','mixed')`, and `get_session_aug_auto(stable_id) -> tuple[str | None, float | None]` reads from the highest-pass row (matching `load_one_session_score` semantics), returning `(None, None)` for missing sessions or NULL columns. Updated `test_session_scores_schema_unchanged_across_v0_2_migration` to include the two new columns in the expected set.
- Files changed:
  - praxis/storage/migrations/003_session_scores_aug_auto.sql (new)
  - praxis/storage/profile_store.py (new `set_session_aug_auto` + `get_session_aug_auto` + `_AUG_AUTO_VALID` tuple in the session scores section)
  - tests/test_storage_schema.py (added 16 US-004 tests + `_seed_session_score_row` helper; updated 1 pre-existing schema-shape assertion to include the new columns)
  - scripts/ralph/feature/schema-migrations/prd.json (US-004 passes=true)
  - scripts/ralph/progress.txt (this entry)
- Verification:
  - `uv run pytest -q` -> 1101 passed (16 new + 1 modified + 1084 prior; the prior baseline was 1085, the modified test is `test_session_scores_schema_unchanged_across_v0_2_migration` which I had to update to include the new columns).
  - `uv run pytest tests/test_storage_schema.py -v` -> 73 passed (16 new US-004 tests + 57 prior).
  - `uv run mypy praxis/storage/profile_store.py` -> 7 errors, identical to the US-001/US-002/US-003 baseline (transitive errors in praxis/scoring/judge.py + praxis/scoring/aggregate.py). My edits introduce 0 new mypy errors.
  - `uv run pyright praxis/storage/profile_store.py` -> 0 errors.
- What the next iteration should know:
  - ALL 4 PRD STORIES NOW PASS. Ralph should detect <promise>COMPLETE</promise> on the next iteration.
  - Patterns established:
    * Two-helper pattern for "optional per-session derived data": `set_session_<thing>` (UPDATE, validates first, no-op when row missing) + `get_session_<thing>` (returns the value tuple, or all-Nones when missing/NULL). The set helper deliberately touches all judge_pass rows so the read helper sees the value regardless of which pass row it reads. If a future schema column is similarly "classifier output piggy-backed onto session_scores," this is the shape to copy.
    * `_AUG_AUTO_VALID = ("augmentation", "automation", "mixed")` is a class-level tuple on `ProfileStore`. Validation is `if classification not in self._AUG_AUTO_VALID: raise ValueError(...)` BEFORE opening any connection. Tests verify this with a `monkeypatch.setattr(ProfileStore, "_conn", boom)` guard - the helper must raise before `_conn` is invoked. Reuse the same monkeypatch trick for any future "validate before SQL" helper.
    * `set_session_aug_auto` UPDATE silently no-ops when no row matches the stable_id. We don't raise because the caller will typically `set_session_aug_auto` shortly after `save_session_score` and the row's absence would be a programmer error, not a runtime contingency. If a caller needs to know whether the update landed, they can `has_session(stable_id)` first.
  - Hooks for upcoming stories: none - PRD complete. If a story lands later that wires up the augmentation/automation classifier itself, the natural integration point is to call `store.set_session_aug_auto(sid, label, confidence)` after `store.save_session_score(score)` in the scoring pipeline (`praxis/scoring/...`).
  - Gotchas:
    * The pre-existing `test_session_scores_schema_unchanged_across_v0_2_migration` asserts the full column set. Adding any new column to `session_scores` (now or later) must update this set or the test will fail. The test isn't testing migration behavior per se; it's pinning the column shape so an accidental schema drift is caught.
    * `aug_auto_confidence` is REAL with no range constraint at the DB layer (so confidence > 1.0 or negative values would persist if anything wrote them). The Python write helper also does not range-check confidence. If a future requirement needs 0.0 <= confidence <= 1.0 enforcement, add it inside `set_session_aug_auto` (before `_conn()`); don't add a CHECK constraint because that would require yet another migration.
    * The migration uses `ALTER TABLE ... ADD COLUMN` (not a table rebuild) because no PK/composite-PK change is needed. Pre-existing rows automatically get NULL in the new columns. SQLite's `ALTER TABLE ADD COLUMN` is O(1) (just updates the schema, no row rewrite), so the migration is cheap even on large DBs.
---
