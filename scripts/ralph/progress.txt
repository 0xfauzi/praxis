# progress.txt: union of factory component logs

## Iteration 1 - US-047: Weekly spend and baseline
- What I did: Added praxis/scoring/cost_ledger.py with a pure aggregator (compute_cost_ledger) that sums per-session USD into a current-week total + a 90-day rolling weekly mean, plus a card-resolution helper (estimate_session_cost_usd) that mirrors models_advisor/advisor.py._estimate_cost. Backed by tests/test_cost_ledger.py (19 tests).
- Files changed: praxis/scoring/cost_ledger.py (new), tests/test_cost_ledger.py (new), scripts/ralph/feature/cost-ledger/prd.json
- Verification:
  - python -m pytest tests/test_cost_ledger.py -v -> 19 passed
  - python -m pytest -q -> all passed
  - pyright praxis/scoring/cost_ledger.py tests/test_cost_ledger.py -> 0 errors
  - mypy praxis/scoring/cost_ledger.py tests/test_cost_ledger.py -> 0 errors in the new files (pre-existing errors in advisor.py / aggregate.py / coach.py / cli/__main__.py are unrelated and were present before this iteration)
- What the next iteration should know:
  - The aggregator is pure: pre-computed `cost_usd` in, ledger out. No disk I/O. Card lookup is isolated to `estimate_session_cost_usd`, which calls `find_card_for_model_hint` (matches the advisor pattern). Tests of the aggregator don't need `tmp_home`; helper tests do.
  - Baseline rule: mean of per-ISO-week spend totals over weeks with at least one priced session. Quiet weeks are NOT zero-padded -- locked in by `test_baseline_excludes_zero_weeks`. Rationale: a user who took time off shouldn't see an artificially low baseline that makes every active week read as "over baseline".
  - `cost_usd=None` marks unpriced sessions (no card on file, or subscription-only models like Copilot whose pricing fields are null). They are skipped, not counted as zero, so the weekly total and the baseline both reflect "priced spend" only. `weekly_session_count` and `baseline_session_count` are priced-session counts. If a future story needs total-session counts (priced + unpriced), pull them from the orchestrator/cluster panel, not from CostLedger.
  - Window mirrors `praxis.scoring.baseline.compute_baseline`: `[as_of_date - 90 days, current ISO week Monday)`. Current ISO week is excluded from the baseline (same logic, same Monday boundary).
  - Cost-estimate constants (`COST_CHARS_PER_TOKEN=4.0`, `COST_OUTPUT_TO_INPUT_RATIO=1.5`) match `models_advisor/advisor.py._estimate_cost`. A future story that consolidates cost estimation should keep them aligned in one place; the constants test (`test_estimate_session_cost_constants_match_advisor`) is the canary.
  - US-048 (biggest (model, task) line) needs per-session model_hint and task_id. Those are NOT on CostLedgerInputSession yet -- US-048 will need a richer input dataclass (or a separate function) that carries the model + task identity per session. Suggest: keep `compute_cost_ledger` minimal for the top-line numbers, add a sibling `compute_top_line` (or similar) that takes a richer input.
  - US-049 (tier-fit savings) needs `user_turn_count` and `avg_prompt_chars` per session plus the cheaper-tier card lookup. The helper `_find_fast_tier_card_in_family` already exists in `models_advisor/advisor.py` -- consider extracting it to `praxis/models_advisor/cards.py` if multiple modules will need it.
  - US-050 (dollar_impact_estimate per moment) modifies the Moment dataclass / persistence path; cost_ledger.py is upstream of moments rendering, so the two stories don't share much code. Use the same chars-per-token + output-multiplier constants for consistency.
  - US-051 (renderer disclaimer) is a reports-layer change. The cost ledger module already enforces the "rough estimate" framing via the dataclass field naming and docstrings; the actual disclaimer text lives in the renderer that consumes CostLedger.
---

## Iteration 2 - US-048: Biggest (model, task) line
- What I did: Added a sibling aggregator `compute_biggest_line` to praxis/scoring/cost_ledger.py, plus a richer per-session input `BiggestLineInputSession` (started_at, cost_usd, model_hint, task_label) and an output `BiggestLine` (model_hint, task_label, spend_usd, session_count). Groups by (model_hint, task_label) within the current ISO week only and picks the max-spend bucket with a deterministic tiebreak. Added 15 tests in tests/test_cost_ledger.py.
- Files changed: praxis/scoring/cost_ledger.py, tests/test_cost_ledger.py, scripts/ralph/feature/cost-ledger/prd.json
- Verification:
  - python -m pytest tests/test_cost_ledger.py -v -> 34 passed (19 from US-047 + 15 new)
  - python -m pytest -q -> all 728 passed
  - pyright praxis/scoring/cost_ledger.py tests/test_cost_ledger.py -> 0 errors
  - mypy praxis/scoring/cost_ledger.py tests/test_cost_ledger.py -> 0 errors in the two source files (the 23 errors mypy reports are all in pre-existing files: trajectory.py, judge.py, aggregate.py, advisor.py -- same set documented in Iteration 1's handoff)
- What the next iteration should know:
  - Two new dataclasses (BiggestLineInputSession, BiggestLine) live alongside CostLedger* in cost_ledger.py. I chose to keep `compute_cost_ledger` minimal (top-line totals + baseline) and add `compute_biggest_line` as a sibling rather than fold the panel data into CostLedger -- the inputs are richer (need model + task identity) and the panel is conceptually a different aggregation. The caller will compute both from the same week of sessions.
  - Skipping rule: a session is excluded from the biggest-line aggregation iff `cost_usd is None OR model_hint is None OR task_label is None`. The `model_hint`/`task_label`/`cost_usd`-all-required rule is stricter than the parent ledger (which only requires priced cost_usd) because the panel exists to NAME a specific pair -- an 'unknown/unknown' row would be a regression in clarity. If a future story (e.g. US-049 tier-fit) wants to count unidentified-task sessions, it needs a different input.
  - Tiebreak rule (locked in by tests): max spend -> max session_count -> ascending (model_hint, task_label). The session-count tier exists because two equal-spend pairs read as "the busier one matters more" to a user; the alphabetical tier is purely for determinism. Float ties at exact USD are unlikely in practice but the test suite locks them in so renderers stay stable across runs.
  - Window: current ISO week ONLY (Monday-inclusive). Baseline-window sessions are silently skipped here -- the baseline answer is exposed via `CostLedger.baseline_weekly_mean_usd`, so duplicating it would be confusing.
  - Empty-result convention: `BiggestLine(model_hint=None, task_label=None, spend_usd=0.0, session_count=0)`. Renderers should treat `model_hint is None` as the signal to omit the panel row entirely, NOT to print "no biggest line" prose.
  - US-049 (tier-fit savings): needs `user_turn_count` and `avg_prompt_chars` per session AND the cheaper-tier card lookup. The existing `find_card_for_model_hint` returns one card; the helper `_find_fast_tier_card_in_family` in models_advisor/advisor.py is the cheaper-tier resolver -- still private, still worth extracting to praxis/models_advisor/cards.py if US-049 is the second caller. The output should expose the qualifying-session count AND the savings estimate to the renderer (same renderer-facing shape as BiggestLine, distinct dataclass).
  - US-050 (per-moment dollar_impact_estimate): downstream of moments persistence; the chars-per-token + output-multiplier constants `COST_CHARS_PER_TOKEN` and `COST_OUTPUT_TO_INPUT_RATIO` should be reused so the per-moment estimates and the weekly ledger agree.
  - US-051 (renderer disclaimer): purely a reports-layer change. cost_ledger.py is intentionally framing-agnostic; the disclaimer literal lives in the renderer.
---

## Iteration 3 - US-049: Tier-fit savings estimate
- What I did: Added a pure aggregator `compute_tier_fit_savings` to praxis/scoring/cost_ledger.py over a new `TierFitInputSession` (started_at, user_turn_count, avg_prompt_chars, tier_fit_savings_usd) returning `TierFitSavings` (qualifying_session_count, estimated_savings_usd). Card-lookup helper `estimate_tier_fit_savings_for_session` resolves the frontier card + family fast-tier sibling and returns per-session (frontier_cost - cheaper_tier_cost) using the shared chars-per-token + output-multiplier constants. Local `_find_fast_tier_card_in_family` mirrors the helper in models_advisor/advisor.py (not extracted to cards.py to keep the change focused on cost_ledger.py). Exposed `TIER_FIT_MAX_USER_TURNS=3` and `TIER_FIT_MAX_AVG_PROMPT_CHARS=200` as module-level constants. Added 26 tests (14 aggregator + 12 helper).
- Files changed: praxis/scoring/cost_ledger.py, tests/test_cost_ledger.py, scripts/ralph/feature/cost-ledger/prd.json
- Verification:
  - python -m pytest tests/test_cost_ledger.py -v -> 60 passed (34 from US-047/US-048 + 26 new)
  - python -m pytest -q -> 754 passed
  - pyright praxis/scoring/cost_ledger.py tests/test_cost_ledger.py -> 0 errors, 0 warnings
  - mypy praxis/scoring/cost_ledger.py tests/test_cost_ledger.py -> 0 errors in the two source files (same 23 pre-existing errors in trajectory.py / judge.py / aggregate.py / advisor.py as documented in iterations 1+2 -- unchanged)
- What the next iteration should know:
  - Three new symbols joined the cost_ledger.py public surface: `TierFitInputSession`, `TierFitSavings`, `compute_tier_fit_savings`, plus the helper `estimate_tier_fit_savings_for_session` and constants `TIER_FIT_MAX_USER_TURNS`/`TIER_FIT_MAX_AVG_PROMPT_CHARS`. Same pure-aggregator pattern as `compute_cost_ledger` / `compute_biggest_line`: aggregator filters + sums, card lookup is in the helper.
  - The PRD says 'Opus/GPT-5 sessions' but the implementation generalizes to any frontier-tier card with a fast-tier sibling in the same family. Concretely: claude-opus-4-7 -> claude-haiku-4-5 and gpt-5 -> gpt-5-mini. Gemini 2.5 Pro is frontier but has no fast-tier sibling shipped, so it naturally drops out (no savings claimable). This is locked in by `test_estimate_tier_fit_savings_gemini_returns_none_no_fast_sibling`. If a user drops a Gemini fast-tier card under ~/.praxis/model_cards/, Gemini sessions will start counting automatically -- intentional, not a bug.
  - Qualifying thresholds are `<=`, not `<`: a session at exactly 3 turns or exactly 200 avg chars qualifies (locked in by `test_tier_fit_thresholds_are_inclusive`). Both must hold (AND not OR -- `test_tier_fit_both_thresholds_must_be_met`).
  - `tier_fit_savings_usd == 0.0` is a valid qualifying session, NOT a skip (locked in by `test_tier_fit_zero_savings_still_counts_as_qualifying`). The 0.0 case happens with `total_input_chars <= 0` -- frontier session, just empty. None signals 'no frontier+fast sibling available' and IS the skip condition.
  - I deliberately did NOT extract `_find_fast_tier_card_in_family` into `praxis/models_advisor/cards.py` (which iteration 2 suggested). I kept a local copy in cost_ledger.py instead, so the change is contained to two files. If a third caller appears (e.g. US-050 referencing the cheaper tier per-moment), extracting at that point would be cleanest. Both copies are tiny (4 lines).
  - US-050 (per-moment dollar_impact_estimate): the per-session helper `estimate_tier_fit_savings_for_session` returns exactly the 'fit (over-tier)' estimate the spec asks for (`frontier_cost - cheaper_tier_cost` for a session). For 'verification' and 'iteration' moment types, you'll need new helpers that price 'next 2 turns' or 'the accepted assistant response' respectively. Use the same `COST_CHARS_PER_TOKEN`/`COST_OUTPUT_TO_INPUT_RATIO` constants for parity.
  - US-051 (renderer disclaimer): still purely a reports-layer change. None of the three aggregators in cost_ledger.py emit a disclaimer string -- that's intentional, the disclaimer literal lives in the renderer that consumes CostLedger/BiggestLine/TierFitSavings.
  - One Pyright false-positive class to note for future iterations: `tmp_home` fixture parameters are flagged 'not accessed' on lines 225/229/234/238/243/256/653/657/661/666. The fixture's side effect IS what the test wants (monkeypatch sets PRAXIS_* env vars + Path.home()). Same warnings predated US-049; I added some new ones in the same shape.
---

## Iteration 4 - US-050: dollar_impact_estimate per moment type
- What I did: Added a pure dispatcher `compute_moment_dollar_impact_usd(dim_key, model_hint, *, next_two_turn_chars, accepted_response_chars, session_total_input_chars)` to praxis/scoring/cost_ledger.py. Applies the spec section 10.2 rule per dim_key: verification + iteration delegate to `estimate_session_cost_usd`, fit delegates to `estimate_tier_fit_savings_for_session`, planning/context/tools/unknown return None unconditionally. Added 20 tests in tests/test_cost_ledger.py (60 -> 80) covering each path, the None-for-no-signal contract, and the cross-wiring guard.
- Files changed: praxis/scoring/cost_ledger.py, tests/test_cost_ledger.py, scripts/ralph/feature/cost-ledger/prd.json
- Verification:
  - python -m pytest tests/test_cost_ledger.py -v -> 80 passed (60 from US-047/US-048/US-049 + 20 new)
  - python -m pytest -q -> 774 passed
  - pyright praxis/scoring/cost_ledger.py tests/test_cost_ledger.py -> 0 errors, 0 warnings
  - mypy praxis/scoring/cost_ledger.py tests/test_cost_ledger.py -> 0 errors in the two source files (same 23 pre-existing errors in trajectory.py / judge.py / aggregate.py / advisor.py as documented in iterations 1-3 -- unchanged)
- What the next iteration should know:
  - One new public symbol joined cost_ledger.py: `compute_moment_dollar_impact_usd`. The constant `_MOMENT_DIM_KEYS_WITHOUT_IMPACT = {'planning', 'context', 'tools'}` is module-private; the rubric module already owns the canonical dim_key list, the frozenset here is just a local mirror so the cost layer doesn't pull `praxis.scoring.rubric` in.
  - Dispatcher API shape: dim_key + model_hint positional, then three keyword-only chars inputs (one per rule). Each dim_key reads exactly one of those kwargs; the others are ignored. `test_moment_impact_only_relevant_kwarg_consulted` locks that in so future refactors don't accidentally cross-wire.
  - Caller contract for filling the kwargs (not implemented yet -- this is for the orchestrator/cost-attribution pass that will populate `Moment.dollar_impact_estimate` per spec 10.2):
    - For a verification moment: count chars in the 2 turns after the lapse turn. The lapse turn is `Moment.turn_index` (per judge.py prompt: 0-indexed *user* turn). 'Next 2 turns after the lapse' is intentionally rough -- spec section 10.2 says 'the work the user had to redo'. Reasonable read: 2 turns immediately after the user turn that lapsed (typically: assistant response + user followup). The dispatcher does not enforce a particular slicing rule; it just prices whatever chars you give it.
    - For an iteration moment: chars in the assistant response the user accepted prematurely. Typically the assistant turn at index (turn-counted-by-role) just before `Moment.turn_index`. Same caveat: dispatcher prices whatever you pass.
    - For a fit moment: total session input chars (concatenation of all turn contents, or just user-turn contents -- the existing tier-fit panel was tested with arbitrary `total_input_chars`, so either definition is acceptable as long as the cost-attribution pass and US-049's caller agree). The current praxis.scoring.features doesn't expose this directly; the orchestrator will probably compute it as sum(len(t.content) for t in session.turns) or similar.
  - 'Token cost' interpretation: spec 10.2 says 'rough estimate from token volume + tier pricing'. Verification and iteration both use estimate_session_cost_usd (4 chars/token, 1.5x output ratio), which models the input AND a 1.5x output stipend. For 'iteration = cost of the assistant response' that's an overestimate vs. pure-output cost, but it keeps the per-moment number internally consistent with the weekly ledger so the ratios across moments are stable. The disclaimer at the bottom of the digest (US-051) handles the framing.
  - Zero chars -> $0, not None. A 'verification moment with no chars in the next 2 turns' still has a defined attribution ($0), same as `estimate_session_cost_usd("claude-opus-4-7", 0) == 0.0` and `estimate_tier_fit_savings_for_session("claude-opus-4-7", 0) == 0.0`. Locked in by test_moment_impact_*_zero_chars_is_zero. This matters for renderers: 'dollar_impact_estimate is None' means 'we couldn't price this', 'dollar_impact_estimate == 0.0' means 'priced, just nothing to attribute'.
  - US-051 (renderer disclaimer): this is the last story. The cost-attribution pass that *calls* compute_moment_dollar_impact_usd to populate Moment.dollar_impact_estimate is NOT done yet -- the dispatcher is in place but no orchestrator code wires it up. US-051 is specifically about the renderer-side disclaimer literal ('rough estimate from token volume + tier pricing'), so it doesn't strictly need the cost-attribution pass to land first. But the disclaimer existing without any populated dollar_impact_estimate values is awkward. Decide in US-051 whether to also wire up the attribution pass, or to keep US-051 purely renderer-scoped.
  - No new pyright/mypy false positives introduced beyond the same tmp_home-not-accessed warnings carried forward from iterations 1-3.
---
