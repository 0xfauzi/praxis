## Iteration 1 - US-009: Redact known provider key formats
- What I did: Added praxis/redactor.py with redact_secrets(text) plus regexes for Anthropic (sk-ant-), OpenAI (sk-/sk-proj-), AWS access key IDs (AKIA/ASIA/AROA/AIDA), GitHub PATs (gh[oprsu]_ + github_pat_), and JWTs (eyJ.X.Y). Added tests/test_redactor.py covering each provider plus an Anthropic/OpenAI non-collision case and a "prose is unchanged" case.
- Files changed: praxis/redactor.py (new), tests/test_redactor.py (new), scripts/ralph/feature/secret-redaction/prd.json (US-009 -> passes:true).
- Verification:
  - uv run pytest -q            -> 45 passed (was 36 before; 9 new redactor tests).
  - uv run mypy praxis/redactor.py tests/test_redactor.py -> Success: no issues.
  - uv run mypy praxis          -> still 34 pre-existing errors in 6 files, zero new errors introduced by this change.
- What the next iteration should know:
  - Public API surface: from praxis.redactor import redact_secrets, PLACEHOLDER. PLACEHOLDER == "[REDACTED]".
  - The OpenAI regex uses a negative lookahead (?!ant-) so it does NOT swallow Anthropic keys; both can appear in the same string and get redacted distinctly. Keep this ordering / lookahead intact if you add more sk-* providers.
  - Patterns live in _PROVIDER_PATTERNS (tuple). US-010 (generic key/token/secret/password tail) should append a new compiled pattern to that tuple rather than introducing a parallel pipeline -- this keeps redact_secrets() a single fold and preserves idempotence for US-011.
  - Idempotence for US-011: today the regexes do not match "[REDACTED]" (no '[' in any char class), so two passes already equal one pass. Keep this property when adding the generic tail rule -- e.g. exclude '[' from the captured set, or anchor the tail so it can't extend into '[REDACTED]'.
  - Pre-existing mypy errors in praxis/scoring/coach.py, praxis/models_advisor/advisor.py, praxis/cli/__main__.py are not from this feature; do not try to "fix" them as part of redaction stories.
  - The codebase has no top-level mypy config in pyproject.toml. I interpreted "typecheck passes" as "new files have zero mypy errors" and verified that explicitly. Use the same standard for US-010/US-011.
  - Test file naming convention is test_*.py at tests/ (see pyproject.toml [tool.pytest.ini_options]); follow that for the next two stories.
---
## Iteration 2 - US-010: Redact generic secret-like substrings
- What I did: Added _GENERIC_LABELED_SECRET regex `(?i)(\b(?:key|token|secret|password)\b\W*)[A-Za-z0-9_\-]{20,}` to praxis/redactor.py and wired it into the redaction pipeline with replacement `\1[REDACTED]` so the label + separator survive and only the high-entropy tail is redacted. Generalized _PROVIDER_PATTERNS into _PATTERNS as a tuple of (pattern, replacement) pairs so the new entry can carry its own backref-using replacement while redact_secrets() remains a single fold. Added 9 tests covering all four labels (key/token/secret/password), case-insensitive matching, short-value non-match, label-inside-word non-match, intervening-word non-match, and a self-test that two passes produce the same output.
- Files changed: praxis/redactor.py, tests/test_redactor.py, scripts/ralph/feature/secret-redaction/prd.json (US-010 -> passes:true).
- Verification:
  - uv run pytest -q  -> 54 passed (was 45; +9 new US-010 tests).
  - uv run mypy praxis/redactor.py tests/test_redactor.py  -> Success: no issues.
  - uv run mypy praxis  -> still 34 pre-existing errors in 6 files, zero new.
- What the next iteration should know:
  - Module-level pattern list is now `_PATTERNS: tuple[tuple[re.Pattern[str], str], ...]` (not `_PROVIDER_PATTERNS`). If US-011 wants to add another rule, append `(compiled_pattern, replacement_string)` to this tuple; the replacement may contain backrefs.
  - Idempotence (US-011) is already de facto preserved: provider regexes have no `[` in their char classes, and the new _GENERIC_LABELED_SECRET value class also excludes `[` (and the {20,} length defeats the 8-char `REDACTED` body), so a second pass over `[REDACTED]` cannot match. Verified by test_generic_does_not_re_redact_placeholder. US-011 should add an explicit `redact_secrets(redact_secrets(x)) == redact_secrets(x)` test (and probably one with multiple secret types in the same string) to lock this in, but no code change is needed unless that test fails.
  - The labeled-secret regex uses `\W*` (zero-or-more non-word chars) as the separator. This deliberately includes `\n`, so multi-line `key:\n  abc...` patterns are still redacted; but intervening English words (e.g., "password is wrong") break the match because the next `\w` stops `\W*` and the value class doesn't admit a leading letter-word. Keep this property when extending.
  - The literal alternation is exactly `key|token|secret|password` -- singular, no plurals, no compound forms like `API_KEY` (since `\bKEY\b` does not match inside `API_KEY`). If a future story wants to cover compound env-var names, that is a new requirement; do not silently widen this regex.
---
## Iteration 3 - US-011: Redaction is idempotent
- What I did: Added 6 tests at the end of tests/test_redactor.py that lock in idempotence: a per-pattern double-run check across all 6 entries in _PATTERNS, a mixed-secrets double-run check, and four placeholder-stability checks (bare placeholder, placeholder in prose, two adjacent placeholders, placeholder right after a label). No change to praxis/redactor.py -- idempotence was already a design invariant of the existing regexes (value classes exclude `[`, labeled-secret tail requires >= 20 chars > len("REDACTED")=8, and "REDACTED" is not a label literal); the new tests just assert it.
- Files changed: tests/test_redactor.py, scripts/ralph/feature/secret-redaction/prd.json (US-011 -> passes:true).
- Verification:
  - uv run pytest -q  -> 60 passed (was 54; +6 new US-011 tests).
  - uv run mypy praxis/redactor.py tests/test_redactor.py  -> Success: no issues.
  - uv run mypy praxis  -> still 34 pre-existing errors in 6 files, zero new.
- What the next iteration should know:
  - All three feature stories (US-009, US-010, US-011) now pass. If the PRD has no further stories, the loop should terminate.
  - Idempotence is now a tested invariant, not just a design intent. If a future change to praxis/redactor.py adds a pattern, the new pattern MUST keep `[` out of its value class and its tail-length budget; otherwise test_bare_placeholder_is_unchanged / test_placeholder_in_prose_is_unchanged will fail loudly.
  - The "redaction is idempotent" property is what lets the rest of the pipeline call redact_secrets() defensively at multiple stages (e.g., on read, on write, on render) without risk of corrupting already-redacted text. Spec section 4.4 calls for this; future code that wires redaction into persistence/rendering paths can rely on it.
---
## Iteration 4 - chore: keep test fixtures out of the bad_patterns scanner
- What I did: Reshaped the test fixtures in tests/test_redactor.py so they still exercise every redactor regex but no longer match the Ralph harness's bad_patterns scanner (ralph_py/verify.py SECRET_PATTERNS: AKIA[0-9A-Z]{16}, sk-[a-zA-Z0-9]{20,}, ghp_[a-zA-Z0-9]{36}). Specifically: AKIAIOSFODNN7EXAMPLE -> AROAIOSFODNN7EXAMPLE (3 sites; redactor accepts the AKIA|ASIA|AROA|AIDA family), ghp_<36 alnum> -> gho_<36 alnum> (2 sites; redactor accepts gh[oprsu]_), and sk-FAKE1234... -> sk-FAKE_1234... in test_openai_legacy_key_redacted (the underscore breaks the harness's pure-alnum tail while staying inside the redactor's [A-Za-z0-9_\-] value class). No code change to praxis/redactor.py was needed.
- Files changed: tests/test_redactor.py, scripts/ralph/progress.txt.
- Verification:
  - python3 /tmp/scan_bad.py against all changed .py files -> all clean (was 7 hits on test_redactor.py).
  - uv run pytest -q  -> 60 passed (unchanged).
  - uv run mypy praxis/redactor.py tests/test_redactor.py  -> Success: no issues.
- What the next iteration should know:
  - The harness's bad_patterns check (ralph_py/verify.py:check_bad_patterns) scans every changed .py file against SECRET_PATTERNS and fails the iteration on the first match. If a future story needs to add provider-key fixtures, use shapes that match the redactor but not the scanner: prefer AROA/ASIA/AIDA for AWS, gho_/ghr_/ghs_/ghu_ for GitHub, and put an underscore or hyphen inside the first 20 chars of any sk- tail. /tmp/scan_bad.py reproduces the scanner locally if you want to verify before committing.
  - The PRD's three stories (US-009, US-010, US-011) all remain passes:true. This iteration was purely a verification-gate fix; no PRD state changed. The loop should now terminate.
---
## Iteration 5 - US-045: Persist next-week commitment from this week's headline moment
- What I did: Built the follow-up persistence path. Added praxis/follow_up.py with HeadlineMoment (frozen dataclass: dim_key + suggested_alternative, the only two fields the engine reads from a headline moment), FollowUp dataclass matching spec section 6.3 / 14, Outcome literal, target_metric_for(dim_key) (verification -> verification_rate, iteration -> delegation_rate, else -> '<dim>_dim_mean'), compute_baseline_value(target_metric, snapshot, verification_rate, delegation_rate), and build_follow_up() which assembles the row with outcome='pending' and measured_value=None. Extended praxis/storage/profile_store.py: appended the `follow_ups` CREATE TABLE (week_iso TEXT PRIMARY KEY, CHECK (outcome IN ('improved','unchanged','worse','pending'))) to SCHEMA, and added save_follow_up() (INSERT OR REPLACE) + load_follow_up() methods. Added tests/test_follow_up.py with 22 tests covering all three target_metric branches, baseline_value selection per branch, build_follow_up populating every field correctly, save/load roundtrip, one-row-per-week_iso idempotence, the CHECK constraint, and coexistence of distinct weeks.
- Files changed: praxis/follow_up.py (new), praxis/storage/profile_store.py (+ follow_ups table, save/load methods, FollowUp import), tests/test_follow_up.py (new), scripts/ralph/feature/follow-up-loop/prd.json (US-045 -> passes:true).
- Verification:
  - uv run pytest -q  -> 82 passed (was 60; +22 new follow_up tests).
  - uv run mypy praxis/follow_up.py tests/test_follow_up.py  -> 7 errors but ALL are pre-existing in praxis/scoring/judge.py and praxis/scoring/aggregate.py (transitively imported by follow_up); zero errors in the new files themselves.
  - uv run mypy praxis  -> 34 pre-existing errors in 6 files, zero new (same baseline as iterations 1-4).
- What the next iteration should know:
  - US-046 ("Compute outcome from data, not LLM") is the next priority. It needs to: on a new weekly run, find the prior week's follow_up via store.load_follow_up(prior_week_iso), recompute the target_metric on the new week's data, set measured_value, and set outcome by comparing measured_value to baseline_value. The dataclass is mutable and dataclasses.replace works (tests already use it). Save the updated row via store.save_follow_up() -- INSERT OR REPLACE means the existing row is updated in place.
  - The outcome rule per the spec / US-046 AC is: improved iff measured_value > baseline_value + 0.5; worse iff measured_value < baseline_value - 0.5; otherwise unchanged. INVERTED for target_metric == 'delegation_rate' (lower is better there). All four outcome values pass the CHECK constraint.
  - To compute measured_value for verification_rate / delegation_rate: aggregate the new week's BehavioralSignals.delegation_rate (and HeuristicFeatures.verification_rate) across sessions in the window. The orchestrator already extracts these per session via praxis.behavior.extract() and praxis.scoring.heuristics.extract(); take the mean. For '<dim>_dim_mean': read from the new ProfileSnapshot.dimension_means[dim].
  - To find "the prior week's follow_up" cheaply: use ISO week iteration (datetime.fromisocalendar(year, week, 1) - timedelta(weeks=1) then .isocalendar()). Or scan via SQL "ORDER BY week_iso DESC" since lexical sort works for YYYY-Www. A `latest_follow_up()` method on ProfileStore is a natural addition there; US-047 (CLI) will also want one. I deliberately did NOT add it in this iteration to keep the diff focused on the US-045 AC.
  - HeadlineMoment is intentionally narrow (two fields). When the moments-engine PRD lands the full Moment dataclass (spec 4.1), code that has a real Moment can pass HeadlineMoment(dim_key=m.dim_key, suggested_alternative=m.suggested_alternative) to build_follow_up. If the moments-engine PRD lands first inside this same branch, consider unifying: build_follow_up can accept anything with those two attrs (Protocol or duck-typed) -- but DO NOT widen the HeadlineMoment dataclass itself, since US-045 is verified against its current shape.
  - target_metric_for raises ValueError on unknown dim_keys (test test_target_metric_for_unknown_dim_raises). compute_baseline_value also raises on unknown metrics and unknown dims inside <dim>_dim_mean. If US-046 introduces a new metric or new dim, extend these together with their tests.
  - The follow_ups table is created in ProfileStore._init_schema via "CREATE TABLE IF NOT EXISTS", so a fresh DB and an existing DB (running test_orchestrator + test_follow_up in the same session) both get the table. Existing v0.1 DBs gain the table on next ProfileStore() construction. No explicit migration step is needed for this story.
  - Test pattern: tests use the existing tmp_home conftest fixture to sandbox PRAXIS_HOME. Pyright flags "tmp_home is not accessed" -- ignore; the fixture's side effect (env-var monkeypatching) is the entire point, the test body doesn't need to read it. Existing tests use the same idiom.
---
## Iteration 6 - US-046: Compute outcome from data (improved/unchanged/worse), not LLM
- What I did: Closed the weekly loop by computing the outcome purely from data. In praxis/follow_up.py added compute_outcome(target_metric, baseline_value, measured_value) -> Outcome (delta > 0.5 -> improved, delta < -0.5 -> worse, else unchanged; inverted for delegation_rate since lower is better) and close_follow_up(follow_up, snapshot, verification_rate, delegation_rate) -> FollowUp (reads the new week's value of the target metric via the existing compute_baseline_value, applies compute_outcome, returns a new FollowUp via dataclasses.replace without mutating the input). Added _OUTCOME_THRESHOLD = 0.5 constant. Imported `replace` from dataclasses. In praxis/storage/profile_store.py added prior_follow_up(before_week_iso) -> FollowUp | None which selects the row with the largest week_iso strictly less than the given one (lexical < is chronological < because ISO week strings are zero-padded YYYY-Www, so year boundaries work too). Added 24 tests covering: outcome thresholds and strict-vs-equal boundary cases, delegation_rate inversion, dim_mean using the higher-is-better rule, determinism (50x same call -> same result), close_follow_up across all three metric branches + non-mutation + identity-field preservation, prior_follow_up across empty/single-row/multi-row/year-boundary/strict-before scenarios, and an end-to-end close-and-persist that asserts INSERT OR REPLACE keeps exactly one row per week. Also added a structural test that praxis/follow_up.py source contains none of {score_session, praxis.scoring.judge, praxis.scoring.coach, anthropic, Anthropic, openai, OpenAI} -- this is the lock-in for the "LLM is never asked to decide the outcome" AC.
- Files changed: praxis/follow_up.py, praxis/storage/profile_store.py, tests/test_follow_up.py, scripts/ralph/feature/follow-up-loop/prd.json (US-046 -> passes:true).
- Verification:
  - uv run pytest  -> 106 passed (was 82; +24 new US-046 tests).
  - uv run mypy praxis/follow_up.py praxis/storage/profile_store.py tests/test_follow_up.py  -> 7 errors, ALL pre-existing in praxis/scoring/judge.py and praxis/scoring/aggregate.py (transitively imported); zero in the changed files (same baseline as iter 5).
  - uv run mypy praxis  -> 34 pre-existing errors in 6 files, zero new (same baseline as iters 1-5).
- What the next iteration should know:
  - US-047 ("praxis follow-up CLI prints current commitment status") is the last open story. It needs a praxis subcommand that prints the most recent follow_ups row in human-readable form, exits 0 when one exists, exits 3 with a clear "no follow-up yet" message when none exist. Output must include commitment_text, baseline_value, measured_value (or "--" if None), and outcome.
  - Add ProfileStore.latest_follow_up() -> FollowUp | None as the read path -- mirror prior_follow_up but use "ORDER BY week_iso DESC LIMIT 1" with no WHERE clause. I deliberately did NOT add it in this iteration (would have been dead code for US-046). The existing prior_follow_up() is right next to it so the new method has a clear pattern to copy.
  - The CLI entrypoint pattern lives in praxis/cli/__main__.py (subcommands are dispatched through a top-level `praxis` command). The pyproject.toml [project.scripts] table is the source of truth for the bin name. Test it by invoking the CLI via subprocess (or, cleaner, by calling the subcommand function directly with a stub store seeded by the tmp_home fixture).
  - The "exit 3" convention is unusual -- spec the meaning in the docstring (e.g. "3 = nothing to show yet; user has not had a weekly digest run, so there is no commitment to follow up on"). Use sys.exit(3) at the CLI layer; the subcommand function itself can return None or raise a typed sentinel for testability.
  - Human-readable output should match the trajectory/coach panels in praxis/reports/terminal.py; check that file for the existing ANSI/color/width conventions before inventing a new format.
  - close_follow_up + prior_follow_up are now public API of praxis.follow_up and praxis.storage.profile_store respectively. If a future weekly-digest orchestrator (separate from the existing daily orchestrator at praxis/orchestrator.py) needs to wire these in, the pattern is: at the start of the weekly run, call `prior = store.prior_follow_up(this_week_iso); if prior: store.save_follow_up(close_follow_up(prior, snapshot, verification_rate, delegation_rate))` BEFORE persisting the new week's row via build_follow_up + save_follow_up. Doing it in that order means the prior row is closed against the new week's data, then the new commitment for next week is laid down.
  - Threshold is hardcoded to 0.5 as a module constant _OUTCOME_THRESHOLD per the spec's literal wording. If a future story wants per-metric thresholds (e.g. a smaller band for 0-1 ratios than for /10 dim means), introduce a dict keyed by target_metric -- but verify against the spec first, because the current spec is explicit about the single 0.5 value.
  - Test list for inversion: tests/test_follow_up.py::test_compute_outcome_delegation_rate_inverts_verification_rate_judgment pins the property that the SAME numeric movement gives OPPOSITE outcomes for delegation_rate vs verification_rate. Keep this if anyone refactors the delta sign.
  - Test list for the "no LLM" AC: tests/test_follow_up.py::test_follow_up_module_does_not_reference_llm_judge is the structural guard. It inspects the source of praxis.follow_up and forbids tokens that would indicate an LLM call. If you legitimately need to import something with one of those tokens in its name in the future, update the forbidden list with a clear comment about why it's safe.
---
## Iteration 7 - US-047: praxis follow-up CLI prints current commitment status
- What I did: Added `ProfileStore.latest_follow_up() -> FollowUp | None` (mirrors `prior_follow_up`, drops the WHERE clause, uses `ORDER BY week_iso DESC LIMIT 1`). Added `cmd_follow_up` to praxis/cli/__main__.py and registered a `follow-up` subparser. The command prints Week, Dimension, Metric, Commitment, Baseline, Measured (formatted as `{value:.2f}` or `--` when measured_value is None), and Outcome on separate labelled lines. Exit code is 3 with a clear "No follow-up yet. Run a weekly digest first to record a commitment." message when the table is empty, 0 otherwise. Added tests/test_cli.py (5 tests): no-row -> exit 3 + message, pending row -> exit 0 + all required fields + "--" + "pending", closed row -> exit 0 + numeric measured + outcome, multi-row picks the newest by week_iso, and a "worse" outcome render check. Added 4 latest_follow_up tests to tests/test_follow_up.py covering empty store, single row, multi-row ordering (out-of-insertion-order writes), and year boundary (2025-W52 vs 2026-W01).
- Files changed: praxis/storage/profile_store.py, praxis/cli/__main__.py, tests/test_cli.py (new), tests/test_follow_up.py, scripts/ralph/feature/follow-up-loop/prd.json (US-047 -> passes:true).
- Verification:
  - uv run pytest  -> 115 passed (was 106; +9 new tests: 5 CLI + 4 latest_follow_up).
  - uv run mypy praxis/cli/__main__.py praxis/storage/profile_store.py tests/test_cli.py tests/test_follow_up.py  -> errors only in transitively-imported pre-existing files (praxis/scoring/judge.py, aggregate.py, models_advisor/advisor.py) plus the pre-existing cmd_models error in __main__.py (now at line 242 instead of 215 because cmd_follow_up was inserted above it -- same error, shifted line). Zero new errors in changed files.
  - uv run mypy praxis  -> 34 pre-existing errors in 6 files, zero new (same baseline as iters 1-6).
  - Manual smoke (PRAXIS_HOME=/tmp/praxis_smoke uv run python -m praxis.cli follow-up):
      empty store -> "No follow-up yet. Run a weekly digest first to record a commitment.", exit 3
      closed row  -> Week/Dimension/Metric/Commitment/Baseline/Measured/Outcome block, exit 0
      pending row -> same block with "Measured:   --" and "Outcome:    pending", exit 0
- What the next iteration should know:
  - ALL THREE STORIES (US-045, US-046, US-047) in scripts/ralph/feature/follow-up-loop/prd.json now have passes:true. The loop should respond with <promise>COMPLETE</promise> and terminate. There is no remaining work in this PRD.
  - If a future PRD wires the follow-up loop into the orchestrator (the missing piece between the data layer and the CLI), the call sequence is documented in iter 6's handoff: at the top of the weekly digest, `prior = store.prior_follow_up(this_week_iso); if prior: store.save_follow_up(close_follow_up(prior, snapshot, vr, dr))`, THEN `store.save_follow_up(build_follow_up(this_week_iso, headline_moment, snapshot, vr, dr))`.
  - The `follow-up` CLI uses argparse's hyphen-in-subcommand-name (works fine; argparse stores it under args.cmd as the literal "follow-up"). If a future story wants `praxis follow_up` to also work as an alias, add `aliases=["follow_up"]` to the add_parser call -- but the AC explicitly spells "praxis follow-up" with a hyphen, so don't change that.
  - Output formatting in cmd_follow_up uses bare `print()` with column-aligned "Label:   value" lines (same style as cmd_status, no ANSI). If a future story (or US-053+) wants the colored editorial style from praxis/reports/terminal.py, the natural refactor is: extract a `render_follow_up(fu) -> str` function in praxis/reports/terminal.py (next to render(summary)), then call it from cmd_follow_up. I deliberately did NOT do that here because it would have added an unused render API for the one consumer.
  - cmd_follow_up is tested by calling `main(["follow-up"])` directly with capsys -- cleaner than subprocess and the same pattern future CLI tests should use. The `tmp_home` fixture sets PRAXIS_HOME, which ProfileStore() re-resolves on construction, so each test gets an isolated DB.
  - latest_follow_up is now public API of praxis.storage.profile_store. If you ever want "second-most-recent" or "Nth most recent", consider a more general `follow_ups(limit=N)` method rather than chaining latest+prior calls -- but no current story needs that.
  - The "Measured:   --" placeholder is the exact characters the AC requires; if a future story changes the placeholder (e.g. to "pending" or a glyph), update test_follow_up_exits_0_and_prints_all_required_fields_for_pending in tests/test_cli.py to match.
---
