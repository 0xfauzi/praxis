# progress.txt: union of factory component logs

## Iteration 1 - US-079: install-weekly creates and loads the plist
- What I did: Added `praxis install-weekly` subcommand backed by `praxis/cli/install_weekly.py`. Generates ~/Library/LaunchAgents/co.praxis.weekly.plist (Label=co.praxis.weekly) using `schedule.day/hour/minute` from ~/.praxis/config.toml and loads it with `launchctl load -w`. Idempotent re-runs unload first. CLI exits 4 with stderr message on launchctl failure (InstallWeeklyError surfacing returncode + stderr).
- Files changed: praxis/cli/install_weekly.py (new), praxis/cli/__main__.py (cmd_install_weekly + subparser), tests/test_install_weekly.py (new, 12 tests), scripts/ralph/feature/macos-launch-agent/prd.json (passes=true), scripts/ralph/progress.txt (this entry).
- Verification: `uv run pytest -q` -> 1024 passed (12 new + 1012 existing). `uv run pyright praxis/cli/install_weekly.py` -> 0 errors. The 3 pre-existing pyright errors on cli/__main__.py (WeeklyRunSummary vs RunSummary) are unchanged before/after this commit; confirmed by git stash + pyright comparison.
- What the next iteration should know:
  - Patterns established:
    * Subprocess is monkeypatched via `praxis.cli.install_weekly.subprocess.run`; tests do not call real launchctl. Reuse this stub fixture (`fake_launchctl`) when implementing US-080 uninstall-weekly.
    * The plist path resolver `plist_path(home=...)` accepts a home override but defaults to `Path.home()`, which the tmp_home conftest fixture patches; that's how tests land the plist inside tmp_path.
    * `PLIST_LABEL = "co.praxis.weekly"` is exported from install_weekly.py; uninstall-weekly should import and reuse it (don't duplicate the string).
    * `_WEEKDAY_BY_NAME` maps 'sunday'..'saturday' to 0..6 per Apple's launchd Weekday spec.
  - Hooks for upcoming stories:
    * US-080 (uninstall-weekly): needs to `launchctl unload <plist>` then delete the file; if file missing or unload fails, must still exit 0 per AC. Reuse `plist_path()` and `PLIST_LABEL`.
    * US-081 / US-082 (--notify path inside `praxis week`): the existing `_post_notify` in cli/__main__.py already shells out to osascript and includes the trajectory label in the title. ACs may want osascript moved to a dedicated module so test stubbing is easier, and the BODY (not just title) should mention `~/.praxis/latest.html`. Today the body only references `~/.praxis/weeks/{iso}.html` -- check whether US-081's AC ("opening ~/.praxis/latest.html") wants the body changed.
    * US-083 (non-macOS path): cmd_install_weekly currently prints a placeholder stderr message on non-Darwin and returns 0. That placeholder is the seam to flesh out: print the systemd timer / Task Scheduler XML to stdout AND save to ~/.praxis/install-weekly-snippet.txt.
  - Gotchas:
    * The pre-existing pyright errors in cli/__main__.py (WeeklyRunSummary type mismatch at lines 145/150/332) are noise from an orchestrator/reports type-coupling issue unrelated to this story. Don't try to "fix" them as part of macos-launch-agent stories unless explicitly assigned.
    * `_resolve_praxis_command()` falls back to `[sys.executable, '-m', 'praxis.cli']` when `shutil.which('praxis')` returns None; this matters when running install-weekly outside an activated venv. Tests don't exercise the fallback path, but they should not need to.
---

## Iteration 2 - US-080: uninstall-weekly removes the plist
- What I did: Added `praxis uninstall-weekly` subcommand backed by `uninstall_weekly_macos()` in praxis/cli/install_weekly.py. It runs `launchctl unload` on the existing plist (best-effort) then deletes the file, returning True. When the plist does not exist, it returns False without shelling out, and the CLI exits 0 in both cases. Non-Darwin platforms exit 0 with an explanatory stderr message.
- Files changed: praxis/cli/install_weekly.py (added uninstall_weekly_macos + docstring update), praxis/cli/__main__.py (cmd_uninstall_weekly + 'uninstall-weekly' subparser), tests/test_install_weekly.py (added 6 tests, import uninstall_weekly_macos, header updated), scripts/ralph/feature/macos-launch-agent/prd.json (passes=true), scripts/ralph/progress.txt (this entry).
- Verification: `uv run pytest -q` -> 1030 passed (6 new + 1024 existing). `uv run pyright praxis/cli/install_weekly.py` -> 0 errors. `uv run pyright praxis/cli/__main__.py` -> 3 errors, identical to baseline before this commit (verified via `git stash` round-trip); they are the pre-existing WeeklyRunSummary issue documented in iteration 1.
- What the next iteration should know:
  - Patterns established:
    * `uninstall_weekly_macos(home=None)` returns `bool` indicating whether anything was actually removed. The CLI uses this to choose between "Removed..." vs "No weekly LaunchAgent found at..." messaging.
    * The "tolerate launchctl unload failure" rule -- launchctl returns non-zero when the job is not currently loaded, and we cannot distinguish that from a real error. uninstall always proceeds to delete the file. If a future story needs to surface real unload errors, it must add a separate code path that distinguishes them (e.g., parse stderr for the "Could not find specified service" text), not change this default.
  - Hooks for upcoming stories:
    * US-081 (--notify body / osascript): `_post_notify` in cli/__main__.py currently puts the trajectory label in the title only and uses a `~/.praxis/weeks/{iso}.html` body. AC US-081 wants the body to mention `~/.praxis/latest.html` -- check whether to change the body text or introduce a `latest.html` symlink/copy. Existing notify happy-path tests (if any) live in tests/ for the week command; grep for `_post_notify` before touching.
    * US-082 (trajectory in body + non-crashing osascript): `_post_notify` already wraps the osascript call in a broad try/except, but the AC says "if osascript fails for any reason, the run logs the failure and still exits 0" -- need a test that covers a FileNotFoundError (osascript binary missing) explicitly, since today's try/except is best-effort but unverified by tests.
    * US-083 (non-macOS install-weekly path): the placeholder "not yet implemented" stderr message in cmd_install_weekly is the seam to flesh out -- print the systemd / Task Scheduler XML to stdout AND save to ~/.praxis/install-weekly-snippet.txt. The matching non-macOS branch for cmd_uninstall_weekly is already a sensible no-op ("non-macOS has no scheduled job to remove."); US-083 may not need to touch it.
  - Gotchas:
    * `fake_launchctl` fixture is reusable but accumulates calls across `install_weekly_macos()` + `uninstall_weekly_macos()` in the same test, so slice the list from the install call count before asserting on uninstall's behavior (see test_uninstall_weekly_unloads_and_deletes_plist).
    * `cmd_uninstall_weekly` imports from `praxis.cli.install_weekly` inside the function (not at module top) to match the existing install pattern -- keeps non-macOS imports off the hot path.
---

## Iteration 3 - US-081: macOS notification via osascript
- What I did: Reworked `_post_notify` in praxis/cli/__main__.py so the AppleScript title is the fixed string "Praxis weekly read is ready" and the body is "Open ~/.praxis/latest.html to read." (per spec 13.2 / AC US-081). Dropped the `week_iso` parameter (no longer referenced) and the title-override-by-trajectory branch; the trajectory-label-in-body work is US-082's. Updated the caller in `cmd_week` to call `_post_notify()` with no args, removing the now-dead `traj_label` calculation.
- Files changed: praxis/cli/__main__.py (refactored `_post_notify`, simplified caller), tests/test_cli.py (added 4 US-081 tests + `fake_osascript` fixture), scripts/ralph/feature/macos-launch-agent/prd.json (passes=true), scripts/ralph/progress.txt (this entry).
- Verification: `uv run pytest -q` -> 1034 passed (4 new + 1030 existing). `uv run pyright praxis/cli/__main__.py tests/test_cli.py` -> 4 errors, all pre-existing (3 WeeklyRunSummary on __main__.py + 1 "pytest could not be resolved" on test_cli.py). Confirmed unchanged via `git stash` round-trip; my edits introduce 0 new pyright errors.
- What the next iteration should know:
  - Patterns established:
    * `fake_osascript` fixture in tests/test_cli.py monkeypatches `praxis.cli.__main__.subprocess.run` and records every call. Reuse it for US-082's "osascript fails -> run still exits 0" test (just have the stub raise FileNotFoundError or return non-zero).
    * `_post_notify()` now takes no arguments. US-082 must re-add `trajectory_label: str | None` (it was removed in this iteration since US-081 has no use for it) and the caller in `cmd_week` needs to recompute `summary.trajectory.label.value.title()` before passing it through. The pre-edit code is in the git log at 381704b for reference.
    * The body text MUST contain the literal substring `~/.praxis/latest.html` -- US-082's expanded body (with trajectory label) must keep that substring intact, otherwise `test_notify_body_mentions_latest_html` will fail.
    * The title MUST stay literally "Praxis weekly read is ready" -- `test_notify_title_is_fixed_string` asserts `'with title "Praxis weekly read is ready"'` verbatim. US-082 adds the trajectory label to BODY, not title.
  - Hooks for upcoming stories:
    * US-082 (trajectory label in body + osascript failure tolerance): Re-add `trajectory_label` parameter to `_post_notify`, then weave the label into the body (spec section 13.2 example: "Praxis: Drifting this week. Open ~/.praxis/latest.html for the detail."). Add an explicit test that monkeypatches subprocess.run to raise `FileNotFoundError("osascript missing")` and asserts the CLI still exits 0 -- today's broad try/except handles it but no test verifies the contract.
    * US-083 (non-macOS install-weekly): the `fake_osascript` fixture is irrelevant; that story is about `install-weekly` printing a systemd/Task Scheduler snippet to stdout and saving it to `~/.praxis/install-weekly-snippet.txt`. See progress entries for iter 1/2 for the placeholder seam in `cmd_install_weekly`.
  - Gotchas:
    * `latest.html` is a SYMLINK created by the HTML digest writer (praxis/reports/digest_html.py, US-064), not by `_post_notify`. Tests don't verify the symlink exists at notification time -- the contract is only that the body MENTIONS the path. If a future change to the notify path tries to validate the symlink before notifying, it would couple notification to the HTML writer's symlink semantics, which is a regression in separation of concerns.
    * The notification posts AFTER `_weekly_html_path(target_week).write_text(...)` in `cmd_week`. The "after writing the HTML" half of the AC is structural (it's the code order), so the test asserts an HTML file exists at notify time as a smoke check rather than ordering subprocess.run vs write_text directly.
    * Tests monkeypatch `sys.platform` via `monkeypatch.setattr(sys, "platform", "darwin")`. monkeypatch reverts at teardown, so the patch is fully scoped to one test even though `sys` is a process-wide module.
---

## Iteration 4 - US-082: Notification includes trajectory label and never crashes the run
- What I did: Re-added the `trajectory_label: str | None = None` parameter to `_post_notify` in praxis/cli/__main__.py and wove the label into the body using the format "<Label> this week. Open ~/.praxis/latest.html for the detail." (spec 13.2 example). The caller in `cmd_week` now maps `summary.trajectory.label.value` through a small `_TRAJECTORY_LABEL_DISPLAY` dict so raw enum strings like `stable_engaged` render as user-facing `Engaged`. Hardened the subprocess block to capture stdout/stderr and log non-zero exits explicitly, plus the broad `except Exception` already in place catches `FileNotFoundError` / `PermissionError` etc. so the run still exits 0.
- Files changed: praxis/cli/__main__.py (`_TRAJECTORY_LABEL_DISPLAY` dict, `_post_notify` takes `trajectory_label`, capture+log non-zero exit, caller wiring in `cmd_week`), tests/test_cli.py (5 new US-082 tests), scripts/ralph/feature/macos-launch-agent/prd.json (passes=true), scripts/ralph/progress.txt (this entry).
- Verification: `uv run pytest -q` -> 1039 passed (5 new + 1034 existing). `uv run pyright praxis/cli/__main__.py tests/test_cli.py` -> 4 errors, identical to baseline before this iteration (verified via `git stash` round-trip). My edits introduce 0 new pyright errors.
- What the next iteration should know:
  - Patterns established:
    * Enum -> display mapping for trajectory labels lives in `_TRAJECTORY_LABEL_DISPLAY` at module top of cli/__main__.py. Keys mirror `TrajectoryLabel` enum values ("learning", "stable_engaged", "stable_passive", "atrophying", "insufficient_data"). The fallback for unknown labels is `raw.title()`. If a new TrajectoryLabel variant lands, add it here so the notification body stays user-facing.
    * The display dict is identical in spirit to the terminal renderer's `_TRAJ_TITLE` (praxis/reports/terminal.py L98), but kept separate to avoid a cross-module import from the CLI hot path. If a third surface needs it, lift it into a shared helper module.
    * To stub `run_weekly` in CLI tests, monkeypatch `praxis.cli.__main__.run_weekly` (not the source module) -- the CLI captured a reference at import time. See `test_notify_body_uses_user_facing_trajectory_label`.
    * Constructing a minimal `WeeklyRunSummary` in tests requires only: week_iso, sessions=[], tasks=[], judge_results={}, moments=[], selection=None, snapshot (with session_count>=1), rendered_html, rendered_terminal, elapsed_seconds, trajectory. All other fields default. `ProfileSnapshot` can be built directly (not via `from_scores`) when you want a known session_count without seeding rows.
  - Hooks for upcoming stories:
    * US-083 (non-macOS install-weekly): the trajectory/notification path is done -- US-083 is fully on `cmd_install_weekly`'s non-Darwin branch. The placeholder stderr message is the seam: replace it with code that prints the systemd timer or Task Scheduler XML to stdout AND saves it to `~/.praxis/install-weekly-snippet.txt`. Exit 0. The matching non-macOS branch in `cmd_uninstall_weekly` is already a sensible no-op -- US-083 likely doesn't need to touch it.
    * The schedule config (day/hour/minute) for the systemd / Task Scheduler snippets lives in `~/.praxis/config.toml` under `[schedule]`. See praxis/cli/install_weekly.py for how the macOS path loads it; reuse the same config reader.
  - Gotchas:
    * The notification body's prefix is `"<Label> this week."`. For `INSUFFICIENT_DATA` this becomes `"Reading this week."` -- mildly awkward but matches the spec 13.2 example phrasing. If product wants different phrasing per-label, change the mapping; don't add special-case branches around the label string concatenation.
    * `subprocess.run(..., capture_output=True, text=True)` is now used so we can log stderr on non-zero exits. The previous US-081 implementation passed neither, which left the subprocess writing directly to the parent's stderr. The change is invisible to existing tests because `fake_osascript` returns a CompletedProcess with empty stdout/stderr.
    * Tests that stub `run_weekly` MUST set `tmp_home` AND `fake_api_key` fixtures, otherwise the exit-2 (no API key) gate fires before `run_weekly` is reached. See `test_notify_body_uses_user_facing_trajectory_label`.
---

## Iteration 5 - US-083: Non-macOS prints snippet and exits cleanly
- What I did: Added pure renderers `build_systemd_snippet` (Linux user-timer) and `build_task_scheduler_snippet` (Windows XML) plus a `write_non_macos_snippet(praxis_home=None, *, platform_override=None)` dispatcher in `praxis/cli/install_weekly.py`. The CLI's non-macOS branch in `cmd_install_weekly` now calls the dispatcher, prints the content to stdout, prints "Saved snippet to: <path>" to stdout, and exits 0. The snippet is persisted to `~/.praxis/install-weekly-snippet.txt` via the new `snippet_path()` helper. Windows is detected via `sys.platform.startswith("win") or == "cygwin"`; everything else (Linux, FreeBSD, etc.) gets systemd.
- Files changed: praxis/cli/install_weekly.py (renderers + dispatcher + day-name maps + `_format_praxis_exec`), praxis/cli/__main__.py (cmd_install_weekly non-Darwin branch rewritten), tests/test_install_weekly.py (14 new US-083 tests, ScheduleConfig import, expanded header docstring), scripts/ralph/feature/macos-launch-agent/prd.json (passes=true), scripts/ralph/progress.txt (this entry).
- Verification: `uv run pytest -q` -> 1053 passed (14 new + 1039 existing). `uv run pyright praxis/cli/install_weekly.py praxis/cli/__main__.py tests/test_install_weekly.py` -> 4 errors, identical to baseline before this iteration (3 WeeklyRunSummary on __main__.py + 1 pytest import on test_install_weekly.py). Confirmed unchanged via `git stash` round-trip; my edits introduce 0 new pyright errors.
- What the next iteration should know:
  - All 5 PRD stories now pass. Ralph should detect <promise>COMPLETE</promise> on the next iteration.
  - Patterns established (in case follow-on stories touch this surface):
    * `build_systemd_snippet(schedule, praxis_argv)` and `build_task_scheduler_snippet(schedule, praxis_argv)` are pure -- no I/O, no config load. Tests call them directly with `ScheduleConfig(...)` to pin output without touching the tmp_home config file.
    * `write_non_macos_snippet(praxis_home=None, *, platform_override=None)` is the I/O layer: it reads `~/.praxis/config.toml`, resolves the praxis argv, dispatches by platform, writes the snippet to disk, and returns `(path, content)`. Tests pass `platform_override="linux" | "win32" | "freebsd13"` to exercise dispatch without touching `sys.platform`.
    * Day-of-week dispatch uses two new dicts: `_SYSTEMD_DAY_BY_NAME` ("sunday" -> "Sun") and `_TASKSCHEDULER_DAY_BY_NAME` ("sunday" -> "Sunday"). Both normalize via lowercase strip; invalid day strings raise `InstallWeeklyError` (so the user gets a usable error instead of an invalid unit/XML file).
    * `snippet_path(home=None)` returns `<home or PRAXIS_HOME>/install-weekly-snippet.txt`, mirroring `plist_path` in shape so tests can pass an explicit home or rely on PRAXIS_HOME via tmp_home.
  - Gotchas:
    * `_resolve_praxis_command()` calls `shutil.which("praxis")`, which on Python 3.13 dives into `_winapi.NeedCurrentDirectoryForExePath` when `sys.platform == "win32"`. That attribute is None on macOS, so the test that monkeypatches `sys.platform = "win32"` must ALSO stub `praxis.cli.install_weekly.shutil.which` to avoid an AttributeError crash. See `test_cli_install_weekly_windows_saves_task_scheduler_xml`. Tests that use the `platform_override=` parameter (not sys.platform monkeypatching) don't need this stub because `shutil.which` still sees the host's real platform.
    * The CLI prints the snippet body AND the "Saved snippet to:" line to stdout (not stderr). This matches the existing macOS branch ("Installed weekly LaunchAgent: ...") which also goes to stdout. If a user pipes stdout to a file, both lines will land there; that's deliberate so the saved unit text is self-contained.
    * Task Scheduler XML uses a fixed `StartBoundary` of `2020-01-01T<hh>:<mm>:00` so the trigger is immediately valid. If a future story needs date-aware output (e.g., to start "next Sunday"), inject a clock; don't randomize the fixed string.
---

## Iteration 6 - US-028: Tool detection + per-tool prompts
- What I did: Added `praxis install-coach` subcommand backed by `praxis/cli/install_coach.py`. The module detects Claude Code (`~/.claude/settings.json` or `~/.claude/projects/`), Codex (`~/.codex/`), and Copilot (any VS Code workspace storage with chatSessions or state.vscdb -- reuses `praxis.scanners.copilot._vscode_user_paths`). For each detected tool the CLI prompts `Found <Tool>. Install the Praxis coaching hook? [Y/n]:` (default Y). `--yes` skips prompts; `--tool NAME` restricts to one tool; `--all` iterates every known tool regardless of detection. `--tool` and `--all` are mutually exclusive at the argparse layer. The empty-detection branch prints `No supported AI tools detected. Pass --all to install anyway.` and exits 0 (not non-zero, per AC). `install_for_tool(tool)` is a placeholder that prints `Installing <Tool> coaching hook... (not yet implemented)` -- US-029/030/031 replace each branch with the real installer.
- Files changed: praxis/cli/install_coach.py (new), praxis/cli/__main__.py (cmd_install_coach + subparser), tests/test_install_coach.py (new, 25 tests), scripts/ralph/feature/install-coach-uninstall-coach/prd.json (US-028 passes=true), scripts/ralph/progress.txt (this entry).
- Verification: `uv run pytest -q` -> 1094 passed (25 new + 1069 existing; baseline at start of branch was 1053, drift from upstream merges accounts for the rest). `uv run mypy praxis` -> `Success: no issues found in 50 source files`. The 3 pre-existing pyright "Code is unreachable" warnings inside `__main__.py` on the `sys.platform != "darwin"` branches and the pytest-import warning in tests are unchanged.
- What the next iteration should know:
  - Patterns established:
    * Tool slugs live in `praxis.cli.install_coach` as `TOOL_CLAUDE_CODE = "claude-code"`, `TOOL_CODEX = "codex"`, `TOOL_COPILOT = "copilot"` with the canonical iteration order in `ALL_TOOLS`. US-029/030/031/032 must import these (not re-stringify) so the sentinel format stays consistent across install + uninstall.
    * `install_for_tool(tool: str) -> None` is the single dispatch seam. US-029 replaces the Claude Code branch (currently the placeholder print) with a call to the new Claude Code installer; US-030 the Codex branch; US-031 the Copilot branch. Today there is no `if/elif` chain (just one placeholder print) -- US-029 should rewrite this function to an `if tool == TOOL_CLAUDE_CODE: ... elif TOOL_CODEX: ... elif TOOL_COPILOT: ...` pattern, with the placeholder kept in the unreached branches so the dispatcher continues to work mid-feature.
    * `_prompt_yes(tool_display)` is the user-prompt helper. Reuse it from the uninstall command (US-032) so the question phrasing is identical: `Found <Tool>. Install the Praxis coaching hook? [Y/n]:`. For the uninstall surface you'll want a parallel `_prompt_yes_uninstall` or a parameterized `_prompt_yes(verb=...)` -- pick whichever stays smaller.
    * `detect_copilot(vscode_user_dirs=None)` accepts an explicit iterable of VS Code user dirs so tests can scope detection to `tmp_path` without monkeypatching `platform.system()`. When `None`, it calls `praxis.scanners.copilot._vscode_user_paths()` -- which already respects `Path.home()` and therefore the `tmp_home` fixture's monkeypatch. If US-031's tests need to assert the per-platform path math, monkeypatch `platform.system` on the scanner module the same way `test_copilot_scanner_skips_malformed_json` does (it constructs `CopilotScanner(roots=[...])` instead).
  - Hooks for upcoming stories:
    * US-029 (Claude Code hook installer): The AC wants a SessionStart hook (`praxis nudge --format claude-code`) and a Stop hook (`praxis reflect --session-end --non-interactive-fallback`) merged into `~/.claude/settings.json`, each carrying `_praxisManaged: true` so uninstall (US-032) can identify them. Atomic write via tempfile + `os.replace`. Unparseable JSON aborts with a clear error and does NOT overwrite. NB: `praxis nudge` already exists as a subcommand (see knowledge `praxis-nudge-command`); the `--format claude-code` flag does NOT yet exist there -- US-029 may need to either (a) add the flag and have nudge dispatch on it, or (b) defer the format flag and have the hook command be just `praxis nudge` since the existing implementation already prints a single line. Check the PRD/spec for whether `--format` is load-bearing for US-029 or just a future-proofing thing.
    * US-030 (Codex hook installer): Writes `~/.codex/hooks.json` (creates default structure if absent), appends SessionStart+Stop entries with the sentinel. Sentinel detection means re-running must update-in-place rather than duplicate. Unwriteable dir must error cleanly without leaving a partial file.
    * US-031 (Copilot file injection): Manages a `<!-- praxisManaged:begin -->` / `<!-- praxisManaged:end -->` block in `<cwd>/.github/copilot-instructions.md` (creates the file if absent). Optionally (behind a confirm prompt) also writes `<vscode-user-data>/prompts/praxis-commitment.instructions.md` and updates user-level `settings.json` with `chat.instructionsFilesLocations`. A fixture-based byte-level preservation test is part of the AC -- write the test BEFORE the implementation.
    * US-032 (Symmetric uninstall): Removes only blocks/entries carrying the sentinel. The structural symmetry is the test: after `uninstall-coach`, re-running `install-coach` should produce a deep-equal config to a fresh install. The "Nothing to uninstall." path is exit 0 with no writes.
  - Gotchas:
    * `_prompt_yes` treats `EOFError` as "no" (so piped stdin doesn't hang). If a follow-up story wants different behavior (e.g., the CI/scripted case), prefer adding a `--yes` flag at the call site -- don't change the EOFError branch since at least one test asserts on it (`test_prompt_eof_treated_as_no`).
    * `--tool` is case-insensitive (`.strip().lower()`); pass canonical slugs (`claude-code`, `codex`, `copilot`) in any docs/help text. The error message lists the valid set so unknown values self-correct.
    * The placeholder `Installing <Tool> coaching hook... (not yet implemented)` line will surprise users of US-028 through US-031 if they run install-coach during the rollout. That's intentional: it gives them feedback rather than silence while the dispatcher's branches are filled in incrementally. The string can stay until US-029-031 land; US-029 should change the Claude Code branch to actually do work and remove the "(not yet implemented)" suffix on that one branch.
    * `detect_all()` calls `detect_copilot()` (no args). On dev machines with VS Code + Copilot installed, this can return True even under tmp_home (because the VS Code scanner reads from system paths, not tmp_home). Tests that need a fully deterministic "no tools detected" branch use the `no_copilot` fixture (`monkeypatch.setattr("praxis.cli.install_coach.detect_copilot", lambda: False)`). Reuse this pattern in US-029-032 tests so detection doesn't leak host state.
---

## Iteration 7 - US-029: Claude Code hook installer with sentinel + atomic write
- What I did: Implemented `install_claude_code(home=None) -> Path` in praxis/cli/install_coach.py. The function reads `~/.claude/settings.json` (treating absent or whitespace-only files as `{}`), merges Praxis-managed blocks for `SessionStart` (command `praxis nudge --format claude-code`) and `Stop` (command `praxis reflect --session-end --non-interactive-fallback`) into `data["hooks"]`, validates the result via `json.dumps`, and writes atomically through a sibling `tempfile.mkstemp` + `os.replace`. A new `InstallCoachError` is raised when the file is unparseable JSON, top-level is not a JSON object, or `hooks` exists but isn't a JSON object -- in every error path the on-disk file is left untouched. The `install_for_tool` dispatcher now routes Claude Code to this installer and prints `Installed Claude Code coaching hook: <path>` (errors go to stderr, the run continues to the next tool).
- Files changed: praxis/cli/install_coach.py (`InstallCoachError`, `SENTINEL`, `CLAUDE_HOOK_COMMANDS`, `claude_settings_path`, `_build_claude_block`, `_atomic_write_json`, `install_claude_code`; rewrote `install_for_tool` to dispatch by tool slug), tests/test_install_coach.py (20 new US-029 tests covering fresh install, idempotence, user-block preservation, stale-block replacement, unparseable JSON, non-object top-level, non-object `hooks`, empty file, atomic-write cleanliness, home override, pretty-print, CLI integration), scripts/ralph/feature/install-coach-uninstall-coach/prd.json (US-029 passes=true), scripts/ralph/progress.txt (this entry).
- Verification: `uv run pytest -q` -> 1114 passed (20 new + 1094 existing). `uv run mypy praxis` -> `Success: no issues found in 50 source files`. The pre-existing pyright "pytest could not be resolved" + "tmp_home is not accessed" warnings in tests are unchanged (fixtures are by-name parameters; pyright can't see them).
- What the next iteration should know:
  - Patterns established:
    * `SENTINEL = "_praxisManaged"` and `CLAUDE_HOOK_COMMANDS` (a `dict[str, str]` mapping event name -> command string) are exported from `praxis.cli.install_coach`. US-030 (Codex) and US-032 (uninstall) MUST import these instead of re-stringifying so the sentinel format and command strings stay consistent across surfaces. US-031 (Copilot) uses the markdown markers, not these, but `SENTINEL` is still the right key name if any JSON-side Copilot artifact ever needs it.
    * `InstallCoachError` is the shared error type for per-tool installers. `install_for_tool` catches it and prints to stderr, intentionally NOT propagating: a bad Claude Code config should not block a Codex install. US-030/031 should raise the same exception type for symmetry; US-032 should likely catch it too for the uninstall path (though uninstall's failure mode is usually "file already gone, no-op").
    * `_atomic_write_json(path, data)` is a small helper -- tempfile in the same dir, `os.replace`, cleans up the tempfile on failure. Reuse it for Codex (US-030) which also writes a JSON file (`~/.codex/hooks.json`). It would currently need to be exported (it's underscore-prefixed); if US-030 wants to share it cleanly, rename to `atomic_write_json` and update one Claude-side test or two that import the public surface.
    * `claude_settings_path(home=None)` is the parameterizable path helper. US-032 (uninstall) should import this so the two commands always touch the same path.
    * Tests use `tmp_home` (which patches `Path.home`) + `no_copilot` (which stubs Copilot detection) to keep CLI tests deterministic on dev machines.
  - Hooks for upcoming stories:
    * US-030 (Codex `~/.codex/hooks.json`): the AC asks for SessionStart + Stop entries with the sentinel, idempotent re-runs, and a clear error on permission-denied. Shape per PLAN.md L356-368 is `{"hooks": [{"event": "SessionStart", "command": "..."}, {"event": "Stop", "command": "..."}]}`. Each entry should carry `_praxisManaged: true` (the same SENTINEL key). The shape is a list of single-event-entries, NOT a dict-of-event-lists like Claude Code -- don't copy-paste the Claude merge logic; write a parallel `install_codex()` that handles the flat-list shape. The atomic-write helper IS reusable.
    * US-031 (Copilot markdown injection): writes `<cwd>/.github/copilot-instructions.md` with a `<!-- praxisManaged:begin --> ... <!-- praxisManaged:end -->` block. Pure text manipulation, not JSON -- the atomic-write helper isn't useful (text, not json.dumps). A separate `install_copilot()` is the right shape. Spec calls out a byte-level preservation test for surrounding markdown content.
    * US-032 (uninstall-coach): symmetry test ("after uninstall + reinstall, deep-equal to fresh install") is the structural anchor. The uninstaller must remove ONLY blocks where `entry.get(SENTINEL) is True` (or markdown bounded by the `praxisManaged` markers for Copilot). Reuse `claude_settings_path`, `CLAUDE_HOOK_COMMANDS` keys (`SessionStart`, `Stop`), and `SENTINEL`. A `uninstall_claude_code(home=None) -> bool` returning whether anything was removed mirrors the install_weekly.py `uninstall_weekly_macos` shape.
  - Gotchas:
    * `install_claude_code` treats `raw.strip() == ""` as "fresh install" (no JSON parse). If a future change wants to error on whitespace-only files, that's a behavior change in the test `test_install_claude_code_empty_file_treated_as_fresh` -- update the test, don't quietly tighten the check.
    * The error messages embed the full `settings_path` and the literal command `praxis install-coach`. Tests assert both substrings (`test_install_claude_code_aborts_on_unparseable_json`). Don't reword the error without updating the test.
    * `install_for_tool` is the dispatch seam for US-030/031 -- right now Codex and Copilot still hit the placeholder branch ("not yet implemented"). Don't be tempted to delete those branches; they keep the dispatcher useful mid-feature rollout. When US-030 lands it should replace its `print(... not yet implemented)` line with a real installer call wrapped in try/except `InstallCoachError`, mirroring the Claude Code branch.
    * `_atomic_write_json` writes the tempfile with `prefix=f".{path.name}."` so a leftover (which shouldn't happen, but) is easy to spot and clean up manually. Tests assert no leftovers via `iterdir()`.
    * The CLI test `test_cli_install_coach_claude_unparseable_prints_error_continues` asserts `code == 0` even on Claude install failure. This is the deliberate choice from the dispatcher: stderr-print and continue, not abort. The AC says "the installer aborts" -- "the installer" being Claude-specific, not the whole install-coach. If product decides this should exit non-zero, the change is in `install_for_tool`, NOT in `install_claude_code`.
---

## Iteration 8 - US-030: Codex CLI hook installer
- What I did: Implemented `install_codex(home=None) -> Path` in praxis/cli/install_coach.py. Reads `~/.codex/hooks.json` (or starts from `{"hooks": []}` when absent or whitespace-only), drops any sentinel-tagged entries, appends fresh `SessionStart` (`praxis nudge --format codex`) and `Stop` (`praxis reflect --session-end --non-interactive-fallback`) entries each carrying `_praxisManaged: true`, then writes back via the existing `_atomic_write_json` helper. Raises `InstallCoachError` for unparseable JSON, non-object top level, non-array `hooks`, AND for `PermissionError` from the atomic write (mapped to a clear "permission denied writing to <dir>" message). The `install_for_tool` dispatcher routes Codex to this installer and prints `Installed Codex coaching hook: <path>` on success; errors go to stderr and the run continues to the next tool, mirroring the Claude Code branch.
- Files changed: praxis/cli/install_coach.py (`CODEX_HOOK_COMMANDS`, `codex_hooks_path`, `_build_codex_entry`, `install_codex`; rewired Codex branch in `install_for_tool`; updated module docstring), tests/test_install_coach.py (19 new US-030 tests covering fresh install, both commands, sentinel, parent-dir creation, idempotence, stale replacement, user-authored preservation, unrelated top-level keys preserved, unparseable JSON, non-object top-level, non-array hooks, empty file, atomic-write cleanliness, home override, permission-denied via monkeypatch, codex_hooks_path resolver, CLI integration happy path, CLI integration with unparseable input, default-detection path), scripts/ralph/feature/install-coach-uninstall-coach/prd.json (US-030 passes=true), scripts/ralph/progress.txt (this entry).
- Verification: `uv run pytest -q` -> 1133 passed (19 new + 1114 existing). `uv run mypy praxis` -> `Success: no issues found in 50 source files`. The pre-existing pyright "pytest could not be resolved" + "<fixture> is not accessed" warnings in tests are unchanged (pyright cannot see pytest fixtures).
- What the next iteration should know:
  - Patterns established:
    * `CODEX_HOOK_COMMANDS: dict[str, str]` mirrors `CLAUDE_HOOK_COMMANDS` -- keys are the event names (`SessionStart`, `Stop`), values are the literal shell commands. US-032 (uninstall) MUST import this so the events it strips match what the installer writes; don't re-stringify.
    * The Codex JSON shape is `{"hooks": [{"event": "...", "command": "...", "_praxisManaged": true}, ...]}` -- a FLAT list of single-event entries, NOT Claude's dict-of-event-lists. Don't try to share merge logic between the two; `install_codex` is intentionally parallel to `install_claude_code`.
    * `_build_codex_entry(event, command)` is the entry-shape factory. Use it from US-032 if you need to construct a Praxis-managed entry for a "deep-equal to fresh install" assertion.
    * `codex_hooks_path(home=None)` is the parameterizable path helper -- US-032 should import it so install + uninstall always touch the same path.
    * Permission-denied is handled by wrapping the `_atomic_write_json` call in a try/except `PermissionError` and re-raising as `InstallCoachError`. The message includes the directory path and the literal recovery command `praxis install-coach`; tests assert both substrings.
  - Hooks for upcoming stories:
    * US-031 (Copilot markdown injection): writes `<cwd>/.github/copilot-instructions.md` with a `<!-- praxisManaged:begin --> ... <!-- praxisManaged:end -->` block. Pure text manipulation, not JSON -- the atomic-write JSON helper is the wrong tool. Add a parallel `_atomic_write_text(path, content)` helper (or inline a small mkstemp/replace dance), and write `install_copilot()` against it. Spec calls out a byte-level preservation test for content outside the markers.
    * US-032 (uninstall-coach): `uninstall_codex(home=None) -> bool` should mirror `install_codex` -- read the file, filter OUT entries where `entry.get(SENTINEL) is True`, write back; if no managed entries existed, return False so the CLI can print "Nothing to uninstall.". For the "deep-equal after uninstall+reinstall" symmetry test, you'll want to assert that the round-trip preserves user entries' order. My current `install_codex` preserves filtered entries in original order (just appends ours), so the symmetry property already holds: install -> uninstall -> install = install on a fresh dir, modulo any pre-existing user entries which round-trip unchanged.
  - Gotchas:
    * The Codex permission-denied test uses `monkeypatch.setattr("praxis.cli.install_coach._atomic_write_json", _raise)` rather than chmod-ing the dir. This is portable across macOS/Linux/Windows and avoids the "running pytest as root bypasses chmod" trap (which actually fires in some CI containers). Reuse the same pattern in US-031/032 tests if a similar error path needs coverage.
    * `install_codex` calls `_atomic_write_json` which calls `path.parent.mkdir(parents=True, exist_ok=True)` -- so the parent dir is created if it doesn't exist. This is why `test_install_codex_creates_parent_directory` does NOT pre-create `~/.codex/`; the AC for US-030 says "creates a default structure", which includes the directory itself when absent. If a future spec change demands `install_codex` refuse to create the dir (e.g., to force the user to install Codex first), that's a behavior change in `_atomic_write_json` callers, NOT in the helper itself.
    * `_atomic_write_json` IS reused between Claude and Codex installers. It's underscore-prefixed (private) but both callers live in the same module, so this is fine. If US-031 needs JSON writes (it probably doesn't -- the Copilot path is markdown + a small settings.json patch for the prompts-location option), the same helper is the right call. If US-031's settings.json patch lands, lift `_atomic_write_json` to a public name in a single follow-up commit so the surface is consistent.
    * `install_for_tool` swallows `InstallCoachError` and prints to stderr, exiting 0 from the outer install-coach command. The AC for US-030 says "raises a clear error" -- "the installer" being Codex-specific, not the whole install-coach. This matches the US-029 dispatcher pattern; tests assert on stderr content rather than exit code for the CLI-level case.
---

## Iteration 9 - US-031: Copilot file injection (workspace + user-level)
- What I did: Implemented `install_copilot_workspace(cwd=None) -> Path` and `install_copilot_user_level(home=None) -> tuple[Path, Path]` in praxis/cli/install_coach.py. The workspace installer reads `<cwd>/.github/copilot-instructions.md` (empty string if absent), replaces or appends a marker-bounded Praxis block via `_replace_or_append_copilot_block`, and writes back through a new `_atomic_write_text` helper (also refactored `_atomic_write_json` to use it). The user-level installer writes `<vscode-user-dir>/prompts/praxis-commitment.instructions.md` and patches `chat.instructionsFilesLocations` in `<vscode-user-dir>/settings.json`, preserving unrelated keys + existing entries. `vscode_user_dir(home=None)` resolves the per-platform path (Darwin / Linux / Windows w/ APPDATA fallback). Rewired the Copilot branch in `install_for_tool` with two opt-in prompts: workspace (default Y, satisfied by `--yes`) and user-level (default N, NOT satisfied by `--yes` per AC). Added a generalized `_prompt_yes_text(prompt, *, default_yes)` helper; the existing `_prompt_yes` now delegates to it. Updated `install_for_tool` signature to accept `assume_yes` kwarg; `run_install_coach` propagates the flag.
- Files changed: praxis/cli/install_coach.py (constants, helpers, two installers, dispatch rewrite, generalized prompt helper, signature change on install_for_tool, propagation in run_install_coach), tests/test_install_coach.py (31 new US-031 tests covering path helpers, workspace install fresh+idempotent+replace+append+byte-preservation+atomic, vscode_user_dir per-platform, user-level prompt-file+settings.json round-trip+preservation+abort-on-bad-JSON+idempotent+non-dict-locations recovery+home override, CLI flows for --yes vs interactive y/y/y vs y/y/n vs y/n/n vs ""/""/"" vs unparseable user settings), scripts/ralph/feature/install-coach-uninstall-coach/prd.json (US-031 passes=true), scripts/ralph/progress.txt (this entry).
- Verification: `uv run pytest -q` -> 1164 passed (31 new + 1133 existing). `uv run mypy praxis` -> `Success: no issues found in 50 source files`. The pre-existing pyright "pytest could not be resolved" + "<fixture> is not accessed" warnings in tests are unchanged.
- What the next iteration should know:
  - Patterns established:
    * `COPILOT_MARKER_BEGIN = "<!-- praxisManaged:begin -->"`, `COPILOT_MARKER_END = "<!-- praxisManaged:end -->"`, `COPILOT_INSTRUCTION_FILENAME = "praxis-commitment.instructions.md"`, `COPILOT_SETTINGS_KEY = "chat.instructionsFilesLocations"` are exported from praxis.cli.install_coach. US-032 (uninstall) MUST import these so the markers + settings-key it strips match what the installer wrote; don't re-stringify.
    * `copilot_workspace_path(cwd=None)` is the path helper for `<cwd>/.github/copilot-instructions.md`. `vscode_user_dir(home=None)` is the per-platform VS Code User dir resolver (Darwin: `~/Library/Application Support/Code/User`; Linux: `~/.config/Code/User`; Windows: `%APPDATA%/Code/User`). Use these in US-032 for symmetric removal.
    * `_replace_or_append_copilot_block(content, new_block)` is the pure text manipulator. It replaces the existing markers-bounded block in place (byte-preserving the rest of the file) or appends with a blank-line separator. US-032's uninstall MUST mirror it: locate the begin/end markers, slice them out (along with the preceding blank-line separator if present), and write back. A naive `content.replace(block, "")` would leave a stray double blank line.
    * `_atomic_write_text(path, content)` is the reusable atomic-write helper for non-JSON files. `_atomic_write_json` now delegates to it. US-032 should use `_atomic_write_text` for the markdown round-trip and `_atomic_write_json` for the settings.json roll-back.
    * `install_for_tool(tool, *, assume_yes=False)` now takes `assume_yes` so the Copilot branch knows to skip the workspace prompt. The Codex/Claude branches ignore it (they have a single all-or-nothing install). US-032's `uninstall_for_tool` should likely follow the same shape.
    * The workspace prompt and user-level prompt are SEPARATE: the workspace prompt defaults Y (and `--yes` opts in); the user-level prompt defaults N and `--yes` does NOT opt in (the AC's "explicit prompt the user must confirm" forbids implicit opt-in via `--yes`). To opt into user-level non-interactively a future story would need to add a separate flag like `--copilot-user-level`.
  - Hooks for upcoming stories:
    * US-032 (uninstall-coach): `uninstall_copilot_workspace(cwd=None) -> bool` should read `<cwd>/.github/copilot-instructions.md`, slice OUT the marker-bounded block (and the preceding blank-line separator if present), and write back. Return True iff the block was removed. If the file becomes empty after removal, consider deleting it (mirrors "Nothing to uninstall" semantics) -- check AC for the exact behavior. `uninstall_copilot_user_level(home=None) -> tuple[bool, bool]` should delete the prompt file and remove `str(vscode_user_dir() / "prompts")` from `chat.instructionsFilesLocations`; if the locations dict becomes empty, remove the key entirely; if settings_data becomes empty, leave it as `{}` (don't delete the file -- user may have other settings the AC didn't mention).
    * US-032's "deep-equal after uninstall+reinstall" symmetry test for Copilot is structurally complex because the workspace file's surrounding content matters. Suggested approach: start with a "clean install" baseline (no user content, just the praxis block), uninstall (should leave an empty file or remove the file), reinstall (should produce the same baseline bytes). The same property should hold with user content surrounding the block: install -> uninstall -> install should be a noop modulo the praxis block.
  - Gotchas:
    * `Path.cwd()` is not auto-redirected by the `tmp_home` fixture (only `Path.home()` is). Tests for the Copilot dispatch use a new `tmp_cwd` fixture (`monkeypatch.chdir(tmp_path); return tmp_path`). Reuse it in US-032 tests.
    * The user-level surface needs `platform.system()` to be pinned for deterministic tests. Use the `force_darwin` fixture (`monkeypatch.setattr("praxis.cli.install_coach.platform.system", lambda: "Darwin")`) so all platform-dependent paths resolve under `tmp_home/Library/Application Support/Code/User`. Linux / Windows paths are tested separately by `test_vscode_user_dir_linux` / `test_vscode_user_dir_windows_*`.
    * The Copilot block content currently includes a static placeholder ("not yet set - run `praxis commit` to set this week's focus."). A future story will wire `praxis commit` to rewrite this block with the active commitment text. Don't be tempted to load the active commitment at install time -- the AC doesn't require it, and doing so couples install-coach to the ProfileStore schema unnecessarily.
    * `install_copilot_user_level` recovers gracefully when `chat.instructionsFilesLocations` is present but not a dict (e.g., a user accidentally typed a list). It overwrites the bad value with a proper dict containing the praxis prompts dir. Tests assert this in `test_install_copilot_user_level_overwrites_non_dict_locations`. If a future story wants to abort instead (preserving the malformed value for the user to see), change the recovery to a `raise InstallCoachError` and update that test.
    * The CLI dispatch for Copilot prints two summary lines on success ("Installed Copilot workspace surface: ..." and "Updated VS Code user settings: ..."). The test `test_cli_install_coach_copilot_workspace_with_yes` asserts the workspace file path is in stdout; future stories can extend the assertions on the user-level branch.
---
